# Chapter 13 — claim snapshot (as of the v0.14.0 release tag)

The chapter rests on four claim-register rows, snapshotted here with their
labels, scopes, and evidence classes as of the tag. The live view is always
[`docs/public-claim-register.md`](../../docs/public-claim-register.md).

## FCT-68 — The Pincer: Exactly Three Identity Modes

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (composition graded
  INTERNAL-BLIND, §7 of the paper)
- Scope: involutive identity modes of a ternary contact, given (S) + (R).
- Statement: a future-readable order scar forces a third anticommuting
  involutive mode (the two-mode algebra's anticommutant is exactly
  `span{XZ}`, squares negative — no involution); the carrier-parity model
  caps the family at `f(n) =` largest odd `≤ n` (reverse oddtown), with the
  `n = 3` maximum unique — the three pair-channels. Exactly three.
- Evidence: shipped — `floor_to_interface.py` §1a–1b (4 checks).

## FCT-69 — Cl(3,0): Central `i`, Quaternion Even Sector, the `1/2`, the Plateau

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Scope: the algebra generated by three anticommuting involutions.
- Statement: `Ω = ABC` is central with `Ω² = −I`; the even sector satisfies
  the quaternion relations (exact dimension 4); an anticommuting partner
  swaps a mode's eigenprojectors, so reflection-invariant states weigh both
  outcomes exactly `1/2`; the central `√−1` exists iff `m ≡ 3 (mod 4)`,
  putting the phase-stable arity window at `{3, 4}` (the candidate-internal
  reading of (S), conditional and labeled).
- Evidence: shipped — `floor_to_interface.py` §2 (2 checks).

## FCT-70 — Retention Selects the Double Cover (Given Self-Hosting)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`, conditional on the named
  axiom (SH)
- Scope: central `C₂` extensions of the four-mark chiral contact symmetry.
- Statement: `H²(C₃;C₂) = 0` (three marks cannot host the lift);
  `H²(A₄;C₂) = C₂` with both classes witnessed — `2T = SL(2,3)` (unique
  involution, nonsplit; every `V₄` preimage squares to the central `z`)
  versus the split twin `A₄ × C₂` (every involution lift squares to the
  identity). Given (SH) — retention carried internally — the split twin
  dies and `2T` is forced; without (SH) the projective class is available,
  not forced. `z → −I₄` yields real quarter-turns `J² = −I₄` with the
  anticommuting triple.
- Evidence: shipped — `floor_to_interface.py` §3 (4 checks).

## FCT-71 — The Minimal Receiver Is `ℍ`; the Handoff to Chapter 8

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Scope: faithful real representations of the retained residue `z → −1`.
- Statement: dimension 1 impossible (arithmetic), 2 impossible
  (Frobenius–Schur `= −1`), 3 impossible (derived-subgroup determinant);
  dimension 4 exists with commutant exactly `ℍ` (division), the invariant
  symmetric form unique up to scale (`I₄`) — the Euclidean `1+3` split.
  Spinor return `J² = z`, `J⁴ = 1`. The resulting object is verbatim the
  starting datum of Chapter 8 (FCT-45), closing the starting-datum residual
  disclosed at v0.13.0 one layer down, with the four received inputs named.
- Evidence: shipped — `floor_to_interface.py` §4–5 (3 checks).

## Received inputs carried by all four rows

(S) ternary arity — measured; (R) readability — the program's standing
door, proven not-forced (private corpus, cited); (SH) internal retention —
named axiom; (C) orientation — proven received `C₂` torsor. The rows are
theorems *given* these; the fork-staging law is the frame.
