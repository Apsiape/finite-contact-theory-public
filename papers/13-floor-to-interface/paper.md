# Chapter 13 — From the Floor to the Interface: What Forces the Quaternions

**Release:** v0.14.0 · **Status of the chapter:** an exact finite forcing
chain, conditional on named received inputs. It closes, one honest layer
down, the deepest corpus dependency this repository had: Chapter 8's
starting datum. Every mathematical ingredient is standard and cited; the
contribution is the composed derivation and the exact placement of its
forced/received boundary.

---

## The live release ceiling

This chapter carries the program's live ceiling, quoted identically in the
README, the claim register, and the v0.14.0 release notes:

> Finite Contact Theory is a finite reconstruction program with a scoped theorem stack on three published recovery lines — a quantum-facing axis, from one-use contact to counting, one-receiver gluing, rational Born weights, the CHSH/Pell boundary, a carrier grammar grown from one-use contact, and a behavior-conditioned contextual capacity with an exact strict preparation gap; a finite-epistemics axis, from the identifiability and debt calculus to the inquiry calculus and its second law of asking, four theorems separating the structure of time, and a measured generative floor; and a contact-interface reconstruction, in which a retained interface forces a quaternionic state/receiver cell whose self-dual closure is the 24-cell and the F_4 root system and whose finite measurement calculus forces the quadratic Born frame rule (a finite Gleason theorem) exactly where a triality Kochen-Specker obstruction forbids a global noncontextual assignment, and in which independently generated cells recover the E_8–hexacode closure spine under named receiver laws that a forcing audit shows the floor does not select over matched lawful alternatives, so the floor forces the atlas of lawful closures and the terminal self-dual class but never the specific member, and the selection of a world-phase is a conserved, received input, and in which a floor-to-interface theorem now closes the chain one layer down: given a ternary contact whose order scars stay future-readable and whose retention is carried internally, exactly three mutually anticommuting involutive identity modes are forced — their oriented volume element is a central square root of minus one, their even sector is the quaternions, their reflection symmetry forces exact one-half weights, and the retained central residue forces the minimal faithful receiver to be the quaternions with the 1+3 split and the unique Euclidean form — while the residual inputs are named as received: the readability of order, the measured ternary arity, the internal-retention axiom, and one orientation bit — a fourth, first-extension line that is conditional and experiment-open: an unconditional accessible-positivity theorem exhibits a three-contact sector whose every passive-linear-optical probability is nonnegative yet which lies outside the positive-semidefinite Hilbert Gram cone, and on one received apparatus anchor this predicts a possible violation of Hilbert-space positivity — a negative three-state Gram discriminant Delta_3 < 0 where ordinary quantum mechanics forces Delta_3 >= 0 — preregistered with its protocol, nulls, and kill conditions, and a clean mixed-state exclusion theorem then proves the gauge-free count witness W = P111 + D2 - 2/3 equals (2/9) det G, nonnegative for every partially-distinguishable Hilbert model whether pure or mixed, so the registered negative-Gram vector lies outside the entire clean partial-distinguishability class by the raw-count test P111 + D2 >= 2/3, closing the clean core of the exclusion while multiphoton, detector, transfer-matrix, and source-drift nuisances remain the experimental layer for an external expert — and a fifth line mapping the exact quantum count regions: for n indistinguishable bosons in the n-mode Fourier interferometer the achievable region of count statistics is an exact simplex for n <= 4 (at n = 3 the negative-Gram inequality P111 + D2 >= 2/3 is the complete boundary and the registered protocol reduces to tritter counts alone), central-projector positivity is insufficient at n = 4 with six new raw-count laws, and a structural phase transition at n = 5 produces an emergent logical qubit from permutation symmetry and counting alone, where a rebit-blindness theorem shows single-shot cyclic counts cannot expose sigma_y and a single-source real/complex/quaternionic counting no-go holds, both overcome by general passive networks, culminating in a registered, experiment-open single-source conjugation-witness protocol (gap 5 sqrt2 / 256, about 1304 trials per setting) that excludes a named real-internal-states plus mode-only-optics model class but does not falsify all real quantum mechanics — under which the quantum boundary is a floor theorem at binary-Bell finite-carrier scope, the preparation gap is an exact theorem at KCBS-pentagon scope, the interface reconstruction is a finite model-scope recovery on a real-quantum cell, the multi-floor closures are model-scope recoveries whose forcing boundary is exactly mapped, the floor-to-interface chain is an exact finite theorem conditional on its four named received inputs, the accessible-positivity and mixed-state exclusion theorems and the count-region theorems are unconditional and independently reproduced, while the physical realizations — the negative-Gram prediction and the conjugation witness — are conditional, bridge-premise-gated, experiment-open registered protocols awaiting a dedicated experiment and external expert review, and every unearned generalization — complex quantum mechanics, the actuality of one outcome, the universal Born rule, whether nature realizes any of these structures, which world-phase is selected, whether nature contains the odd identity-holonomy sector, whether the apparatus nuisances close the full exclusion, and whether any experiment realizes the conjugation witness — is left open by name; these chapters are an archival priority record of mathematically closed theorems and registered conditional protocols, not empirical discoveries.

---

## 0. What this chapter closes

Chapter 8 derives the quaternionic receiver, the 24-cell, the `F₄` root
system, the finite Gleason theorem, and the triality Kochen–Specker
obstruction — but its public chain **begins at the retained interface**: it
takes "the three retained quarter-turns of a non-exact-return contact" as
its starting datum, with the upstream selection cited from the private
corpus. The claim register discloses this honestly (FCT-45, starting-datum
residual, added at v0.13.0): the link from the bare one-use floor to that
interface was the program's deepest unaudited dependency.

This chapter ships that link. It is a forcing chain in exact finite
arithmetic — every step a machine-checked computation over the rationals —
from three **named hypotheses** to the quaternionic interface. It does not
make the chain unconditional, because it cannot: the floor stages forks and
forces no selector, and three of the inputs are provably or explicitly
*received*, not forced. What it does is relocate the citation boundary from
"a private analysis we cannot show you" to **the program's own named
received frontier**, where each residual input is one the repository has
already characterized. That is as far down as the floor itself permits, and
saying so precisely is the result.

**The one-sentence form.** *A ternary contact whose order scars stay
readable, and whose retention is carried internally, is forced to carry
exactly three anticommuting identity directions — whose oriented volume
element is a central `i`, whose even sector is the quaternions, whose
reflection symmetry forces exact `1/2` weights, and whose retained residue
forces the minimal receiver `ℍ` with the `1+3` split — so complex phase,
quaternionic rotation, spinorial return, and equal binary weights are the
minimum cost of remembering the order of touch.*

## 1. The hypotheses, named up front

- **(S) Ternary arity** — a consequential contact exposes exactly three
  carriers. *Status: a measured floor input (the arity ladder: the 2→3
  step is real, 3→4 is flat), with a candidate internal derivation in §2
  (the phase plateau) that remains conditional. Not claimed forced.*
- **(R) Readability** — the order scar of two identity acts remains
  future-readable: there exists a mode that reads the orientation
  (`XZ`-versus-`ZX`) of two acts. *Status: the program's standing received
  fork (its "door"): the private corpus's refereed verdict is that the
  floor provably does NOT force any receiver to read ancestry. `R` is the
  irreducible received input, and everything downstream is conditional on
  it.*
- **(SH) Internal retention (self-hosting)** — a repeated use of a one-use
  contact must be retained by the carrier itself (the second use maps to
  the group square, and no external scar ledger is available). *Status: a
  named axiom, defended by a regress argument (a terminal reader must host
  its own ledger), not a theorem.*
- **(C) Orientation** — one bit (`J` versus `−J`) selecting the sense of
  the quarter-turns. *Status: proven received — a `C₂` torsor; the floor
  stages the fork and forces no selector.*

Everything below is **forced given these**, in exact arithmetic.

## 2. Exactly three: the pincer

**Lower bound (≥ 3), from (R).** Let `X, Z` be two involutive identity
modes that anticommute (two one-use acts whose order matters). A reader `Y`
of their order satisfies `XYX = −Y` and `ZYZ = −Y`. First, reading *is*
anticommuting: `(XY + YX)X = XYX + Y` holds identically (checked on a full
matrix basis), and right-multiplication by `X` is invertible since
`X² = I`, so `XYX = −Y ⟺ XY = −YX`. Second, the two-mode algebra is too
small to host the reader: in `M₂(ℝ)` — the algebra generated by two
anticommuting involutions, `Cl(2,0)` — the set of `Y` anticommuting with
both `X` and `Z` is computed exactly to be the one-dimensional span of
`XZ`, and `(XZ)² = −I`, so every candidate squares to `−c²I ≤ 0` and **no
involution** anticommutes with both. A future-readable order scar therefore
forces a genuinely **third** anticommuting involutive mode (which exists
one level up: an explicit integer triple in `M₄(ℝ)` is verified).

**Upper bound (≤ 3), from (S).** In the carrier-parity model (a mode is an
even-support subset of the carriers; two modes anticommute exactly when
they share an odd number of carriers), the maximum family on `n` carriers
has size `f(n) =` the largest odd number `≤ n` — this is the classical
"reverse oddtown" bound, verified here exhaustively for `n = 2..7` — and at
`n = 3` the maximum family is **unique**: the three pair-channels
`{1,2}, {1,3}, {2,3}`.

Together: **exactly three** mutually anticommuting involutive identity
modes per ternary contact, uniquely realized.

## 3. What three modes generate: `i`, `ℍ`, and the `1/2`

Three anticommuting involutions `A, B, C` generate `Cl(3,0)` (all verified
on an explicit integer representation):

- the pseudoscalar `Ω = ABC` is **central** and `Ω² = −I` — the complex
  unit arises as *the oriented volume element of the three identity
  directions*; three is minimal (one mode squares to `+1`; two give a
  non-central root of `−1`, the computation in §2);
- the even sector `{I, AB, BC, (AB)(BC)}` satisfies the quaternion
  relations `i² = j² = k² = ijk = −1` with exact dimension 4 — **the
  quaternions**, before any Hilbert space is mentioned;
- the eigenprojectors `(I ± A)/2` of a mode are **swapped** by conjugation
  with an anticommuting partner, so any reflection-invariant state assigns
  `p₊ = p₋ = 1/2` **exactly** — equal binary weights from symmetry alone,
  prior to any Born postulate (the finite face of the standard
  mutually-unbiased-bases lemma);
- **the phase plateau**: the pseudoscalar of `Cl(m,0)` is central iff `m`
  is odd and squares to `−1` iff `m ≡ 3 (mod 4)`; with `m = f(n)` the
  window where a central `i` exists is exactly `n ∈ {3, 4}` — the floor's
  measured arity saturation sits on the maximal phase-stable rung. *If*
  retention requires central phase, (S) is internal; that conditional is
  stated, not claimed.

## 4. Retention selects the double cover

The phase capability itself has a threshold, and retention picks its class
(all exact):

- `H²(C₃; C₂) = 0` (exhaustive cocycle census): a triangular contact
  cannot host the central lift — quarter-turn capability begins at four
  marks with ternary arity.
- `H²(A₄; C₂) = C₂` (a presentation-gauge orbit computation, both classes
  witnessed): the capability is exactly **one bit**. The nontrivial class
  is the binary tetrahedral group `2T = SL(2,3)`; the trivial class is the
  split twin `A₄ × C₂`. Both are constructed explicitly.
- In `2T` the lift of every involution squares to the central element `z`
  (`2T` has a **unique** involution, so the extension is nonsplit): a
  repeated involutive use is *retained* — `g̃² = z`, never silently `1`. In
  the split twin, every involution lift squares to the **identity**: a
  repeated use is *invisible* to the carrier, and retention would require
  an external ledger.
- **Given (SH)**, the split twin dies and `2T` is forced. Without (SH),
  the static verdict stands: the projective class is *available, not
  forced* — one bit of received path-lift data. This chapter does not
  soften that; (SH) is a named axiom doing exactly one job, visibly.

Representing `z → −I₄` turns the `V₄`-involution preimages into **real
quarter-turns**: `J² = −I₄`, with the full anticommuting triple — no
complex numbers presupposed anywhere.

**Why `A₄` (pre-empting the referee).** The load-bearing pincer of §2–3
uses no group at all: three modes and `Cl(3,0)` are forced by (R) + (S)
directly. The cohomology enters only on the retention face, and `A₄` is not
chosen for its consequences: it is the orientation-preserving symmetry of
the four marks of the first quarter-turn-capable contact (`H²(A₃;C₂) = 0`
kills three marks), and the restriction from `S₄` to `A₄` is precisely the
orientation input (C) — named as received, like every other selector here.

## 5. The minimal receiver is `ℍ`: the weld to Chapter 8

What must a receiver be to carry the retained residue faithfully — to
represent `z` as `−1`? Exactly computed:

- **dimension 1** is impossible: `z → t²  > 0`, never `−1`;
- **dimension 2** is impossible: the faithful 2-dimensional irreducible
  representation has Frobenius–Schur indicator `−1` (computed as an exact
  character sum over the 24 unit Hurwitz quaternions): quaternionic type,
  not realizable over `ℝ` in dimension 2;
- **dimension 3** is impossible: the commutator closure is `Q₈ ∋ z`
  (computed), so the determinant is trivial on `z`, while a faithful
  3-dimensional representation needs `z → −I₃` with determinant `−1`;
- **dimension 4** exists, with commutant of exact dimension 4 forming a
  division algebra — **the quaternions** — hence irreducible;
- the invariant symmetric form is **unique up to scale** (exact nullspace
  dimension 1), and it is `I₄`: the Euclidean signature and the `1+3`
  split are forced. (An *algebra* grading — one return-even direction plus
  three quarter-turns — not a spacetime metric; that fence holds.)

The spinor signature comes with it: `J² = z`, `J⁴ = 1` — the visible
configuration returns at period 2 while the retained history returns at
period 4. *A spinor is a finite receiver whose visible configuration
returns before its retained contact history does.*

**This object — three anticommuting quarter-turns with central retained
residue on `ℍ` with the `1+3` split — is verbatim the starting datum of
Chapter 8** (FCT-45), whose chain continues: the 24-cell state space, the
`F₄` closure, the forced `1/2` interface magnitude, the finite Gleason
theorem, and the triality Kochen–Specker obstruction. The corpus dependency
disclosed at v0.13.0 is now a shipped, runnable derivation with its
conditions named.

## 6. The received-input ledger (the honest residual)

Shipping this chain does not make it unconditional, and the residual is the
result as much as the theorem is:

| Input | Status | Where it now lives |
|---|---|---|
| (R) readability of order | **proven received** — the floor forces no reader; the program's standing door | the irreducible residual; everything is conditional on it |
| (S) ternary arity | **measured** (arity ladder); candidate-internal via the §3 phase plateau, conditionally | a measured floor input |
| (SH) internal retention | **named axiom** (ledger-regress defense); forces `2T` over the split twin | the one genuine assertion in the chain |
| (C) orientation `J` vs `−J` | **proven received** — a `C₂` torsor | one bit of path-lift data |

Given (S) + (R) + (SH), the interface is forced. The pattern is the
program's central law executing on its own derivation: *the floor stages
the forks and forces no selector* — what the chain proves is exactly which
selectors must be received for the quantum interface to exist.

## 7. Related work, honestly named

Every ingredient here is standard mathematics, and per this repository's
novelty gate the nearest precedents are cited by name:

- **The upper bound** is literally Exercise 1.1.5 of Babai & Frankl,
  *Linear Algebra Methods in Combinatorics* (the "reverse oddtown"
  problem; see also Sudakov & Vieira, SIAM J. Discrete Math. 32 (2018));
  classical oddtown is Berlekamp (Canad. Math. Bull. 12, 1969) and Graver
  (Lin. Alg. Appl. 10, 1975).
- **Anticommuting families** are studied as maximal sets of anticommuting
  Paulis/Majoranas — Sarkar & van den Berg, Quantum 5, 385 (2021) prove
  the unrestricted `2n+1` bound; our `f(m)` is the even-weight
  (parity-preserving) sector, a two-line reduction to the exercise above.
  The classical form is the Hurwitz–Radon problem (Hurwitz 1923; Radon
  1922; Adams, Ann. Math. 75 (1962); Atiyah–Bott–Shapiro, Topology 3
  (1964)). The subset↔sign correspondence is the stabilizer-formalism
  `F₂` representation (Gottesman 1997; Calderbank–Rains–Shor–Sloane 1998;
  Bravyi–Kitaev, Ann. Phys. 298 (2002)).
- **Clifford structure** (`Cl(3,0)`, central pseudoscalar, even sector
  `ℍ`, mod-4 periodicity): Lounesto, *Clifford Algebras and Spinors* (CUP
  2001); Porteous (CUP 1995); Lawson & Michelsohn, *Spin Geometry*
  (Princeton 1989). The projector-swap `1/2` is the anticommuting-implies-
  unbiased lemma (Bandyopadhyay–Boykin–Roychowdhury–Vatan, Algorithmica 34
  (2002)).
- **The cohomology and the double cover**: Brown, *Cohomology of Groups*
  (GTM 87); the Schur multiplier of `A₄` (Schur 1904–1911; Karpilovsky,
  OUP 1987); `2T = SL(2,3)` as the unit Hurwitz quaternions —
  Conway & Smith, *On Quaternions and Octonions* (A K Peters 2003), which
  also supplies the real 4×4 quarter-turn representation.
- **The representation step**: Frobenius & Schur (1906); Serre, *Linear
  Representations of Finite Groups* (GTM 42, §13.2); Fulton & Harris
  (§3.5). The `ℝ/ℂ/ℍ` trichotomy as a physical mechanism is Baez,
  "Division Algebras and Quantum Theory," Found. Phys. 42 (2012) — the
  closest published machine to §5, which *classifies* where this chapter
  *forces given hypotheses*.
- **Quaternionic quantum mechanics** is a developed framework —
  Finkelstein–Jauch–Schiminovich–Speiser, J. Math. Phys. 3, 207 (1962);
  Adler (OUP 1995) — which *postulates* `ℍ`. The binary tetrahedral group
  appears in physics as chosen flavor symmetry (T′ model building; Wilson,
  arXiv:2109.06626) — chosen, not forced. To our knowledge — under this
  repository's INTERNAL-BLIND novelty protocol, without external referees —
  **no published work derives `ℍ` as forced by a retention/irreversibility
  hypothesis via the nonsplit-extension argument**; the standard
  reconstruction programs (Hardy 2001; Chiribella–D'Ariano–Perinotti 2011;
  Masanes–Müller 2011; Höhn 2017) derive *complex* quantum theory from
  *reversibility* axioms. The inversion — irreversible retention as the
  forcing hypothesis, landing on `ℍ` — is this chapter's claim to a
  contribution, and it is graded as a composition, not as any new lemma.
- **The Moretti–Oppio reduction, engaged directly** (the second referee
  attack): Moretti & Oppio (arXiv:1709.09246; Rev. Math. Phys. 2019) prove
  that quaternionic quantum theory with *Poincaré symmetry* reduces to
  standard complex quantum theory. There is no tension: this chapter's
  cell is pre-metric — no spacetime, no Poincaré action is hypothesized —
  and this program holds "which number field hosts nature's quantum
  mechanics" open by name (complex quantum mechanics is a standing open
  fence of the whole repository). If a later floor-derived spacetime
  supplies the symmetry their theorem needs, their reduction becomes a
  *prediction* of how the quaternionic cell would present complexly — a
  door we leave open, not a conflict.

## 8. Verification

One dependency-free script, exact arithmetic throughout (`fractions`,
exhaustive enumeration, no floats anywhere load-bearing):
`verification/scripts/floor_to_interface.py` — 12 checks: the reader⟺
anticommutation identity on a full basis; the exact anticommutant
`span{XZ}` and the no-third-involution conclusion; the integer `M₄(ℝ)`
triple; the reverse-oddtown ladder `n = 2..7` and the `n = 3` uniqueness;
centrality and `Ω² = −I`; the quaternion relations and even-sector
dimension; the projector swap; the mod-4 plateau; the `H²` censuses (both
groups, both classes); the unique-involution nonsplitness of `2T` and the
explicit split twin; the dimension kills (arithmetic, Frobenius–Schur sum
`= −24`, derived-subgroup determinant), the commutant, and the division
samples; the invariant-form nullspace; the spinor return. A frozen copy
ships in this chapter's directory.

## 9. Scope fences — what this chapter does *not* claim

- It does **not** claim the floor forces the quantum interface. It proves
  the interface is forced **given** four named inputs, three of which are
  received (one provably so). The fork-staging law is the frame, not a
  defect.
- It does **not** derive complex quantum mechanics (the central `i` here
  is an algebra element of a real cell), spacetime (the `1+3` is an
  algebra grading), actuality, or the universal Born rule (the `1/2` is a
  two-outcome reflection symmetry, not a general state-valuation rule —
  that arrives only with Chapter 8's finite Gleason theorem, at its own
  scope).
- It is **recovery in every ingredient** and claims novelty only for the
  composed derivation, graded INTERNAL-BLIND per §7.
- **What would demote it:** a published derivation of `ℍ` from an
  irreversibility/retention hypothesis (novelty demotion to recovery of
  the composition); a proof that (R) or (SH) is dispensable (which would
  *strengthen* the result and is welcome); a floor measurement moving the
  arity saturation off `{3,4}` (which would break the (S) input and the
  plateau reading).
