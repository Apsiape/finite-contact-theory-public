# Release Notes — v0.36.0

Version DOI (v0.36.0): 10.5281/zenodo.21506169. Concept DOI:
`10.5281/zenodo.21253591`. Freeze date: 2026-07-23.

## Overview

This is the largest single release in the program's history: **six chapters
(62–67)**, one continuous arc that follows a single distinction from the
identity root all the way to the program's central registered wager. In five
sentences: the floor's tolerance relation stops being a posit and is **derived**
from one root — using a thing's identity is an event, never free (Ch 62). One
distinction, iterated, generates a **law tower** whose even rungs are laws and
odd rungs are appearances, with every law generated twice over — as the carry of
the appearance below and the shadow of the appearance above (Ch 63–64). Follow
that tower's coherence and a chain forces itself, from a graded anomaly through
the quaternion group to the **24-cell** (minimal, chirality selecting 24 over 48)
and onward to a forced **E₈** closure with its residual choices enumerated
(Ch 65). The famous powers-of-two structure turns out to be the zero-carry
stratum of a full **carry-depth filtration** (Ch 66). And the whole stack
converges on **the next i** — a physical unit exactly zero to every
complex-quantum observable yet nonzero to the next detector stratum, with a
registered experimental program and zero empirical divergences claimed (Ch 67).

## Chapter 62 — Equality Is an Event

The program used to posit a tolerance relation on the floor. This chapter removes
the posit: from one root — **identity/use is an event, never free** — the
tolerance relation is derived as *equality minus free transitivity*. Classical
mathematics assumes identity is a standing fact reusable at no cost; the floor
charges for it, and that single change is what makes the rest of the arc forced
rather than chosen. The chapter ships the exact-twin census (finite objects the
floor cannot distinguish; the C5/C6 `det G = 0` pair and the coat-universality
theorem) **together with its correction**: an early count conflated two twin
cases (the (B)/(C) conflation), which is scored loudly and re-derived. The
falsifiability-line process rule is stated as the chapter's method. Constituent
identity-theory anchors cited (Hofmann–Streicher; HoTT Book).

## Chapter 63 — The Closure Charge and Its Witnesses

The closure charge χ is a Z₂ odd-cycle holonomy; its fusion algebra is verified
6/6 exactly (multiplicativity, odd-times-odd cancellation, screening at
`v = 1/2`, survival). The chapter derives the tritter amalgamability boundary
`θ_max(v)` (Menssen 2017; a one-line Gram corollary offered as a presentational
sharpening, not a new constituent) and audits the route-phase spoofs: the known
`−I` / channel-interference phenomenon is cited to Oi 2003, and the program's
contribution is stated as the **spoof-closure design** — a commutator plus a
sector-conditional correlator (the K-witness). Recovery-with-citation
throughout; the design posture is the novel layer.

## Chapter 64 — The Law Tower

The two-sector theorem and the half-eraser (top-cell) lemma set up `Z₂[x]` on one
generator; the exact identity `DU + UD = 1` with `ker D = im D = im Frobenius`
carries the p = 2 uniqueness (THEOREM, model scope). The operator center is a
**recovery** of a classical result (Revoy 1973; BMR). The chapter's headline is
the **Bockstein carry theorem**: β is the carry of the differential, so every law
is **doubly generated** — the carry of the appearance below and the shadow of the
appearance above — an operator identity for where laws come from that, per the
blind sweep, no incumbent framework states because none asks the question.
Vertical exactness (`x⁴ = D(x⁵)`) and the carry-depth filtration
`ν₂(Cat(n−1)) = s₂(n) − 1` (Alter–Kubota; Deutsch–Sagan; Kummer) close the
chapter, with the mod-4 floor and lift-decides-depth.

## Chapter 65 — From the Faceless Class to the 24-Cell and E₈

A law-bundle model in which the pentagon edges evaluate the coboundary (Mac Lane;
EGNO): 0 of 256 dressings survive without *i*, which flattens the curvature;
facelessness is the associahedron topology (essential cohomology —
Adem–Karagueuzian; Green — adopted alongside the program's "faceless"). The
derivation chain is then forced: Q₈ (minimal anticommuting lift) + triadic
symmetry = the binary tetrahedral group = the **24-cell** (minimal, index-3,
chirality gating 24 vs 48); F₄ = the pre-chirality gate itself; **E₈ forced** by
evenness + self-duality with the glue choice = a received S₃ triality (35 glues →
6). **Required firewall (shipped):** this is *not* the McKay correspondence
(McKay sends the binary tetrahedral group to E₆ and E₈ to the binary icosahedral
group; our glue-forcing route lands differently), and a note distinguishes this
E₈ from the E₈ of Kitaev-type SPT physics. Constituents cited to Conway–Smith,
Coxeter, SPLAG, Nikulin.

## Chapter 66 — The Carry Engine

The mechanical heart of the tower. The Cartan repair fixes the mirror
(`ι_alt² = 0`, `Q² = L`) after the naive square failed — shipped as a kill, not
hidden. K is a projector (a 50/50 split; the transverse sector comes back
**non-cohomological** — a second honest null). Dyadic selection is Catalan
parity (Kadeishvili; Merkulov; Markl); the space of contractions is a
**117-dimensional moduli object** with arity-3 rigidity and in-model delayed law
memory (with the A-infinity gauge caveat and the BV/propagator home named —
Mnev; Costello). The mirror is a Z/4 extension class — the **first Witt carry**
(Witt; Serre). The chapter's honesty section carries three kills (the mirror
repair, the non-cohomological transverse, the abelian wall) and states the
powers-of-two structure as the **zero-carry stratum** of the full filtration.

## Chapter 67 — The Next i

The synthesis. The absorption map (public tiers) grades seventeen hard-problem
faces the stack now touches. The chromatic frame is presented at CITED/CONJ: the
mirror is the multiplicative FGL quadratic term (verified); the dyadic tower is a
doubling series (verified); transport is nonabelian, hence not height-1
(verified); G₂₄ is the maximal finite subgroup of the **strict height-2 Morava
stabilizer at p = 2** (cited: Hewett; Bujard; Beaudry), with the extended/Galois
version of order 48 and the observation that our chirality gate (24 vs 48) aligns
with strict-vs-Galois; Stolz–Teichner is the named incumbent height ladder. The
central wager: **ℑ₂ ≠ 0, L₁(ℑ₂) = 0** — a root of invisibility, zero is
detector-relative. The **registered experimental program**: law memory in
process-tensor form (invisible at every finite depth — distinct from
non-Markovian memory, kill condition = process-tensor completeness; Pollock 2018;
Milz–Modi 2021), the τ-package commutator, the faces-of-15 arity-visibility
ladder, and a subthreshold photonic validation. The OPEN gates are named in
print: formal-group extraction, the HKR check, the no-cloning-distinction check.
The broader absorption list ships as an explicitly labeled speculative outlook —
CONJ everywhere, **no absorption claimed as done, zero empirical divergences.**

## Label summary

- **THEOREM (model scope):** the two-lift relation `DU+UD=1` and its two
  resolutions (Ch 64); `ker D = im D = im Frobenius`, p = 2 uniqueness (Ch 64);
  the Bockstein carry identity / laws doubly generated (Ch 64); the fusion
  algebra 6/6 (Ch 63); the exact-twin / coat-universality theorem (Ch 62); the
  forced derivation chain Q₈ → 24-cell → E₈ *as a sequence within the model*
  (Ch 65); the carry-depth filtration `ν₂(Cat(n−1)) = s₂(n) − 1` (Ch 66).
- **RECOVERY (cited):** the operator center (Revoy 1973; BMR); the tritter
  boundary (Menssen 2017); the route-phase phenomenon (Oi 2003); the pentagon
  coboundary model (Mac Lane; EGNO); every exceptional-object constituent
  (Conway–Smith; Coxeter; SPLAG; Nikulin); essential cohomology
  (Adem–Karagueuzian; Green); transfer/homotopy-transfer (Kadeishvili; Merkulov;
  Markl); the BV propagator (Mnev; Costello); Witt vectors (Witt; Serre); the
  height ladder and stabilizer facts (Stolz–Teichner; Hewett; Bujard; Beaudry).
- **EXTENSION / registered-novel (with named hazard):** contextuality from two
  conservation laws (hazard: distinguish from no-cloning/monogamy repackaging —
  in-house check named); the `2^{s₂(n)}` physical modulus ladder (derived three
  independent ways as artifact control; still CONJ); the witness/provenance
  mathematics (contraction moduli, witnessed Witt, substructural identity
  witness); the spoof-closure witness designs.
- **CONJ / registered wager:** the chromatic height-2 identification (single-
  legged, gated on formal-group extraction); ℑ₂ and the next-i program; the
  broader absorption prospectus (YM-descent, generations-orbit, and the rest).

## Corrections shipped in this release

- **The twin census (Ch 62):** an early count conflated the (B)/(C) twin cases;
  caught, re-derived, corrected count shipped with the script.
- **The chromatic height-2 reading (Ch 67):** the exponent test does not
  discriminate repeated height-1 doubling from a native height-2 series; the
  height-2 claim is demoted to a single-legged conjecture, gated on the
  formal-group extraction, in print.
- **The carry engine's mirror and transverse (Ch 66):** the naive squared mirror
  failed and required the Cartan repair; the transverse sector came back
  non-cohomological. Two honest nulls, one chapter.

## Verification note

All six chapters ship **dependency-free Python scripts** (standard library only —
`fractions`, `itertools`, `math`; no numpy), each self-contained, each
re-deriving its chapter's core exact results with `[PASS]`/`[FAIL]` lines, each
ending in an explicit falsifiability comment and exiting nonzero on any failure.
Run the release with `python verification\scripts\run_all.py`; the flagship
subset with `--fast`. Release hygiene is enforced by
`python scripts\release_audit.py`.

## The ceiling

> **Finite Contact Theory is a finite reconstruction program whose newest stack begins one layer below its own floor — the tolerance relation the program had always posited is derived from a single root, that identity is an event and is never free — and builds upward through an exactly verified tower on one generator, in which even rungs are laws and odd rungs are appearances, every law is generated twice over (as the Bockstein carry of the appearance below it and as the derivative shadow of the appearance above it), and the first law is at once the sign of exchange, the square of the generator, the factor set that glues the integers modulo four, and the first Witt carry; a two-sector theorem splits the fourth rung into gluing data reachable from faces and an essential class reachable from none; the essential class, through its anticommuting lift and triadic closure, forces the quaternion group and the 24 Hurwitz units of the 24-cell, reflection admits the F4 configuration and received chirality selects the half, and two such receivers under evenness and self-duality close to the 240 roots of E8 with the residual glue choice exactly a received triality element — a forcing chain distinct from the McKay correspondence; a carry-depth filtration organizes all arities by binary digit count with the powers of two as the zero-carry stratum, and the same dyadic skeleton is selected independently by binomial parity, by Catalan tree parity in a transfer engine that writes higher operations through the cohomologically invisible half of the cochain world, and by the doubling series of the multiplicative formal group; a moduli of contraction witnesses is rigid at arity three and first free at arity four; and the whole is read, at cited and conjectural labels only, as a chromatic proposal — complex quantum mechanics as the height-one chart of a stratified reality — whose central registered wager is the next i: a physical unit that is exactly zero to every height-one observable and nonzero to the next stratum, with spoof-closed witnesses registered in advance, every heavy claim shipped as a dependency-free script, every correction published, and zero empirical divergences claimed — under which the program remains a reconstruction with registered extensions, and every unearned generalization is left open by name.**
