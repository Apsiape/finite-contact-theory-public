# Chapter 26 — claim snapshot (as of the v0.21.0 release tag)

Four claim-register rows; live view in
[`docs/public-claim-register.md`](../../docs/public-claim-register.md).

## FCT-124 — The Steering Radius and the Gossip Theorem

- Status: `THEOREM / MODEL-SCOPE` (instances of neighborhood
  reconstruction and the LOCAL model — Mossel-Ross, Linial,
  Naor-Stockmeyer cited; a registered rows-lean died and is scored)
- Statement: rows do not determine reproduction (2 of 32 types
  ambiguous); the radius-1 ball does, perfectly (138 types, zero
  violations, exhaustive n=5); the ball = one round of neighbor
  gossip; a concrete privacy-failure pair is exhibited.
- Evidence: shipped — `verification/scripts/habitability.py`.

## FCT-125 — The Privacy Census and the Zero-Traffic Bootstrap

- Status: `THEOREM / MODEL-SCOPE` (LCL/proof-labeling census frame
  and silent self-stabilization cited — the private sector is a
  zero-communication regime; a frozen habitability bet held)
- Statement: only one contact shape needs gossip (capless +
  asymmetric periphery); 27/33 classes private; the entire
  habitable test set private; private data is fresh by construction
  and the task stationary — maintenance self-consistent with no
  channel; the gossip sector provably cannot run on memory (96/125
  cached schedules mis-steer).
- Evidence: shipped — `habitability.py` (census + bootstrap).

## FCT-126 — No Doom States (the Price List)

- Status: `THEOREM / MODEL-SCOPE` (the recovery property is
  self-stabilization's guarantee — Dijkstra, von Neumann, Gacs
  cited; the exhaustive cost-annotated finite price list is the
  priority artifact per the sweep)
- Statement: every class of the conserved 13-class sector is
  steerable (max 3 bits/contact); the slip outcome is steerable at
  1 bit — failure is a detour, not a sentence.
- Evidence: shipped — `habitability.py` (resilience section).

## FCT-127 — The Habitability Theorem

- Status: `THEOREM / MODEL-SCOPE` (plausibly-new as a stated
  equivalence per the sweep; conceptual parents Conant-Ashby,
  requisite variety, autopoiesis all cited; THE CAUTION STATED IN
  PRINT: both sides are downstream of sparseness — the theorem is
  a convergence result about that shared root, and is presented as
  such)
- Statement: at tested scope, the worlds that can host bounded
  observers are worlds their inhabitants can afford to maintain —
  habitable = maintainable.
- Evidence: shipped — `habitability.py` (synthesis section).
