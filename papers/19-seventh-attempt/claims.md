# Chapter 19 — claim snapshot (as of the v0.18.0 release tag)

Five claim-register rows; live view in
[`docs/public-claim-register.md`](../../docs/public-claim-register.md).

## FCT-93 — The Price of Self-Description

- Status: `THEOREM / MODEL-SCOPE` (impossibility core KNOWN and cited:
  Breuer 1995, Wolpert 2008, Dalla Chiara 1977; the PRICE structure —
  exact deficit, anti-monotone in inert capacity, key-retired — is the
  claim, adjudicated the sweep's strongest priority candidate)
- Statement: a causally decoupled register carries zero
  world-information without a scheme key; the keyless deficit is a
  floor invariant; adding inert capacity strictly increases it (the
  anti-regress); the key suffices exactly and is underived (no
  equivariant scheme selection) — a column-2 constant of the hosted
  world.
- Evidence: shipped — `seventh_attempt.py` Movement A.

## FCT-94 — The Books of a Self-Describing World

- Status: `THEOREM / MODEL-SCOPE` (the zero-slack identity is presented
  as Shannon-chain-rule bookkeeping per the sweep's demand, not as a
  result; the unwritability route — factorial/polynomial gap +
  tower-level equivariance no-go — is a new exact proof of a known
  conclusion: Löfgren, Putnam's permutation argument, Harnad, Kripke,
  all cited)
- Statement: minted = banked + deficit exactly, zero slack; the scheme
  space outweighs the history space factorially (the complete
  interpretation map is unwritable by the entire floor); no
  equivariant convention exists at any level of a meta-register tower.
  Every self-describing world runs on injected capital, and the
  capital is a constant.
- Evidence: shipped — `seventh_attempt.py` Movement B.

## FCT-95 — The Two Semantics and the Measurement Dial

- Status: `THEOREM / MODEL-SCOPE` (two registered misses upgraded into
  the results, scored in-text; null-result information labeled an
  exact RECOVERY of Renninger/Elitzur–Vaidman; Wheeler/RQM/QBism cited
  as the prefiguring frame; the two exact theorems — measurement mints
  and key-free = co-minted — are the claims per the sweep)
- Statement: with coupling, key-free information exists exactly on
  co-minted events (measurement mints: the joint floor out-mints the
  decoupled product); null-result information is exact (silence
  informs on exhaustion floors); consumptive + conventional
  information partition totals exactly, each priced in its own
  currency; across the dial, a hosted observer's key burden is exactly
  its non-participation.
- Evidence: shipped — `seventh_attempt.py` Movement C.

## FCT-96 — First-Person Theorems

- Status: `THEOREM / MODEL-SCOPE` (biography = Everett memory sequences
  + Lamport happened-before, cited; monotone healing has quantum
  Darwinism's shape, cited; THE JUXTAPOSITION — inert capacity worsens
  while participation heals, one model both regimes — is the claim;
  the privacy-unit flagged plausibly-new-but-near-trivial, honestly)
- Statement: the keyless residual is monotone decreasing in observer
  size (participation heals what inert capacity worsens); privacy's
  unit is the unobserved contact (a one-mark exterior has no private
  life); every observer's causal biography and internal chronology are
  key-free and exact — the unknown is always and only
  when-relative-to-others.
- Evidence: shipped — `seventh_attempt.py` Movement D
  (self-consumption).

## FCT-97 — Intersubjectivity Theorems

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (Mazurkiewicz/Zielonka
  trace theory cited wholesale — forced agreement is a property of the
  semantics, not a discovery; presented against the quantum backdrop
  where intersubjectivity is contested: Frauchiger–Renner-style
  arguments, Ozawa's QM-side agreement theorem, Darwinism consensus)
- Statement: for disjoint observers, events partition into four
  sectors; co-witnessed events appear in both experiences in the same
  relative order in every history (forced agreement — no Rashomon);
  each observer determines the other exactly where they touched; the
  pooled experience determines everything except what neither touched.
- Evidence: shipped — `seventh_attempt.py` Movement D (witnesses).
