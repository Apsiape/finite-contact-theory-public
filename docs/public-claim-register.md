# Public Claim Register

Status: release-draft public ledger.

This register is the controlling public list of what Finite Contact Theory
(v0.36) is allowed to say. Every claim is scoped. A label stronger than the
written scope is not implied.

Evidence states:

- `shipped`: runnable public script/result is included in this repository.
- `cited`: locked private source or result is named but not copied here.
- `historical`: provenance only, not public-load-bearing.
- `held`: not public evidence yet.

For label meanings, see `status-labels.md`. For verification posture, see
`../verification/evidence-manifest.md`.

## Release Ceiling

The v0.36 public ceiling is (quoted identically in the README, the synthesis
chapter, and the v0.36.0 release notes):

> **Finite Contact Theory is a finite reconstruction program whose newest stack begins one layer below its own floor — the tolerance relation the program had always posited is derived from a single root, that identity is an event and is never free — and builds upward through an exactly verified tower on one generator, in which even rungs are laws and odd rungs are appearances, every law is generated twice over (as the Bockstein carry of the appearance below it and as the derivative shadow of the appearance above it), and the first law is at once the sign of exchange, the square of the generator, the factor set that glues the integers modulo four, and the first Witt carry; a two-sector theorem splits the fourth rung into gluing data reachable from faces and an essential class reachable from none; the essential class, through its anticommuting lift and triadic closure, forces the quaternion group and the 24 Hurwitz units of the 24-cell, reflection admits the F4 configuration and received chirality selects the half, and two such receivers under evenness and self-duality close to the 240 roots of E8 with the residual glue choice exactly a received triality element — a forcing chain distinct from the McKay correspondence; a carry-depth filtration organizes all arities by binary digit count with the powers of two as the zero-carry stratum, and the same dyadic skeleton is selected independently by binomial parity, by Catalan tree parity in a transfer engine that writes higher operations through the cohomologically invisible half of the cochain world, and by the doubling series of the multiplicative formal group; a moduli of contraction witnesses is rigid at arity three and first free at arity four; and the whole is read, at cited and conjectural labels only, as a chromatic proposal — complex quantum mechanics as the height-one chart of a stratified reality — whose central registered wager is the next i: a physical unit that is exactly zero to every height-one observable and nonzero to the next stratum, with spoof-closed witnesses registered in advance, every heavy claim shipped as a dependency-free script, every correction published, and zero empirical divergences claimed — under which the program remains a reconstruction with registered extensions, and every unearned generalization is left open by name.**

The v0.1–v0.35 ceilings remain quoted, unchanged, in their frozen chapters
and release notes; this row supersedes them as the live ceiling.

The release does not claim all quantum mechanics, quantum gravity, or
nature-facing prediction is solved; the one nature-facing item — the
negative-Gram prediction — is explicitly conditional, preregistered, and
experiment-open, an archival record and not a discovery.

---

## FCT-01 - Content Is Not Representative Selection

Status: `THEOREM / SCHEMA`
Scope: free symmetry actions and finite projection covers.
Evidence state: `cited`.

Public statement:

If the declared structure has a free symmetry action, no equivariant rule can
select a canonical representative. Public content must instead live in stable
equalized faces, invariant structure, or graded defects.

Evidence:

- private `admissibility-calculus/12-public-claim-register.md` AC-01/03;
- private `admissibility-calculus/25-theorem-claim-evidence-crosswalk.md`
  T0-T3.

Checks / controls:

- proof obligation is ordinary finite group-action/equivariance reasoning;
- public proof rewrite still needed.

Residuals:

- not a physical theorem by itself;
- needs clean public hypotheses before DOI finalization.

## FCT-02 - Admissible Content Criterion

Status: `SCHEMA`
Scope: finite presentations with declared projection cover, grade, controls,
and failure condition.
Evidence state: `cited`.

Public statement:

A public claim counts as forced content only when a stable equalized face or
graded defect survives registered controls. Otherwise it is a lens, admission
datum, open hinge, or uninformative readout.

Evidence:

- private admissibility-calculus guardrails and classification ledgers;
- public `docs/admissibility-calculus.md`;
- public `docs/status-labels.md`.

Checks / controls:

- method rule; the control is whether the rule prevents overclaiming in the
  claim register and correction ledger.

Residuals:

- not empirical content;
- not a theorem about any physical domain.

## FCT-03 - One-Use Directed Order Does Not Store Symmetric Memory

Status: `THEOREM / RECOVERY`
Scope: pure directed one-use incidence without additional symmetric record
grade.
Evidence state: `cited`.

Public statement:

Pure one-use order does not by itself supply stored symmetric GF(2)-style
record capacity. Memory requires a persistent or multiply-bindable record
grade.

Evidence:

- private AC-05;
- private crosswalk T4-T6;
- private research memory verdicts.

Checks / controls:

- proof should be expressible as finite incidence linear algebra;
- controls are record-grade additions that restore capacity.

Residuals:

- public proof rewrite still needed;
- does not classify every possible memory channel.

## FCT-04 - Memory Is Structural Excess, Not Raw Capacity

Status: `MODEL-SCOPE / EXTENSION`
Scope: record-channel audits with matched count, placement, and null controls.
Evidence state: `cited`.

Public statement:

Raw record capacity is not memory. Memory is null-beating structural excess
that survives the declared record-channel controls.

Evidence:

- private AC-06/07/08;
- record-channel and q-channel audits.

Checks / controls:

- matched count and placement nulls;
- channel comparisons where raw capacity equals null but structure differs.

Residuals:

- graph-floor selective q=3 is one successful generator;
- higher-arity and alien substrate claims remain scoped.

## FCT-05 - Native One-Use Extension Is Support-Irreversible

Status: `THEOREM / MODEL-SCOPE`
Scope: native one-use extension sites with strictly growing support/rank.
Evidence state: `cited`.

Public statement:

Native one-use extension has an intrinsic support arrow: active native moves
increase support and do not carry their own reverse support unless a quotient
or reverse bridge is admitted.

Evidence:

- private AC-09/10;
- flow/action rows.

Checks / controls:

- compare native add-only support with recurrent comparison sectors;
- verify that reverse motion requires an admitted quotient or additional
  channel.

Residuals:

- recurrent thermodynamic entropy production belongs to comparison sectors, not
  native add-only growth.

## FCT-06 - Closure-Seeking Repair Is A Measured Dynamics Class

Status: `MODEL-SCOPE`
Scope: graph repair presentation and closure-opportunity readout.
Evidence state: `cited`.

Public statement:

The graph-floor repair law belongs to a closure-seeking dynamics class. The
exact exponent and weights are admission data that affect clustering and
memory.

Evidence:

- private AC-11/12;
- law-of-motion and unique-potential audits.

Checks / controls:

- vary closure potential/exponent and measure changed observables;
- compare against structure-blind rules.

Residuals:

- do not claim a unique forced potential;
- do not use closure dynamics as the Tsirelson selector.

## FCT-07 - Composition Grammar Is Forced More Strongly Than Product

Status: `RECOVERY / MODEL-SCOPE`
Scope: local-registration composition families and coherent products.
Evidence state: `cited`.

Public statement:

Local registration recovers no-signaling and symmetric monoidal grammar at
declared scope, while the general product law and correlation ceiling remain
admission data unless further gates select them.

Evidence:

- private AC-16/17/18;
- composition audits;
- the private research corpus for the binary-Bell carrier sharpening.

Checks / controls:

- compare no-signaling/coherent-product families with signaling/null families;
- separate grammar recovery from product/correlation selection.

Residuals:

- later private work sharpened binary-Bell carrier scope;
- the general selector remains open.

## FCT-08 - Product Law And Correlation Ceiling Are Admission Data At General Scope

Status: `OPEN / ADMISSION DATA`
Scope: classical, quantum, PR, signaling, and almost-quantum interface
comparisons beyond the tested binary-Bell carrier.
Evidence state: `cited`.

Public statement:

No-signaling and monoidal grammar do not by themselves select classical,
quantum, PR-strength, or almost-quantum correlations. At general scope, which
product/composition law is received remains an explicit hinge.

Evidence:

- private AC-18;
- the private research corpus;
- almost-quantum diagnostics.

Checks / controls:

- compare families with identical grammar but different correlation ceilings;
- keep FCT-18/FCT-19 separate as scoped binary-Bell results.

Residuals:

- general quantum selector theorem remains open.

## FCT-09 - No-Totality For Open Fresh-Mark Rule

Status: `THEOREM / CONDITIONAL`
Scope: admission rules with an uncapped fresh-mark channel under act-indexed
one-use.
Evidence state: `shipped` plus `cited`.

Public statement:

For open admission rules with uncapped fresh marks, every finite fragment
admits a fresh legal extension; totality is not a native object at that scope.
Capped rules form the other side of the fork.

Evidence:

- shipped: `verification/scripts/no_jam_open_rule.py`;
- shipped: `verification/results/FCT-09-no-jam-open-rule-RESULTS.md`;
- cited: the private research corpus and blind record.

Checks / controls:

- shipped script checks exact open-rule BFS through depth 7 with zero jams;
- capped controls and blind replication remain cited from the private research corpus.

Residuals:

- theorem is conditional on the open fresh-mark rule;
- it does not decide which admission rule nature runs.

## FCT-10 - Probability As Counting Plus A Typicality Residual

Status: `THEOREM / RECOVERY / LIMITED`
Scope: finite staged forks and bounded local witness families.
Evidence state: `shipped` plus `cited`.

Public statement:

The counting measure is the coherent public measure at declared finite scope,
and frequency matching is a finite counting theorem with an explicit typicality
residual.

Evidence:

- shipped: `verification/scripts/frequency_bridge_exchangeable.py`;
- shipped:
  `verification/results/FCT-10-frequency-bridge-exchangeable-RESULTS.md`;
- cited: the private research corpus.

Checks / controls:

- shipped script verifies the exact fixed-fork exchangeable core;
- growing-floor compensator and greedy/anti-greedy controls remain cited.

Residuals:

- the single-case chance residual `[TYP]` is not dissolved.

## FCT-11 - Receiver Is Not An Agent

Status: `NOTHING / RECOVERY`
Scope: event-presheaf and measure-gluing formulations.
Evidence state: `cited`.

Public statement:

The "receiver" is dissolved as an agent: at public scope it is a gluing
condition over contexts or fragments, not a mind or selector.

Evidence:

- the private research corpus;
- public extraction map and admissibility-calculus posture.

Checks / controls:

- compare one-receiver and two-receiver contrast models;
- forbid personified receiver language in public claims.

Residuals:

- actual observers are later aperture/reception structures, not primitive
  selectors.

## FCT-12 - Continuum Is An Admitted Completion Layer

Status: `RECOVERY / OPEN`
Scope: finite algebraic/computable horizons and constructive prediction paths.
Evidence state: `cited`.

Public statement:

The floor supplies finite and computable skeletons; completed continuum objects
are overlays unless a finite reconstruction supplies the needed predictive
content.

Evidence:

- private AC-25;
- continuum/finite reconstruction audits.

Checks / controls:

- replace completed-object use with finite certificate or computable horizon
  where possible;
- identify where completion remains an overlay.

Residuals:

- interacting QFT continuum limit remains the universal hard edge;
- no claim that the completed continuum is natively derived.

## FCT-13 - Aperture-Level Energy And Temperature

Status: `MODEL-SCOPE / RECOVERY`
Scope: tested level-2 visible sectors where visible entropy production
vanishes.
Evidence state: `cited`.

Public statement:

Temperature and thermodynamic energy appear at aperture level exactly where
the visible sector is reversible at tested scope. Physical geometric windows
can retain non-equilibrium residue.

Evidence:

- the private research corpus;
- the private research corpus;
- level-2 and leak-lattice results.

Checks / controls:

- exact-hider comparisons;
- visible entropy production grading;
- leak/lattice controls.

Residuals:

- gravity sourcing remains open;
- observer-lattice is not yet a nature-facing prediction.

## FCT-14 - Arrow Divergence Withdrawn, Thermodynamics Recovered

Status: `WITHDRAWN / RECOVERY`
Scope: trace-arrow and stochastic-thermodynamics audits.
Evidence state: `cited`.

Public statement:

The proposed arrow statistic is not a public nature-facing divergence. What
survives is recovery: one-use/counting machinery reconstructs standard
stochastic thermodynamic structure at tested scope.

Evidence:

- the private research corpus;
- the private research corpus;
- private `verification/arrow_nature_strike_RESULTS.md`.

Checks / controls:

- failed nature-facing/divergence gates are recorded in the correction ledger;
- known stochastic thermodynamics is treated as recovery.

Residuals:

- do not advertise this as a prediction.

## FCT-15 - Fixed Response Class Program

Status: `OPEN`
Scope: stacked gates across flow, records, composition, coarse-graining, and
held-out substrates.
Evidence state: `cited`.

Public statement:

The strongest frontier thesis is that physics-like structure is a stable
response class under multiple admissibility gates. This is a theorem program,
not a completed theorem.

Evidence:

- private AC-24;
- fixed-class audits.

Checks / controls:

- held-out substrates;
- stacked gates rather than one favorable projection.

Residuals:

- remains open and should not headline the release as completed.

## FCT-16 - CHSH/Pell Boundary At Rational Scope

Status: `THEOREM / RECOVERY`
Scope: CHSH-family rational partners; one graph family; `[REAL]` completion
separated.
Evidence state: `shipped` plus `cited`.

Public statement:

The CHSH-family boundary can be walked by finite rational partners, with a
Pell fence deciding which side of Tsirelson a rung occupies.

Evidence:

- shipped `verification/scripts/chsh_pell_boundary.py` and its ledger
  `verification/results/FCT-16-chsh-pell-boundary-RESULTS.md`;
- cited private research corpus (glue-depth boundary certificate);
- cited admissibility-calculus tensor-carrier notes.

Checks / controls:

- the shipped script verifies the exact Pell ladder, the `p^2-2q^2=-1` fence,
  and the CHSH readout, with an above-boundary control rung;
- the full theta-body / semidefinite boundary certificate remains cited;
- the public release keeps `[REAL]` completion separate.

Residuals:

- product law, left inclusion, general scenarios, and `[REAL]` completion
  remain scoped.

## FCT-17 - Native Carrier Grammar

Status: `THEOREM / MODEL-SCOPE`
Scope: binary-Bell finite carrier scope; binary two-setting sites.
Evidence state: `shipped` plus `cited`.

Public statement:

The carrier grammar of local involutions, recurrent multiplication,
idempotent splits, site-disjoint tensoring, positive readout, and block form is
generated from one-use contact rather than imported as Hilbert structure.

Evidence:

- shipped: `verification/scripts/native_lift_binary_bell.py`;
- shipped:
  `verification/results/FCT-17-FCT-18-native-lift-binary-bell-RESULTS.md`;
- cited: the private research corpus.

Checks / controls:

- shipped script verifies the exact involution, witness-overlap, and
  site-disjoint factorization cores;
- full positive-readout/block-form work remains cited.

Residuals:

- q >= 3, more outcomes, cross-site interlocking, and general selector remain
  open.

## FCT-18 - Witness-Overlap Complementarity

Status: `THEOREM / MODEL-SCOPE`
Scope: tested finite binary carrier families.
Evidence state: `shipped` plus `cited`.

Public statement:

The carrier complementarity angle is a counting quantity:
`t(k,n)=k/n`, the fraction of witnesses shared by two settings.

Evidence:

- shipped: `verification/scripts/native_lift_binary_bell.py`;
- shipped:
  `verification/results/FCT-17-FCT-18-native-lift-binary-bell-RESULTS.md`;
- cited: the private research corpus.

Checks / controls:

- public script verifies `n=4` and `n=6` exactly;
- private research corpus carries broader context.

Residuals:

- uniform intermediate block was exhibited, not grown.

## FCT-19 - Binary-Bell Quantum Boundary

Status: `THEOREM / RECOVERY`
Scope: binary-Bell finite projective binary tensor carrier scope.
Evidence state: `cited`, supported by shipped FCT-17/FCT-18 core checks.

Public statement:

Standard finite tensor certificates bind a floor-grown carrier at this scope.
Therefore the binary-Bell quantum boundary is a floor theorem at this scope.

Evidence:

- cited: the private research corpus;
- cited: the private research corpus;
- shipped support: FCT-17/FCT-18 core identity scripts.

Checks / controls:

- carrier-growth part is supported by shipped and cited native-lift evidence;
- tensor certificate part is cited recovery/certificate evidence, not shipped
  as public script in v0.1.

Residuals:

- do not claim all quantum mechanics;
- Sliwa certificates are imported recovery;
- Sliwa-41 artifacts need public packaging or citation-only wording.

## FCT-20 - Carrier States And Temperature Share The Reversible Sector

Status: `MODEL-SCOPE / MEASURED / RECOVERY`
Scope: tested recurrent-chain binary-fork family; visible zero-EP /
detailed-balance sector.
Evidence state: `cited`.

Public statement:

Positive carrier readout exists exactly where the visible sector has
temperature: the same detailed-balance condition read through two apertures.

Evidence:

- cited: the private research corpus;
- cited: the private research corpus;
- cited: the private research corpus.

Checks / controls:

- private tests compare reversible and driven sectors;
- must-fail driven controls remain cited.

Residuals:

- positivity-implies-detailed-balance is not proved generally;
- driven sectors remain tensioned with CHSH-weight needs.

## FCT-21 - Behavior-Conditioned Capacity And The Universal Inequality

Status: `THEOREM`
Scope: general finite exclusivity graphs `G` with positive weights; sharp
(projective, operationally exclusive) realizations of a fixed behavior.
Evidence state: `cited`, with shipped support via the FCT-22 certificate
script.

Public statement:

For a behavior `p` on `G`, the behavior-conditioned capacity
`kappa_G(p) = inf ||sum_i w_i P_i||_op` over sharp realizations satisfies
`beta(p) = sum_i w_i p_i <= kappa_G(p)`. The capacity reduces faithfully to a
Gram-fiber semidefinite program (bordered-Gram reconstruction both ways), and
rank-one pure realizations suffice: purify-and-project compresses any sharp
realization behavior- and exclusivity-preservingly without increasing
capacity.

Evidence:

- shipped: the FCT-22 certificate exercises the SDP fiber and the
  compression step end-to-end at pentagon scope;
- cited: private verification suite (SDP round-trips, 20 reconstruction
  cases; 30 random-realization compression checks).

Checks / controls:

- the inequality is a one-line trace bound; faithfulness is a Schur-complement
  construction in both directions;
- compression verified on random mixed/higher-rank realizations.

Residuals:

- the compression theorem is fenced to this positive weighted-capacity
  objective; rank matters for other contextuality quantities;
- general-graph claims beyond the definitions, inequality, SDP, and
  compression are not made.

## FCT-22 - Exact Strict Preparation Gap

Status: `THEOREM` (exact rational arithmetic; hand-checkable)
Scope: the pentagon (KCBS) exclusivity scenario with unit weights; the named
rational behavior; all sharp realizations in all finite dimensions.
Evidence state: `shipped`.

Public statement:

The behavior `p = (49/100, 25/81, 16/25, 36/121, 4/9)` with
`beta = 2137213/980100` is realizable by a sharp quantum model, and every
sharp realization satisfies `||sum_i P_i||_op > beta` strictly; hence
`Delta_prep(p) > 0`. Realizability is proved by an exact rational LDL
factorization; impossibility by exact rigidity (unique real part, determinant
`112/495`) plus an exact rational negative witness that kills the entire
Hermitian completion family. No floating-point number is load-bearing.

Evidence:

- shipped: `verification/scripts/exact_gap_certificate.py` and
  `verification/results/FCT-22-exact-gap-certificate-RESULTS.md`; frozen
  chapter copy under `papers/02-behavior-conditioned-capacity/verification/`.

Checks / controls:

- every load-bearing step is `fractions.Fraction` arithmetic;
- the first search candidate failed realizability and was discarded —
  realizability is checked, not assumed;
- the numerical SDP magnitude (`~ 0.0096474`) is quoted as context only; the
  shipped script proves strictness.

Residuals:

- one behavior, one scenario: no genericity claim rides on this row (see
  FCT-24 for the corrected geometry);
- magnitude is numerical, strictness is exact.

## FCT-23 - Headroom Law: The Gap Is A Re-Preparation Resource

Status: `THEOREM`
Scope: inequality at general scope; canonical receiver closed form at
pentagon scope.
Evidence state: `cited`.

Public statement:

Every sharp implementation of a behavior carries headroom
`h = ||K||_op - Tr(rho K) >= Delta_prep(p)`, and re-preparation at the top
eigenvector pays out exactly `h`. A strictly gapped behavior therefore
certifies, from the behavior alone, that any device showing it can extract at
least `Delta_prep` more weighted event-mass by re-preparation. On the
canonical pentagon receiver, `h_can = g(1 - F)` with `g = (3*sqrt(5)-5)/2`,
with an asymmetry lower bound `h_can >= ((5-sqrt(5))/8) * A(p)`.

Evidence:

- cited: private rigidity-bridge verification (8/8 checks).

Checks / controls:

- the inequality is immediate from the definition of `kappa` as an infimum;
- the payoff identity and canonical closed form verified numerically in the
  private suite.

Residuals:

- the robust bridge to asymmetry with universal constants is `OPEN` (C1);
- "resource" means the operational re-preparation payoff defined here, no
  more.

## FCT-24 - Pentagon Tightness Phase Geometry

Status: `THEOREM` (criterion) + `MEASURED` (geometry)
Scope: tightness criterion general; all geometry statements `C_5`-fenced.
Evidence state: `cited`.

Public statement:

`Delta_prep(p) = 0` iff the top-eigenvector Gram feasibility system is
solvable. On the pentagon: the full symmetric contextual line is tight
(`kappa = q`, `2 <= q <= sqrt(5)`); the tight set strictly contains the
symmetric orbit and has interior; explicit gapped behaviors exist; the
boundary is semialgebraic with two named crossing modes; the symmetric spine
is the singular locus of the rigidity system. The earlier "gap is generic"
reading was a sampling artifact and is corrected on the record.

Evidence:

- cited: private SDP + certificate suite (behavior-receipt, global-gap,
  rigidity-bridge scripts).

Checks / controls:

- boundary crossings certified on both sides (Gram-positivity failure vs
  maximality failure);
- the correction history is published (chapter 2, section 5).

Residuals:

- the two-margin boundary machinery uses `#non-edges = #vertices`, special to
  `C_5`; general graphs keep only the feasibility criterion;
- `n = 7, 9` behavior of pentagon-exact laws is a named warning.

## FCT-25 - Sharpness Boundary And Novelty Posture

Status: `THEOREM` (construction) for the boundary; graded `INTERNAL-BLIND`
posture for novelty.
Scope: POVM trivialization at general scope; novelty claim as worded, no
stronger.
Evidence state: `cited`.

Public statement:

Unrestricted POVMs trivialize the gap (`E_i = p_i I` reproduces any behavior
at capacity `beta` and destroys the certifying exclusivity structure), so
`Delta_prep` is a sharpness-conditioned receiver invariant. The novelty of
the object package is claimed only at INTERNAL-BLIND evidence class: a
four-lens, repository-barred, refutation-first literature search (77
published query strings) found the functional, the gap, the exact
certificate, the phase geometry, and the headroom law NOT-FOUND as a package,
with all ingredients standard and nearest precedents named
(Sikora–Varvitsiotis–Wei; Moroder et al.; Bharti et al.). No external expert
reviewed this before release, and the paper says so.

Evidence:

- cited: the published novelty review with search logs and residual risks.

Checks / controls:

- collapse candidates to weighted theta refuted numerically at reference
  points;
- the in-house anchored dual derived and verified (strong duality to 1e-6).

Residuals:

- the four published residual risks (appendix-buried lemmas; no
  citation-database access; recent-preprint aliases; extremal self-testing
  folklore) remain open and are quoted in the paper.

## FCT-26 - The Waist: Identifiability Biconditional

Status: `THEOREM`
Scope: finite sets, arbitrary descriptions `pi : X -> D`, arbitrary purpose
equivalences `~_T`; two-sided (biextensional) form exhaustive for binary
pairings through 3x3.
Evidence state: `shipped` (`identifiability_debt_calculus.py`).

Public statement:

A purpose-respecting reconstruction from a description exists iff the
description's kernel is contained in the purpose's indifference relation
(`ker pi` within `~_T`); both directions are elementary and are verified on
400 randomized finite models per run with exhaustive per-model
counterexample search. The two-sided form: mutual-indistinguishability
quotients of a finite pairing yield a canonical biextensional core
independent of reduction order (verified exhaustively for all binary
pairings through 3x3 under four reduction orders).

Evidence:

- shipped: T-18 checks in `identifiability_debt_calculus.py`.

Checks / controls:

- both biconditional directions tested independently on every model;
- reduction-order independence tested against four distinct orders.

Residuals:

- infinite-model and structured-category versions are not claimed;
- which purposes any physical system runs is not claimed.

## FCT-27 - Selector Debt And The No-Equivariant-Selector Theorem

Status: `THEOREM`
Scope: finite alternative sets; receipt schemes into binary strings;
symmetry groups acting without a common fixed point.
Evidence state: `shipped`.

Public statement:

Exact later identification of which of `m` pairwise future-inequivalent
alternatives occurred requires at least `ceil(log2 m)` binary receipt
values, and this is attained (verified for `m = 2..8`). Separately: when a
symmetry group of the alternative set preserves all admitted structure and
fixes no alternative, no equivariant selection of a single alternative
exists (an equivariant point selection is a fixed point of the action); the
minimal two-element case is shipped with both candidate selections shown to
break equivariance while the alternative *set* and all symmetric
functionals remain invariant.

Evidence:

- shipped: T-19 checks in `identifiability_debt_calculus.py`.

Checks / controls:

- attainment and un-improvability both checked (the bound is exact, not
  merely a bound);
- selector non-existence checked by exhaustion over all candidate maps in
  the minimal case.

Residuals:

- the theorem forbids *derived, symmetry-respecting* selection; it does not
  assert that selection cannot occur, only that its record carries an
  irreducible cost and its rule is not a consequence of the admitted
  structure.

## FCT-28 - Continuation Sufficiency

Status: `THEOREM`
Scope: finite completion models (lawful completions restricted to present
boundaries; all-future protocol equivalence).
Evidence state: `shipped`.

Public statement:

A present boundary is future-complete iff all of its lawful completions are
future-equivalent — i.e. the restriction kernel on its fiber is contained
in all-future protocol equivalence. This is the waist (FCT-26) applied to
the description "present boundary" and the purpose "all future protocols";
verified as a biconditional on 400 randomized completion models per run.

Evidence:

- shipped: T-20 checks in `identifiability_debt_calculus.py`.

Checks / controls:

- both directions tested independently per fiber, all fibers per model.

Residuals:

- what counts as a lawful completion for any physical system is not
  claimed; the theorem is conditional on the completion model.

## FCT-29 - No Universal Tomography Depth

Status: `THEOREM` (construction)
Scope: explicit certificates for depths 1..8; the general construction is
elementary at all finite depths.
Evidence state: `shipped`.

Public statement:

For every depth `d` there exist completion pairs that agree under every
protocol of depth `< d` and separate at depth `d`; hence no finite protocol
family certifies future-completeness in general. Shipped as explicit
certificate pairs for `d = 1..8`.

Evidence:

- shipped: T-21 checks in `identifiability_debt_calculus.py`.

Checks / controls:

- each certificate's first separation depth computed and asserted equal to
  its advertised `d`.

Residuals:

- none at this scope; the statement is a construction.

## FCT-30 - Questions As Residual Operators; The Boolean Shadow

Status: `THEOREM`
Scope: finitely supported weight functions (formal series) over a finite
alphabet; the count-only biconditional exhaustive over the length-2
coefficient family; randomized composition-law checks at longer horizons.
Evidence state: `shipped` (`inquiry_calculus.py`).

Public statement:

Asking acts on epistemic objects as residual operators
(`(a^-1 S)(w) = S(aw)`), which compose contravariantly
(`u^-1 (v^-1 S) = (vu)^-1 S`). The operator algebra is noncommutative in
general — an explicit two-letter witness is shipped — and commutes exactly
on count-only (order-blind) series: the classical/Boolean sector of asking
is precisely the sector where histories carry no more than their letter
counts.

Evidence:

- shipped: T-22 checks in `inquiry_calculus.py`.

Checks / controls:

- composition law on 300 randomized series;
- noncommutativity witness explicit;
- count-only biconditional exhaustive on the 81-member length-2 family.

Residuals:

- the biconditional is shipped at exhaustive small scope; the general-
  horizon statement is elementary but not exhaustively verified here;
- no claim that quantum measurement is this calculus (structural rhyme is
  cited program context).

## FCT-31 - The Second Law Of Asking

Status: `THEOREM` (exact identity)
Scope: binary decision-tree protocols identifying finite sources.
Evidence state: `shipped`.

Public statement:

For any binary decision-tree protocol with leaf depths `d_x` and Kraft
weight `Z = sum 2^(-d_x) <= 1`, the expected cost decomposes exactly as
`EC = H(p) + KL(p || q) + O` with implicit belief `q_x = 2^(-d_x)/Z` and
slack `O = -log2 Z >= 0`. Hence `EC >= H(p)` always: the cost of asking is
irreducible entropy plus belief mismatch plus unused resolving power, and
nothing identifies below the entropy. The chapter-3 selector debt
`ceil(log2 m)` is the uniform worst-case corner of this law.

Evidence:

- shipped: T-23 checks in `inquiry_calculus.py`.

Checks / controls:

- identity verified to 1e-12 on 300 random tree/source pairs;
- Kraft inequality in exact rational arithmetic;
- the equality case (dyadic source, full tree) witnessed separately.

Residuals:

- binary answer alphabets only (general alphabets rescale the logarithm);
- protocols are identification trees; interactive games with lies/noise
  are not covered.

## FCT-32 - Adaptivity Interest

Status: `THEOREM` + witness
Scope: finite outcome spaces with an admitted-question family; exhaustive
strategy search at |X| = 4; strict witness under threshold questions.
Evidence state: `shipped`.

Public statement:

With the admitted question family part of the model, the optimal adaptive
expected cost never exceeds the optimal fixed expected cost
(`J = EC_fixed* - EC_adaptive* >= 0`, strategy-space inclusion), and the
inequality is strict on a shipped witness: the uniform source on four
ordered outcomes under threshold questions has adaptive cost `2.0` and
best fixed cost `2.25`. Notably, with unrestricted subset questions and
early stopping, fixed matches adaptive at this size — the interest is a
property of asking under a constrained repertoire.

Evidence:

- shipped: T-24 checks in `inquiry_calculus.py`.

Checks / controls:

- both optima computed by exhaustive search (all adaptive trees via
  recursion; all fixed orders);
- J >= 0 checked across a source grid; the strict witness printed with
  both values.

Residuals:

- small-scope exhaustion only; asymptotic interest rates not claimed.

## FCT-33 - Self-Question Typing: Paradox Is Type Collapse

Status: `THEOREM` (witnesses)
Scope: two-element answer space; graded recursion over six steps.
Evidence state: `shipped`.

Public statement:

Ungraded self-identity `x = x` has exactly two solutions; ungraded
self-negation `x = 1 - x` has none; the graded recursion
`x_(n+1) = 1 - x_n` has exactly two lawful orbits and no contradiction.
Self-reference is lawful when the grade separating a live asking from its
recorded answer is kept; paradox is exactly what remains when that grade is
collapsed.

Evidence:

- shipped: T-25 checks in `inquiry_calculus.py`.

Checks / controls:

- exhaustive at the stated scope.

Residuals:

- the general guarded-recursion/type-level framing is cited program
  context, not shipped mathematics.

## FCT-34 - Torsor Time: Lawful, Origin-Free

Status: `THEOREM`
Scope: `Z_n` under `[x, y, z] = x - y + z`, exhaustive for `n = 5, 6, 7`.
Evidence state: `shipped` (`becoming_webs.py`).

Public statement:

The time fiber modeled as a heap satisfies all heap axioms; choosing any
element as origin recovers a group and all such choices are equivalent; the
translation action is free and transitive, so no element is invariant —
there is no derivable "now". Differences, translations, and durations are
lawful without an origin; an origin is a selection on a symmetric fiber,
which chapter 3 (FCT-27/T-19) proves underivable and prices at
`ceil(log2 m)` receipt bits.

Evidence:

- shipped: T-26 checks in `becoming_webs.py`.

Checks / controls:

- heap identities and para-associativity exhaustive;
- group recovery checked at every origin;
- fixed-point-freeness of every nontrivial translation checked.

Residuals:

- finite cyclic models only; the reading of the private floor's time fiber
  as this torsor is cited program context.

## FCT-35 - The Helix: Law-Time Covers Visible-Time

Status: `THEOREM`
Scope: period-4 visible cycle with unit grade step; finite grade
truncation.
Evidence state: `shipped`.

Public statement:

A cyclic visible dynamics lifts to an ascending helix: the projection is a
covering; finite visible paths lift uniquely given a start; one traversal
of the visible cycle raises the grade by exactly the deck step `q`
(monodromy), and deck translations commute with the dynamics, acting freely
and transitively on fibers. The visible cycle is the quotient of the helix
by its deck group: a system can be exactly periodic in every visible
observable while its law-history strictly ascends.

Evidence:

- shipped: T-27 checks in `becoming_webs.py`.

Checks / controls:

- lifting, monodromy (from every basepoint), and deck commutation checked
  directly.

Residuals:

- one covering model at small scope; the identification of the private
  floor's law hysteresis with this helix is cited.

## FCT-36 - Arrow Without Thermodynamics

Status: `THEOREM` (exhaustive)
Scope: reversible rotation on `Z_4` coupled to a per-step ledger; all step
words through length 8.
Evidence state: `shipped`.

Public statement:

With both step directions admitted (fully reversible visible dynamics) and
a ledger that counts steps taken, the visible state returns often (170
returning words at the tested scope) but the joint (visible, ledger) state
never returns for any nonempty word; the ledger gap between visits to the
same visible state equals exactly the number of steps asked. An arrow of
time from pure bookkeeping — no probabilities, no entropy, no
coarse-graining. Undoing a step is another step.

Evidence:

- shipped: T-28 checks in `becoming_webs.py`.

Checks / controls:

- exhaustive over all 510 words to length 8; joint-return counter asserted
  zero; visible-return counter asserted positive (the contrast is the
  content).

Residuals:

- a possibility theorem: an arrow *can* arise this way; no claim that
  nature's arrow does — that reading is cited program context.

## FCT-37 - Lawfulness Without Foundation

Status: `THEOREM` (witnesses)
Scope: guarded/unguarded stream equations at truncation depths 1..4; the
Mobius-twisted 3-patch `Z_2` cover, exhaustive.
Evidence state: `shipped`.

Public statement:

Guarded self-reference is productive: `x = cons(a, x)` has exactly one
solution at every truncation depth, reached from every initial guess — no
base case, no foundation; the unguarded `x = tail(x)` has multiple
solutions at every depth, so uniqueness is the guard's doing. Separately:
the Mobius-twisted three-patch cover has perfect local sections and *no*
global section (exhaustive over all candidates; the untwisted control has
exactly two): a consistent global "now" can fail topologically while every
local time is flawless.

Evidence:

- shipped: T-29 checks in `becoming_webs.py`.

Checks / controls:

- fixed-point uniqueness verified from every initial condition;
- global-section exhaustion over all eight candidates, with the untwisted
  control run alongside.

Residuals:

- the general guarded-corecursion and gerbe/gluing framings are cited
  program context, not shipped mathematics.

## FCT-38 - The Public Floor Engine And Dynamics Spec

Status: `MODEL / INSTRUMENT`
Scope: the exact four-channel driven one-use dynamics specified in the
chapter (add-missing / add-fork / fill / remove, weights 1, 6, 0.5, 1, add
budget `E = n(n-1)/2`).
Evidence state: `shipped` (`floor_engine_measurements.py`).

Public statement:

A complete, self-contained, dependency-free implementation of the driven
one-use generative dynamics is published, together with its exact spec, a
replay facility, and a maximally-coupled counterfactual mode (identical
random stream, one redirected fork choice). The engine is an *independent
implementation* — it shares no code, no language constructs of record, and
no bitwise relationship with the private laboratory engine; agreements
between the two are therefore evidence about the dynamics, not about a
codebase.

Evidence:

- shipped: the engine section of `floor_engine_measurements.py`; budget
  consumption asserted for every world.

Checks / controls:

- one-use budget consumption asserted per world;
- counting observables only (no clock-stamped quantities).

Residuals:

- the engine is not bitwise-reproducible against the private engine (by
  design); parameters other than the shipped (1, 6, 0.5, 1) are not
  measured here.

## FCT-39 - Delayed Individuation Is Measured

Status: `MEASURED / MODEL-SCOPE`
Scope: 18 worlds (n = 32 ×10, n = 40 ×8), fixed seeds; twin = identical
nonempty adjacency rows.
Evidence state: `shipped`.

Public statement:

The drive manufactures twins — constituents the world cannot yet tell
apart — and the overwhelming majority separate before world end: 262
births, separation fraction 0.927, censoring 7.3% (fence asserted:
> 0.75). Independent-implementation agreement: the private engine
(different codebase, n = 60/90, 30 seeds) measured 0.927/0.911 with 7–9%
censoring. Individuation is delayed but arrives; the phenotype is a
property of the dynamics.

Evidence:

- shipped: T-30 check in `floor_engine_measurements.py`;
- cited: the private twin-wait census (M14-class result) at larger scale.

Checks / controls:

- births asserted positive; censoring counted and printed;
- raw-twin definition stated exactly (non-adjacent, nonempty rows).

Residuals:

- measured at the shipped sizes and seeds; no n-scaling law is claimed
  here.

## FCT-40 - Separation Waits Are Short-Tailed

Status: `MEASURED / MODEL-SCOPE`
Scope: 243 uncensored separation waits pooled over the shipped worlds;
maximum likelihood on both laws; power-law exponent grid 1.05–6.00.
Evidence state: `shipped`.

Public statement:

Twin-separation waits (in add events) are exponential-form: the geometric
MLE (mean wait 9.0 adds) beats the best discrete power law by ΔLL = 67.8
(fence asserted: > 10, decisive). Separation is a short-tailed process,
not a scale-free one — reproducing the private engine's tail verdict on
the same quantity.

Evidence:

- shipped: T-31 check in `floor_engine_measurements.py`.

Checks / controls:

- both laws fit by maximum likelihood; censored waits excluded from both.

Residuals:

- this constrains separation waits only; novelty-birth waits (a different
  quantity, with its own private history) are not measured here.

## FCT-41 - Counterfactual Defects Are Ballistic

Status: `MEASURED / MODEL-SCOPE`
Scope: 6 maximally coupled world pairs (n = 32), one redirected fork
choice each, defect tracked at matched add counts k = 0..60.
Evidence state: `shipped`.

Public statement:

Under maximal coupling (identical random stream, one fork choice
redirected to the closest-weight alternative), the edge-set defect between
the actual and counterfactual worlds grows linearly with subsequent
contacts: slope 1.25 edges/add, R² = 0.998 (fences asserted: slope > 0.3,
R² > 0.90, monotone). One changed contact neither heals nor explodes — it
propagates at a constant rate, a counted light-cone-like memory of the
counterfactual. The private instrument measured the same ballistic law
(defect mass ≈ 2 + 1.4k).

Evidence:

- shipped: T-32 check in `floor_engine_measurements.py`;
- cited: the private counterfactual-perturbation campaign.

Checks / controls:

- coupling verified by construction (every random draw identical);
- linearity, slope, and monotonicity all fenced.

Residuals:

- defect RADIUS (cone speed proper, as opposed to mass) is not measured
  here; the private radius instrument history is cited.

## FCT-42 - The Two-Axis Public Stack

Status: `PROGRAM / INDEX` — **frozen v0.7.0-tag snapshot; superseded as the
live index by the v0.10 state** (the live ceiling above: three recovery lines
plus a first extension/divergence line, Chapter 10, and rows through
FCT-61 / T-45 / fifteen scripts). Index rows are append-only and not rewritten;
this row remains the accurate index *as of the v0.7.0 tag*.
Scope: the public repository at the v0.7.0 tag — claim rows FCT-01..FCT-41,
theorem rows T-01..T-32, organized as a quantum-facing axis (chapters 1–2)
and a finite-epistemics axis (chapters 3–6).
Evidence state: `shipped` by reference — the full `run_all.py` suite
(ten scripts) passes on a clean clone.

Public statement:

The published program is exactly its rows: each is runnable or cited as
labeled, each carries its own scope and residuals, and the chapter-7 map
adds no claim beyond the organization itself. Falsified by any row failing
its shipped check or misstating its scope.

Evidence:

- shipped: the entire verification suite; the release audit.

Checks / controls:

- `run_all.py` (chapters 1–6 scripts) and `release_audit.py` both pass at
  the tag.

Residuals:

- the map's architecture commentary (how the axes meet) is exposition, not
  a claim row.

## FCT-43 - The Forced/Received Schema

Status: `PROGRAM / SCHEMA`
Scope: shipped instances named below; the program-level generalization is
cited research posture.
Evidence state: `shipped` instances + `cited` schema.

Public statement:

Across every published chapter, what the finite structure forces is
grammar, signs, order, exact biconditionals, and exact constants of form
(the `ceil(log2 m)` debt, `EC = H + KL + O`, the heap identities, the
helix monodromy); what it never forces is a selection — which alternative,
which origin, which magnitude. Shipped instances: T-18/T-19, T-23, T-26.
The generalization — that this forced/received split is the deep structure
of physical law — is a schema, not a shipped theorem, and carries this
label precisely so it cannot be read above its evidence.

Evidence:

- shipped: the named theorem rows;
- cited: the private program's forced/received boundary corpus.

Checks / controls:

- each named instance has its own shipped check.

Residuals:

- no criterion is shipped for classifying an arbitrary new structure as
  forced or received; the schema earns rows only through new scoped
  theorems.

## FCT-44 - The No-Selector Recurrence

Status: `PROGRAM / SCHEMA`
Scope: T-19 shipped; its recurrence across shipped rows checkable; the
program-level reading cited.
Evidence state: `shipped` instances + `cited` schema.

Public statement:

One obstruction arrives at every level examined: a symmetry-respecting
rule cannot select a point of a symmetric fiber (T-19). Any actual
selection — an origin of time (T-26), an actual alternative, a unit or
scale (cited) — must be received, and its record costs at least
`ceil(log2 m)` bits, refined by the second law of asking (T-23) and run
forward as the ledger arrow (T-28). The reading of selection as the single
received residue of physics is cited posture, labeled as such.

Evidence:

- shipped: T-19, T-23, T-26, T-28;
- cited: the private no-selector corpus (multiple independent arrivals).

Checks / controls:

- the shipped instances are machine-checked; the recurrence is checkable
  by reading the rows together.

Residuals:

- "every level examined" is bounded by the published chapters; new levels
  must earn their own rows.

## FCT-45 - Non-Exact Return And The Quaternionic Receiver

Status: `MODEL-SCOPE / RECOVERY`
Scope: the minimal real receiver faithful on the retained central return
of a single contact cell.
Evidence state: `shipped`.

Public statement:

The three retained quarter-turns of a non-exact-return contact satisfy the
quaternion relations; the minimal real carrier faithful on the retained
return `z -> -1` is four-dimensional and irreducible (its real commutant is
the division algebra `H`), and the invariant positive form is unique up to
scale, forcing the isotropic quadratic evaluation and a `1+3` split.

Evidence:

- shipped: Section 1 of
  `verification/scripts/nonexact_return_reconstruction.py`.

Checks / controls:

- quaternion relations, commutant dimension, and invariant-form dimension
  all computed in exact arithmetic; dims 1, 2, 3 excluded by explicit
  arithmetic / Frobenius-Schur / determinant arguments.

Residuals:

- the `1+3` split is an algebra grading, not a spacetime metric;
- the projective phase that would complexify the cell is held open;
- **starting-datum disclosure (corpus dependency).** The public forcing
  chain of Chapter 8 begins at the *retained interface* — it takes the
  three retained quarter-turns (equivalently, the retained central return
  `z -> -1` and its chirality) as its given datum and derives `H`, the
  24-cell, `F_4`, the forced `1/2`, finite Gleason, and triality
  Kochen-Specker from there, all by shipped exact checks. Why exactly those
  quarter-turns are the retained ones — the floor-level selection/chirality
  analysis upstream of the interface — is `cited` from the private corpus,
  not shipped here. So this row and FCT-46..50 are forced *given the
  retained interface*; the link from the bare contact floor to that
  interface is a named cited dependency, not a public theorem. This is the
  program's deepest current corpus dependency and is called out as such
  (see also [related-work note discipline](related-work-linear-optics.md)
  for the sibling recovery-grading pass on Chapters 10-12).

## FCT-46 - The Self-Hosting State/Receiver Fixed Point (24-cell)

Status: `MODEL-SCOPE / RECOVERY`
Scope: the unit-state polytope of the quaternionic cell and its polar dual.
Evidence state: `shipped`.

Public statement:

The unit states are the 24 Hurwitz units (the 24-cell); exhaustive polar
enumeration gives `conv(states)` polar = the 24 receiver vectors and
`conv(receivers)` polar = the 24 states. States generate receivers and
receivers regenerate the states: an exact self-hosting state/receiver
fixed point.

Evidence:

- shipped: Section 2 (all `C(24,4)=10626` supporting quadruples enumerated
  exactly).

Checks / controls:

- polar computed both directions; the fixed-point identity is the exact
  set equality of the enumerated vertices.

Residuals:

- recovers the known 24-cell self-duality; novelty is the derivational
  role (states and receivers co-generate on one finite object).

## FCT-47 - The F_4 Closure Of The State/Receiver Polarity

Status: `RECOVERY`
Scope: the 48 combined state and receiver vectors.
Evidence state: `shipped`.

Public statement:

The 48 vectors are reflection-closed with integer Cartan numbers — the
`F_4` root system — and its Weyl group has order 1152. The exceptional
root system arises as the reflection closure of the state/receiver
polarity, not as an installed gauge symmetry.

Evidence:

- shipped: Section 3 (closure and integrality checked over all pairs;
  `|W(F_4)|=1152` by direct generation).

Checks / controls:

- reflection closure and Cartan integrality are exhaustive over the 48
  roots; the Weyl order is generated, not asserted.

Residuals:

- the two-floor `E_8` extension is a later chapter, not claimed here.

## FCT-48 - The Forced 1/2 Interface Magnitude

Status: `MODEL-SCOPE`
Scope: state/receiver overlaps on the 24-cell closure.
Evidence state: `shipped`.

Public statement:

Every nonzero state/receiver overlap has `|<p,r>| = 1`, so the
dimensionless ratio `|<p,r>|^2/(|p|^2|r|^2) = 1/2` for every contact (a
`pi/4` angle): a forced, scale-free, gauge-free interface invariant, the
first nontrivial magnitude the closure generates.

Evidence:

- shipped: Section 4 (all `24 x 24` overlaps computed exactly).

Checks / controls:

- exhaustive over all state/receiver pairs; the magnitude is a ratio.

Residuals:

- scope is this 24-cell closure, not nature; a ratio, not a dimensionful
  constant.

## FCT-49 - Finite Gleason: The Born Frame Rule

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: normalized frame valuations on the 24-ray Peres configuration.
Evidence state: `shipped`.

Public statement:

The 24 rays carry exactly 24 orthonormal tetrad contexts; the
context-incidence rank is 15, so frame valuations form a nine-dimensional
affine family equal to real-symmetric trace-one `Sym(4,R)` (the projectors
span, every context resolves the identity). Therefore every normalized
frame valuation is uniquely `f(x)=tr(rho P_x)` — the quadratic Born frame
rule, forced by the finite context family with no continuum and no
continuity.

Evidence:

- shipped: Section 5 (exact rank and span computations; resolution of
  identity per context).

Checks / controls:

- the two nine-dimensional families are matched by exact dimension count;
  the projector span (rank 10 in `Sym(4)`) gives uniqueness.

Residuals:

- recovers a finite, exact Gleason theorem; does not derive the universal
  Born rule for general measurements;
- real-quantum scope (`Sym(4,R)`), not complex.

## FCT-50 - Triality Contextuality; Born And Pointing Are Two Faces

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: deterministic noncontextual assignments on the same 24 rays.
Evidence state: `shipped`.

Public statement:

No global deterministic noncontextual assignment exists; there are exactly
16 minimal nine-context parity proofs (Kernaghan-type), all in the
state/receiver interface, each using three contexts from every `D_4`
triality sector; any one or two sectors are consistent, all three are not.
Neither pole is contextual alone — contextuality is an irreducibly
three-way (triality) obstruction generated at the interface. Placed beside
FCT-49: the valuation is uniquely lawful exactly where a global pointing is
impossible; Born valuation and Kochen-Specker contextuality are the two
faces of one finite closure.

Evidence:

- shipped: Section 6 (global-section search exhaustive; the 16 parity
  proofs enumerated and sector-classified).

Checks / controls:

- both the impossibility and the sector-restricted possibilities are
  exhaustive; every proof's `3+3+3` sector split is verified.

Residuals:

- recovers the Peres 24-ray Kochen-Specker set (Kernaghan proofs);
- the actual outcome must be contextual, history-bearing, explicitly
  received, or totalized — the standing open hinge; complex quantum
  mechanics and every nature-facing prediction remain held open.

## FCT-51 - The Triality Boundary And The Bridge-Code Model

Status: `MODEL-SCOPE / SCHEMA`
Scope: the boundary of a single non-exact-return cell and the additive
glue-code model of multi-cell binding.
Evidence state: `shipped`.

Public statement:

Each cell carries a `D_4` triality boundary `K = C_2 x C_2` (with `S_3`
triality on `{v,s,c}`); a bridge ecology is an additive glue code
`H <= K^N`, with bridge debt `2N - 2 dim H`. Operational receiver-
completeness is a separating dual pole `H < H-perp` (verified: an isotropic
seed over `GF(4)` has `|H| = 4` strictly inside `|H-perp| = 64`); self-
duality `H = H-perp` is the terminal CLASS an equivariant dynamics can
force (FCT-58), not a definition of completeness, and it still does not pick
a member (FCT-57).

Evidence:

- shipped: Section 1 of `verification/scripts/multifloor_worldweave.py`;
  the dual-pole and self-dual-orbit facts in Section A of
  `verification/scripts/forcing_audit.py`.

Checks / controls:

- the Klein-four structure and `S_3` triality are computed exactly; the
  dual pole `H < H-perp` and the three-member `S_3` orbit are exact.

Residuals:

- a structural model under named closure assumptions; not a claim that
  nature runs this code; do NOT read "completeness" as "self-duality".

## FCT-52 - E_8 As The Unique Debt-Free Two-Cell Closure

Status: `MODEL-SCOPE / RECOVERY`
Scope: the bridge geometry of two cells.
Evidence state: `shipped`.

Public statement:

Two cells' triality boundaries close into the `E_8` root system:
`240 = 48 (D_4 + D_4) + 3 x 64` triality-matched bridges, reflection-closed,
with the glue-lattice determinant ladder `16 -> 4 -> 1` (debt `4 -> 2 -> 0`
bits). `E_8` is the unique debt-free two-cell world.

Evidence:

- shipped: Section 2 (decomposition, reflection closure, and determinant
  ladder all exact/exhaustive).

Checks / controls:

- 240 roots classified into the internal `D_4 + D_4` and the three `64`-root
  triality bridges with no leftovers; determinants by exact Gram elimination.

Residuals:

- `E_8` is the complete bridge geometry of two cells, NOT an installed
  physical gauge group; nature is not claimed to realize it;
- this is a recovery UNDER the named receiver laws; those laws are not
  floor-selected over matched lawful alternatives (FCT-57), so `E_8` is a
  forced *point of the atlas*, not a forced *choice*.

## FCT-53 - Octonionic Fusion; A Three-Contact Receipt

Status: `RECOVERY`
Scope: the local fusion algebra of two quaternionic cells.
Evidence state: `shipped`.

Public statement:

Fusing two quaternionic receivers gives the octonions; of the 35
imaginary-unit triples exactly 7 associate and 28 do not, so
nonassociativity is a pure three-contact order receipt.

Evidence:

- shipped: Section 3 (associator census over all `C(7,3)` triples, exact).

Checks / controls:

- Cayley-Dickson product in exact arithmetic; the 7/28 split is the Fano
  line structure.

Residuals:

- recovers the octonions and the Fano-line structure.

## FCT-54 - Normed Fusion Stops At The Octonions

Status: `RECOVERY`
Scope: the Cayley-Dickson tower above the octonions.
Evidence state: `shipped`.

Public statement:

The sedenions have zero divisors while the octonions have none, so positive
normed monolithic fusion terminates at the octonions; larger populations
cannot be one division algebra and must remain a code worldweave.

Evidence:

- shipped: Section 4 (explicit sedenion zero divisor; octonion search
  returns none).

Checks / controls:

- both factors verified nonzero and the product exactly zero.

Residuals:

- the specific zero-divisor indices are Cayley-Dickson-convention-dependent;
  existence is the structural claim.

## FCT-55 - Self-Dual Worlds Are The Unitary Orbit GU(k,2)

Status: `MODEL-SCOPE / RECOVERY`
Scope: triality-covariant Hermitian self-dual standard-form codes on `N`
cells.
Evidence state: `shipped`.

Public statement:

The self-dual code census is `|GU(N/2, 2)|`: 3 for `N=2`, 18 for `N=4`, 648
for `N=6` -- the exhaustive standard-form search confirmed by the
finite-unitary-group closed form.

Evidence:

- shipped: Section 5 (all `4^{k*k}` candidate matrices enumerated exactly).

Checks / controls:

- the counts match `|GU(k,2)|` independently.

Residuals:

- recovers `GU(k,2)`; a structural model, not a nature claim.

## FCT-56 - Six Cells: The First Hidden World (The Hexacode)

Status: `MODEL-SCOPE / RECOVERY`
Scope: the minimum-distance structure of six-cell self-dual codes.
Evidence state: `shipped`.

Public statement:

Of the 648 self-dual six-cell codes, 162 have minimum distance 2 and 486
have minimum distance 4; the distance-four phases (canonically the hexacode
`[6,3,4]`, weight enumerator `1 + 45 y^4 + 18 y^6`, Hermitian self-dual) have
no one-, two-, or three-cell bridge words, so a complete six-cell world can
be invisible to every pairwise and triple probe. `N=6` is the first such
hidden collective world.

Evidence:

- shipped: Section 6 (hexacode weight enumerator + self-duality; the
  162/486 minimum-distance split over all 648 codes).

Checks / controls:

- the hexacode weight enumerator and Hermitian self-orthogonality computed
  exactly; the distance split is exhaustive.

Residuals:

- whether nature realizes any code, and any metric/spectrum/constant, are
  held open; recovers the hexacode.

## FCT-57 - Static Forcing Audit: The Closure Laws Are Not Forced

Status: `THEOREM / MODEL-SCOPE`
Scope: the five receiver laws (positivity, integrality, triality alphabet,
self-dual completion, delocalization) that produce the E_8-hexacode spine.
Evidence state: `shipped`.

Public statement:

Each closure law has a matched lawful counterworld the floor does not rank
below the spine: (a) a doubly-even self-dual `[24,12,4]` code (six
weight-four words = the six local port receipts; explicitly constructed and
MacWilliams-verified) shows Golay's parity is an extra delocalizing law,
not forced; (b) the bridge family `G_t = [[I,tI],[tI,I]]` has determinant
`(1-t^2)^4` and is positive at the irrational `t = sqrt(2)/2`, indefinite at
`t = 2`, so positivity/integrality/magnitude are not singled out; (c) both
`Q8/{+-1} = V4` and `2T/Q8 = C3` are valid export quotients; (d) operational
completeness gives a dual pole `H < H-perp`, and the self-dual codes are an
`S_3` orbit; (e) `E_8 + E_8 + E_8` is even-unimodular with 720 roots, so
rootlessness (Leech) is an extra law.

Evidence:

- shipped: Section A of `verification/scripts/forcing_audit.py` (exact
  Krawtchouk / MacWilliams; the matched `[24,12,4]` code by exhaustive
  port-map search; the determinant, group-quotient, dual-pole, and
  lattice-root computations).

Checks / controls:

- both weight enumerators are MacWilliams-self-dual and doubly-even; the
  counterworld code is fully constructed and verified, with an independent
  `[24,12,4]` witness.

Residuals:

- the specific zero-divisor / port-map representatives are convention-
  dependent; the counterworld *existence* is the claim.
- recovers: Pless-Sloane doubly-even self-dual `[24,12]` classification;
  the MacWilliams identity; Leech rootlessness.

## FCT-58 - Dynamic Selection Equivalence: Selection Is A Conserved Input

Status: `THEOREM / MODEL-SCOPE`
Scope: deterministic-settling and reversible-stochastic dynamics on a finite
phase set (the floor's own gradient-on-a-potential dynamics is inside this
class).
Evidence state: `shipped`.

Public statement:

Unique strict dynamic selection is equivalent to a preferred scalar ranking:
of the 256 maps on four phases, exactly 64 (the rooted-tree count
`4^3 = n^{n-1}`) strictly settle to one terminal phase, and every one
carries a Lyapunov ranking `V(F(x)) = V(x) - 1`; reversible dynamics has
stationary law `pi ~ e^{-V}` (`V = -log pi`). So dynamics never CREATES a
selector; it RELOCATES a conserved one into a potential / kernel / rate law
(two passive potentials on the same bridge family select an integral vs an
irrational attractor; the `V4/C3` crossover is `lambda* = 2 - log2(3)`). The
one positive: self-duality can be forced as a terminal CLASS, but its
members are `S_3`-transitive, so target-blind equivariant dynamics yields
uniform `1/3`.

Evidence:

- shipped: Section B of `verification/scripts/forcing_audit.py` (the 64
  settlers + Lyapunov; exact detailed balance; the potentials and
  `lambda*`; the `S_3`-orbit `1/3`).

Checks / controls:

- the settler count and Lyapunov property are exhaustive over all 256 maps;
  detailed balance is exact.

Residuals:

- the theorem covers deterministic-settling and reversible-stochastic
  dynamics; non-reversible driven dynamics is not claimed here (though it
  too carries a burden functional).
- recovers: the rooted-tree count; Lyapunov / detailed-balance theory.

## FCT-59 - Actualization By Counting: The FORM Is Forced, Born Is Not

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: the internal statistics of a GIVEN (actual) code phase.
Evidence state: `shipped`.

Public statement:

The floor cannot select which phase exists; given an actual phase, counting
terminal witnesses forces the FORM of its internal law, but NOT the Born
rule. On the 24-cell (12 rays in 3 orthonormal frames), the 144 ordered
histories split 48 return + 96 transfer, and the most general positive
equivariant weight is a single ticket `alpha` (return) and `beta` (transfer)
times the incidence `|<p,q>|^2` -- a lawful stochastic response for every
`alpha,beta>0`, with `P(p->p)=alpha/(alpha+2beta)`,
`P(cross)=beta/(4alpha+8beta)`, `P(orthogonal)=0`. The Born kernel
`K=(1/3)|<p,q>|^2` (values `{0,1/12,1/3}`, row-stochastic, trace 4, rank
`10 = dim Sym(4,R)`) occurs ONLY at equal tickets `alpha=beta`; equal-tickets
is an ADDED magnitude law -- the conserved selector FCT-58 relocates, now at
the actualization layer. Positive additive counting sums probabilities and is
decohered (the destructive value 0 is unreachable; verified across all 576
two-path configurations); the retained `Z_2` sign scar (`z -> -z`) supplies
the `{+1,-1}` alphabet under which the three frames are three real
mutually-unbiased bases (transition entries `+-1/2`, cocycle
`U_ut.U_ts=U_us`, 768 real order-4 Hadamard matrices), restoring genuine real
coherent interference. Under the equal-ticket measure, for a linear-code phase
with dual distance `d_perp` every marginal on `<= d_perp-1` coordinates is
exactly uniform, so the first global binding arity is `d_perp` (hexacode 4;
`[24,12,4]` counterworld 4; Golay 8). Which phase exists is received; the FORM
of its statistics is forced; its Born MAGNITUDE (`alpha=beta`) and complex
PHASE are received.

Evidence:

- shipped: `verification/scripts/wcd_actualization.py` (the 48/96 split; the
  `(alpha,beta)` family and Born-only-at-`alpha=beta`; the 576-config
  decoherence; the three real MUBs, `+-1/2` cocycle, and 768 Hadamards; the
  two open gaps). Section C of `verification/scripts/forcing_audit.py` carries
  the equal-ticket kernel spectrum and the dual-distance marginals.

Checks / controls:

- the split, closed forms, kernel spectrum, cocycle, and Hadamard count are
  exact; Born-only-at-`alpha=beta` is proven algebraically and by
  counterexample; the marginal uniformity is exhaustive through and past the
  dual distance; the decoherence is exhaustive over all two-path configs.

Residuals:

- real-quantum scope (`Sym(4,R)`); does not derive the universal Born rule
  or the actuality of one outcome.
- TWO INDEPENDENT gaps held open: (a) the magnitude law `alpha=beta`
  (asymmetric tickets are equally lawful); (b) the enlargement from the binary
  `{+1,-1}` sign phase to a complex U(1) phase (`|1+i|^2=2` is unreachable
  with real signs).
- recovers: Delsarte's dual-distance = orthogonal-array-strength + 1; real
  MUBs; the count of order-4 Hadamard matrices; the Born-measure
  underdetermination Gleason's theorem also exhibits (form fixed, measure
  free).

## FCT-60 - Universal Accessible Positivity (The Theorem)

Status: `THEOREM / UNCONDITIONAL / EXTENSION-MATH`
Scope: the passive-linear-optical measurement grammar on one complex `3x3`
interferometer submatrix `A`, at the registered point `r = 3/5`.
Evidence state: `shipped`.

Public statement:

A three-contact class function whose sign-sector eigenvalue is negative
(`1 - 3r^2 - 2r^3 < 0` for `r > 1/2`) nonetheless keeps every physically
accessible probability nonnegative -- for EVERY interferometer. The accessible
amplitudes are the monomials `z_sigma = prod_i A[i][sigma(i)]`, which obey the
toric identity `prod_{even} z = prod_{odd} z`; and for every complex `3x3` `A`,
`152||z||^2 + 9|per A|^2 - 36|det A|^2 >= 0` (with `per A = sum z`,
`det A = sum sgn(sigma) z`), equality only at `A = 0`. The proof uses ONLY the
toric relation (no unitarity): a C_3-Fourier change of coordinates gives
`Q = 152N - 243M + 810 Re(alpha conj delta)` (with `alpha = (sum x)/3`,
`delta = (sum y)/3` the sector means and `M = |alpha|^2 + |delta|^2`), closed
by a cubic-deviation bound -- exactly the identity the shipped script verifies. Because the form is degree-6 homogeneous, positivity on all matrices is
equivalent to positivity on all contractions -- i.e. on every passive
linear-optical transfer matrix, LOSSLESS OR LOSSY. So the sector is a lawful
probabilistic model on the complete admitted grammar that lies OUTSIDE the
positive-semidefinite Hilbert Gram cone: global Hilbert PSD fails while
operational block positivity survives. On the lossless unitary core `U(3)` the
sharper strict margin `152||z||^2 + 9|per A|^2 - 36|det A|^2 >= (7/2)||z||^2`
holds (saturated by the Fourier tritter); the `(7/2)` margin for ARBITRARY
matrices is FALSE (`A=[[-4,-1,1],[-7,1,-3],[6,-3,-5]]` gives `-495`) and stays
killed. This is unconditional mathematics.

Evidence:

- shipped: `verification/scripts/negative_gram_holonomy.py` (the foundational
  Fourier identity `Q = 152N - 243M + 810 Re(alpha conj delta)` exact; the
  toric identity; POSITIVITY exact on thousands of rational toric witnesses;
  an INDEPENDENT adversarial descent over the full toric set finding no
  violation; the exact `U(3)` reduction identity and strict margin on rational
  Cayley unitaries, permutation matrices, and the Fourier tritter; the `-495`
  kill of the `(7/2)`-universal; exact separable-loss covariance).

Checks / controls:

- the foundational identity, the positivity on rational toric witnesses, the
  `U(3)` reduction/margin, the `-495` counterexample, and separable-loss
  covariance are all exact; the adversarial descent over the toric set bottoms
  out at `min Q/N ~ 3.26 > 0`.

Residuals:

- proves positivity for the DECLARED passive-linear-optical grammar (any
  transfer matrix / contraction); it does NOT prove positivity for arbitrary
  measurements outside that grammar.
- this is the model's internal consistency; the empirical mixed-state
  PSD-exclusion (whether a positive-Hilbert model fits the data) is a DIFFERENT
  and OPEN question -- see FCT-61.

## FCT-61 - Negative-Gram Identity Holonomy (The Prediction)

Status: `CONDITIONAL EXTENSION / EXPERIMENT-OPEN / BRIDGE-PREMISE-GATED`
Scope: a preregistered three-particle experiment on the received fiber
`(r, chi, receiver) = (3/5, -1, Fourier tritter)`. NOT a settled result.
Evidence state: `shipped` (the exact prediction numbers) + `held` (the
experiment).

Public statement:

The program's FIRST extension/divergence. On the odd `Z_2` identity-holonomy
sector (`Phi = pi`), the three-state Gram discriminant is
`Delta_3 = 1 - 3r^2 - 2r^3 = (1-2r)(1+r)^2 = -64/125 < 0`, where ordinary
complex-Hilbert quantum mechanics forces `Delta_3 = det G >= 0` (at `r = 3/5`
the PSD cone caps the cyclic phase at `Phi <= arccos(5/27) ~ 79.3 deg`, while
the extension reaches `Phi = pi`). The registered joint count vector
`(Delta_3, W, Q_3) = (-64/125, -128/1125, -12/125)` both excludes the PSD Gram
class and rejects the matched pairwise-only null (which predicts `Q_3 = 0`).
This is a PREREGISTERED BET with a frozen protocol (interleaved
pairwise-HOM/tritter/control acquisition, late randomization, blinded
PSD-constrained fit) and kill conditions, NOT a discovery. `r = 3/5` is a
received apparatus anchor, not a fundamental constant.

Evidence:

- shipped: `verification/scripts/negative_gram_holonomy.py` (all core numbers
  exact -- `Delta_3` and its factorization, the tritter probabilities and
  normalization, `D_2`, `W = (2/9)Delta_3`, `Q_3 = (4/9)b`, the null
  `Q_3 = 0`, and the PSD cap `cos Phi >= 5/27`).
- held: the nature-confrontation experiment (no suitable public dataset
  exists).

Bridge premises (NOT floor theorems; held open):

- that a physical particle class carries `Z_2` identity holonomy;
- that photons receive it (the natural home is anyonic/topological matter;
  photons are the clean-grammar long shot);
- that passive linear optics is the complete measurement grammar.

Residuals / the open crux:

- the CLEAN mixed-state + mode-mismatch PSD-exclusion is now CLOSED (proven):
  FCT-62 shows no partially-distinguishable Hilbert model, pure or mixed,
  reproduces the registered `(Delta_3, W, Q_3)`. The remaining open part of the
  crux is the APPARATUS-NUISANCE layer -- multiphoton contamination, detector
  response, transfer-matrix uncertainty, source drift -- which has NOT been
  vetted by a quantum-optics expert; external expert review is the gate before
  any outreach.
- this DOI is an archival/priority record of a mathematically closed
  conditional prediction, NOT an empirical discovery.
- Chapter 12 (FCT-63) upgrades this: the negative-Gram inequality is THE
  complete boundary of quantum three-photon tritter statistics, and this
  protocol simplifies to tritter counts alone.

## FCT-62 - The Clean Mixed-State Exclusion (The Theorem)

Status: `THEOREM / UNCONDITIONAL / EXTENSION-MATH`
Scope: the partially-distinguishable Hilbert model (pure and mixed) at the
Fourier tritter, at the registered anchor `r = 3/5`.
Evidence state: `shipped`.

Public statement:

Closes the CLEAN core of the Chapter-10 crux. For every partial-
distinguishability model, the gauge-free raw-count witness
`W = P111 + D2 - 2/3` satisfies `W = (2/9) det G`, where `G` is the `3x3`
internal-state Gram matrix (`P111 = (2 - S + 4tau)/9`, `D2 = 2/3 - S/9`,
`S = sum |g_ij|^2`, `tau = Re(g12 g23 g31)`, `det G = 1 - S + 2tau`; the count
formula is derived from first principles in the verifier). Gram positivity
forces `det G >= 0`, hence `W >= 0` for every pure config, hence -- because `W`
is affine in the counts -- for every MIXED state (convexity), certified
operator-level by the antisymmetrizer `A_- = (1/6) sum sgn(pi) P_pi` (a positive
projector, `A_-^2 = A_-`) via `W = (4/3) Tr(A_- Omega) >= 0`. The registered
extension has `W = (2/9) Delta_3 = -128/1125 < 0`, so it lies OUTSIDE the entire
class; the exact separating raw-count test is `P111 + D2 >= 2/3` (the extension
gives `622/1125 < 750/1125`, violation `128/1125`). So no partially-
distinguishable Hilbert model, pure or mixed, reproduces the registered count
vector. Unconditional mathematics.

Evidence:

- shipped: `verification/scripts/mixed_state_exclusion.py` (the first-principles
  `P111` formula, exact on a rational witness and numerical over random models;
  `det G = 1 - S + 2tau`; the count-witness identity `W = (2/9) det G`; the
  antisymmetrizer certificate `<A_-> = det G/6` with the `Q[S_3]` projector
  property; `W >= 0` on exact rational PSD Grams and under an adversarial
  descent; convexity for mixtures; the exclusion and raw-count test).

Checks / controls:

- all load-bearing steps are exact (`fractions.Fraction` over `Q(sqrt 3)`); the
  count formula is verified from first principles, not asserted.

Residuals:

- closes the CLEAN mixed-state + mode-mismatch exclusion ONLY. Multiphoton
  contamination, detector response, transfer-matrix uncertainty, and source
  drift remain the external experimental layer (FCT-61); the Chapter-10 bridge
  premises are unchanged.

## FCT-63 - n=3 Count Region: The Negative-Gram Boundary Is Complete

Status: `THEOREM / UNCONDITIONAL / QUANTUM-INFO`
Scope: three indistinguishable bosons in the 3-mode Fourier interferometer.
Evidence state: `shipped`.

Public statement:

The exact achievable region of count statistics is the tetrahedron
`{B, R, L >= 0, C - B/2 >= 0}` in cyclic-orbit coordinates (`B` = full bunching
`P300+P030+P003`, `C = P111`). The sign inequality `C >= B/2` is the ONLY facet
not inherited from raw probability nonnegativity, and it is the negative-Gram
boundary: `P111 + D2 - 2/3 = (4/3)(C - B/2) = (2/9) det G`, with
`D2 = 2/3 + C/3 - 2B/3` redundant with the tritter counts. So Chapter 11's
`P111 + D2 >= 2/3` (FCT-62) is THE complete boundary of quantum three-photon
tritter statistics, and the FCT-61 protocol simplifies to tritter counts alone.
Four vertices; `W_min = -2/3`; the registered Ch10 point sits at 17% (= 64/375)
of maximal depth. Verified from first principles (bosonic occupation
probabilities) exactly on a rational witness.

Evidence: shipped, `verification/scripts/count_regions.py` (Section A).

Residuals: standalone quantum-information mathematics; upgrades FCT-61/FCT-62.

## FCT-64 - n=4 Count Region: Central Positivity Is Insufficient

Status: `THEOREM / UNCONDITIONAL / QUANTUM-INFO`
Scope: four indistinguishable bosons in the 4-mode Fourier interferometer.
Evidence state: `shipped`.

Public statement:

The region is a 9-simplex whose complete facet description is ten
primitive-projector positivities; only the trivial and sign central irreps give
facets. Central-projector positivity is INSUFFICIENT: the exact counterexample
`x = (3/80,1/20,1/10,1/20,1/20,3/10,1/5,1/16,0,3/20)` has all five central
weights `(1/10,3/10,1/5,3/10,1/10) >= 0` yet `alpha_211A = x9 - x2 = -1/20 < 0`.
Six new nontrivial raw-count inequalities every quantum model obeys: `x9 >= x2`,
`x6 >= x4`, `x7 >= x8 + x1`, `3 x10 >= x3 + x5`, `2 x3 >= x5`, `3 x8 >= x1`.

Evidence: shipped, `verification/scripts/count_regions.py` (Section B).

Residuals: standalone quantum-information mathematics.

## FCT-65 - n=5: The Emergent Qubit and Rebit-Blindness

Status: `THEOREM / UNCONDITIONAL / QUANTUM-INFO`
Scope: five indistinguishable bosons in the 5-mode Fourier interferometer.
Evidence state: `shipped`.

Public statement:

The cyclic-shift commutant has dimension 4, 10, 28 for n = 3, 4, 5; at n = 5 the
irrep `(3,1,1)` gains multiplicity two, so the commutant becomes
`C^24 (+) M_2(C)` -- an emergent logical qubit from permutation symmetry and
counting alone. REBIT-BLINDNESS THEOREM: no linear functional of single-shot
cyclic counts exposes `sigma_y` (the single-shot fiber operator system is
exactly `{I, sigma_x, sigma_z}`, dim 3); multiplication recovers it
(`[F00131,F00212]|M2 = -(2i/25) sigma_y`, `P_M2 = (625/4)[A,B]^dag[A,B] = I`).
The exact body `K_5` has affine dimension 25; the coherence cross-section is a
DISK (not a ball); hidden-center identity
`w0 = (5/6) v_(3,2),0 + (1/6) v_(1^5),0`.

Evidence: shipped, `verification/scripts/count_regions.py` (Section C).

Residuals: standalone quantum-information mathematics.

## FCT-66 - The Single-Source R/C/H Counting No-Go

Status: `THEOREM / UNCONDITIONAL / QUANTUM-INFO`
Scope: single-source Fourier counting; real / complex / quaternionic internal
cones.
Evidence state: `shipped`.

Public statement:

`K_R = K_C = K_H` exactly: every count effect is real-symmetric on the fiber
(no `sigma_y` component), so complex/quaternionic off-diagonals contribute only
their real part (affine dimension 25 in all three; Hausdorff distance zero;
`sigma_min ~ 0.128764`). Single-source Fourier counting provably CANNOT
discriminate real, complex, and quaternionic quantum mechanics -- retroactively
explaining why the Renou-et-al. real-QM test required networks.

Evidence: shipped, `verification/scripts/count_regions.py` (Section D).

Residuals: standalone quantum-information mathematics.

## FCT-67 - Sequential Closure and the Conjugation-Witness Experiment

Status: `THEOREM (closure) / UNCONDITIONAL` + `REGISTERED / EXPERIMENT-OPEN`
Scope: general passive networks on five modes; a registered single-source
protocol.
Evidence state: `shipped` (the exact G1 invariants and gap) + `held` (the
experiment).

Public statement:

Rebit-blindness is a property of the cyclic-Fourier POVM, not of bosonic
counting: general passive networks span all of `C[S_5]` (exact rank 120). One
network (`B^R_03, B^R_13, B^i_01, B^i_14`, detect `s=(0,3,1,1,0)`) exposes
`sigma_y`:
`G1 = (21/1024)I - (1/256) sigma_x - (5 sqrt2/512) sigma_y - (15/1024) sigma_z`,
trace `21/512`, rank 1, `|y| = 5 sqrt2/512`. REGISTERED EXPERIMENT: a complex
preparation `rho+ = (I + sigma_y)/2` breaks the conjugation law with exact gap
`|p - p_bar| = 5 sqrt2/256 ~ 0.0276`; one source, two conjugate settings,
~1304 trials/setting at 5 sigma.

SCOPE FENCE (mandatory): this excludes the real-internal-states +
mode-only-passive-optics model class. It does NOT falsify every real
reformulation of QM: the McKague-Mosca-Gisin realification restores simulation
IF optical transformations may act on a universal rebit -- which is NOT
implementable in the fixed grammar (mode unitaries generate only the permutation
actions `P_pi`, never `J . P_pi`). Cite arXiv:0810.1923 (McKague et al.);
arXiv:2101.10873 (Renou et al.); broader operational real formulations exist
that reproduce arbitrary sequential protocols.

Evidence: shipped, `verification/scripts/count_regions.py` (Section E) + held
(the experiment).

Residuals: the witness is a REGISTERED, EXPERIMENT-OPEN protocol with a named
target model class -- NOT a performed experiment and NOT a claim about nature.

## Held Claim Families

The following are not public-load-bearing in this register:

- general almost-quantum selector theorem;
- q >= 3, more-outcome, non-projective, and cross-site-interlocking
  extensions;
- CHSH weight selection;
- terminalized one-receiver carrier/interface mechanisms beyond their model
  scope;
- claims that the floor derives all quantum theory.

See `hold-register.md`.

## FCT-68 - The Pincer: Exactly Three Identity Modes

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: involutive identity modes of a ternary contact, given the named
inputs (S) ternary arity (measured) and (R) readability (received).
Evidence state: `shipped`.

Public statement:

A future-readable order scar forces a third anticommuting involutive mode:
the reader condition `XYX = -Y` is exactly anticommutation (a linear-map
identity, `X^2 = I`), and the two-mode algebra `M_2(R) = Cl(2,0)` hosts no
such involution -- its anticommutant of `{X, Z}` is exactly `span{XZ}`,
whose squares are negative. The carrier-parity model caps the family at
`f(n) =` largest odd `<= n` (the reverse-oddtown bound), and at `n = 3` the
maximum family is unique: the three pair-channels. Exactly three mutually
anticommuting involutive identity modes per ternary contact.

Evidence:

- shipped: `verification/scripts/floor_to_interface.py` sections 1a-1b
  (anticommutant nullspace, integer `M_4(R)` triple, exhaustive ladder
  `n = 2..7`, uniqueness at `n = 3`).

Checks / controls:

- the reader-iff-anticommutation equivalence checked on a full `M_2` basis;
  the anticommutant computed as an exact nullspace; the ladder exhaustive.

Residuals:

- (R) is the program's standing received fork (proven not-forced; private
  corpus, cited); the chain is conditional on it by construction;
- the upper bound is Babai-Frankl Ex. 1.1.5 (reverse oddtown) and the
  anticommuting-family question is standard (Sarkar-van den Berg 2021;
  Hurwitz-Radon) -- recovery, cited in the chapter.

## FCT-69 - Central i, Quaternion Even Sector, the 1/2, the Plateau

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: the real algebra generated by three anticommuting involutions.
Evidence state: `shipped`.

Public statement:

Three anticommuting involutions generate `Cl(3,0)`: the pseudoscalar
`Omega = ABC` is central with `Omega^2 = -I` (the complex unit as the
oriented volume element of the three identity directions); the even sector
satisfies the quaternion relations with exact dimension 4; an anticommuting
partner swaps a mode's eigenprojectors, so any reflection-invariant state
assigns exactly `1/2` to each outcome, prior to any Born postulate; and the
central square root of `-1` exists iff `m = 3 (mod 4)`, so with `m = f(n)`
the phase-stable arity window is exactly `{3, 4}` -- where the floor's
measured saturation sits.

Evidence:

- shipped: `verification/scripts/floor_to_interface.py` section 2.

Checks / controls:

- all identities in exact integer/rational matrices; the even-sector
  dimension by exact rank; the plateau by the mod-4 sign formula.

Residuals:

- the plateau upgrades (S) from measured to internal ONLY under the
  additional premise "retention requires central phase" -- stated as a
  conditional, not claimed;
- all ingredients standard (Clifford periodicity; the mutually-unbiased
  swap lemma) -- recovery, cited in the chapter.

## FCT-70 - Retention Selects the Double Cover (Given Self-Hosting)

Status: `THEOREM / MODEL-SCOPE / RECOVERY` (conditional on the named
axiom (SH))
Scope: central `C_2` extensions of the four-mark chiral contact symmetry.
Evidence state: `shipped`.

Public statement:

`H^2(C_3;C_2) = 0` (a triangular contact cannot host the central lift;
capability begins at four marks, ternary arity); `H^2(A_4;C_2) = C_2` with
both classes witnessed: `2T = SL(2,3)` has a unique involution (nonsplit)
and every `V_4` preimage squares to the central `z` -- a repeated
involutive use is retained -- while in the split twin `A_4 x C_2` every
involution lift squares to the identity -- a repeated use is invisible
without an external ledger. GIVEN (SH), internal retention, the split twin
dies and `2T` is forced; without (SH) the projective class is available,
not forced. Representing `z -> -I_4` gives real quarter-turns `J^2 = -I_4`
with the full anticommuting triple, no complex numbers presupposed.

Evidence:

- shipped: `verification/scripts/floor_to_interface.py` section 3
  (cocycle census, exact `SL(2,3)` and `A_4 x C_2` constructions,
  presentation-gauge orbits).

Checks / controls:

- both extension classes constructed and distinguished by the
  involution-square invariant; the census exhaustive.

Residuals:

- (SH) is a named axiom (ledger-regress defense), not a theorem -- the one
  genuine assertion in the chain, stated in the chapter and here;
- the restriction to the chiral group `A_4` (from `S_4`) is the
  orientation input (C), a received `C_2` torsor;
- Schur multipliers and the binary tetrahedral double cover are standard
  (Conway-Smith) -- recovery, cited.

## FCT-71 - The Minimal Receiver Is H; the Handoff to Chapter 8

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: faithful real representations of the retained residue `z -> -1`.
Evidence state: `shipped`.

Public statement:

The minimal faithful real receiver representing the retained residue as
`-1` has dimension exactly 4: dimension 1 dies by arithmetic, dimension 2
by the Frobenius-Schur indicator `-1` (quaternionic type), dimension 3 by
the derived-subgroup determinant; the 4-dimensional carrier has commutant
exactly the quaternions (a division algebra, hence irreducible), and the
invariant symmetric form is unique up to scale (`I_4`) -- the Euclidean
`1+3` split, one return-even direction plus three anticommuting
quarter-turns, with spinor return `J^2 = z`, `J^4 = 1`. This object is
verbatim the starting datum of Chapter 8 (FCT-45): the starting-datum
residual disclosed at v0.13.0 is closed one layer down, conditional on the
four named received inputs.

Evidence:

- shipped: `verification/scripts/floor_to_interface.py` sections 4-5.

Checks / controls:

- the Frobenius-Schur sum, commutant nullspace, invariant-form nullspace,
  and division samples all exact; the dimension kills each by a separate
  argument.

Residuals:

- an ALGEBRA grading `1+3`, not a spacetime metric (the fence holds);
- the quaternionic-representation mechanism is standard (Serre 13.2; Baez
  2012 threefold way) -- recovery; the forced-composition claim is graded
  INTERNAL-BLIND in the chapter's related-work section, where the
  Moretti-Oppio reduction is engaged by name.

## FCT-72 - The Capability Axes on Presented Observed Floors

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: presented observed floors (state space + act monoid + designated
content map and act equations); framing graded INTERNAL-BLIND.
Evidence state: `shipped`.

Public statement:

Order-writability = noncommutativity of the realized transformation image.
Residue-retention lives in the specification/realization gap: RR holds iff
the history action fails to factor through the formal content
specification -- it cannot exist in a bare act system (a genuine action
satisfies its monoid's equations), and its minimal realization is a
formally-idempotent operation realized as a toggle on two fixed states
(retention needs no growth). The axes are independent (all four
combinations at |state| <= 2) and incomplete (unbounded orbit growth is a
third independent axis; growth-rate refinements continue).

Evidence:

- shipped: `verification/scripts/atlas_of_floors.py` section 1.

Checks / controls:

- minimal examples exact; independence exhibited; the presentation-
  relativity is stated as scope, with the designating structures named.

Residuals:

- the constituent mathematics is standard (transformation monoids;
  factorization criteria); the capability lens on generative floors is
  the contribution, INTERNAL-BLIND.

## FCT-73 - The Four Cells, Fenced; Quotient Monotonicity In Safe Scope

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: exemplar floors per cell; terminal semantics where stated.
Evidence state: `shipped` (+ `cited` boundary layer).

Public statement:

Lattice floors: meet-acts with a justification-stable propagation closure
have order-independent settled states (the fence is shipped: two
noncommuting interior operators write order -- the theorem is
terminal-scope and the stability condition is necessary); coagulation is
orderless by the same lattice mechanism, exhaustively. Reversible floors:
order-writable iff the image is noncommutative; never residue-retaining
against full-state equations. Enriched counters: retention without order.
Marks (Chapter 13): both. Equivariant quotients with fixed equation
designations only destroy capabilities; 11->01 and 11->10 realized; a
quotient can CREATE an equation designation (parity example), so
residue-monotonicity requires a fixed presentation.

Evidence:

- shipped: `atlas_of_floors.py` sections 2-3;
- cited: the boundary-layer measurements (priced reversal as the
  infinite-budget limit; record marginalization as classicality) from the
  research corpus.

Checks / controls:

- 300 random constraint systems, staged vs joint closes exact; the
  interior-operator counterexample included; exhaustive merge-order
  census; both quotient transitions constructed.

Residuals:

- the boundary layer is cited, not shipped;
- confluence scope per Newman's lemma; trace-monoid refinements exist and
  are cited in the chapter.

## FCT-74 - The CAR Kernel Of The Retained Contact

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: the Cl(3,0) cell of Chapter 13.
Evidence state: `shipped`.

Public statement:

f = (A + Omega B)/2 and f# = (A - Omega B)/2 satisfy f^2 = f#^2 = 0 and
f f# + f# f = 1 exactly: one complex fermionic mode per retained-contact
cell, with the central i as its complex structure. A CAR-capable
substrate is forced; fermionic statistics additionally require received
composition assumptions (polarization, adjoint, parity, vacuum, graded
multi-contact composition -- the last blocked from being free by the
Chapter-9 forcing audit).

Evidence:

- shipped: `atlas_of_floors.py` section 4.

Checks / controls:

- exact integer/rational matrices; centrality and Omega^2 = -I verified.

Residuals:

- statistics-family addresses (hard-core vs Fermi; q-ons; braid) verified
  in the research corpus, summarized in the chapter, cited.

## FCT-75 - The Retention Tower Through Third Order

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: the four-mark chiral contact group; trivial C_2 coefficients.
Evidence state: `shipped`.

Public statement:

Exact F_2 censuses, recomputed inside the shipped script: H^2 = C_2 (the
unique first-order scar -- Chapter 13's double cover), H^3 = C_2 x C_2
(four second-order classes, all pentagon-coherent, none gauge-removable),
H^4 = C_2 (exactly one third-order class). The bracket witness
omega(g,g,g) -- the parenthesization defect of a triple self-contact of an
involutive act -- is gauge-invariant and reads a nontrivial second-order
class. Manufactured pentagon defects are always removable
(free-asphericity): genuine third-order structure must realize the unique
H^4 class.

Evidence:

- shipped: `atlas_of_floors.py` section 5.

Checks / controls:

- full coboundary ranks over F_2; gauge invariance checked algebraically
  and under random gauges; representatives constructed and tested.

Residuals:

- H^3 classes are within-quantum fusion data (Dijkgraaf-Witten twists,
  cited); the beyond-quantum question is exactly the H^4 realization
  question, held open;
- the involution-silent second-order class needs a mixed-cycle witness
  (open).

## FCT-76 - The Relocated Measurement Obstruction

Status: `THEOREM / MODEL-SCOPE / RECOVERY` (structural; the nature-facing
echo is an open question by name)
Scope: the double cover E over its law quotient L (24 elements).
Evidence state: `shipped`.

Public statement:

No equivariant section L -> E exists (exhaustive over all 4096
lift-choices); every q-factoring observable is scar-blind while the double
action of any involutive law element flips the retained bit exactly (the
tomographically silent echo); the best of all law-only prediction rules
for the retained bit is exact chance, and promoting the read bit into the
law-state gives certainty. A law-defined outcome selector would be an
equivariant section: the obstruction is the program's central no-selector
law. Reading, at model scope: a reversible law over a retaining history
space is complete as law and incomplete as history.

Evidence:

- shipped: `atlas_of_floors.py` section 6.

Checks / controls:

- exhaustive section search; echo and blindness exact; the prediction
  game exhaustive over rules.

Residuals:

- whether any physical scar reader could separate what state-complete
  tomography calls identical is an OPEN nature-facing question with heavy
  named controls (roadmap); nothing here claims it.

## FCT-77 - The Boundary Layer (cited)

Status: `MEASURED / MODEL-SCOPE`
Scope: the research corpus's floor engines.
Evidence state: `cited`.

Public statement:

A priced-reversal floor is scar-free exactly when its cost never binds:
the reversible cell is the infinite-budget limit of a scarred floor, with
scar readability decaying past the horizon (never to zero at finite
budget). Marginalizing a creating floor's record sector returns its
statistics to the scar-free cells; the order-reading capability appears at
exactly two retained records.

Evidence:

- cited: the research corpus (engines, outputs, and the frozen campaign
  design named there).

Checks / controls:

- carried in the corpus; not shipped here.

Residuals:

- a shipped, self-contained boundary-layer script is a candidate for a
  future release.

## FCT-78 - The Ladder: One Witness-Readable Obstruction Tower

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: the four-mark contact group A_4 and its double cover; bar-complex
cohomology over C_2; classical dimensions and ring facts cited, not claimed.
Evidence state: `shipped`.

Public statement:

The retention class powers the whole tower: the unique H^4(A_4;C_2) class
is the retention class cup-squared (the extension cocycle alpha of 2T is
built explicitly, alpha(g,g) = 1 on every involution; alpha cup alpha is a
4-cocycle and not a coboundary), and it is read gauge-invariantly, value 1,
by a quadruple involutive self-contact. The involution-silent H^3 class is
read by an explicit 25-word bar-cycle found by F_2 duality — a
class-separating protocol. Every rung from H^1 to H^4 has an explicit,
shipped, gauge-invariant witness.

Evidence:

- shipped: `verification/scripts/ladder_of_scars.py` §A (15-check suite).

Checks / controls:

- normalized-cochain gauge invariance verified algebraically and against
  random gauges; non-coboundary certified by reduction against all 1331
  coboundary generators; cycle witness verified against all 121 coboundary
  pairings.

Residuals:

- the cohomology dimensions and cup-square ring structure are classical
  (Adem–Milgram; Jena database) — labeled RECOVERY; the witness suite is
  the composition claim per the release's blind sweep.

## FCT-79 - The Time Link and the Arrow Below the Tower

Status: `THEOREM / MODEL-SCOPE`
Scope: finite covers/extensions and act monoids as in the shipped script;
the torsor pattern itself is classical (cited).
Evidence state: `shipped`.

Public statement:

The no-global-now of Chapter 5 (Cech H^1 loop witness on the Möbius
3-patch cover: gauge-invariant under all re-trivializations; twisted reads
1 with 0 global sections, untwisted reads 0 with 2) and the
no-outcome-selector of Chapters 13–14 (no multiplicative section of the 2T
extension: 0 of 4096, exhaustive) are adjacent instances of one
torsor-obstruction pattern. Below the whole tower: the arrow of time is
act-monoid non-invertibility — a strict intrinsic arrow exists on a
provably retention-free monotone floor, while reversible floors admit no
strict monotone invariant at all. The arrow is a capability strictly below
memory.

Evidence:

- shipped: `verification/scripts/ladder_of_scars.py` §B.

Checks / controls:

- all 8 re-trivializations; exhaustive 4096-section search; exhaustive
  per-step arrow check and the all-acts-return cycle argument.

Residuals:

- the torsor pattern is classical nonabelian cohomology (Giraud) — labeled
  RECOVERY; the arrow-below-memory separation was found in no literature
  by the blind sweep and is fenced to its act-monoid formalization.

## FCT-80 - The Second Storey: Coherence Memory Without Event Memory

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: exact bar-complex censuses on 2T = SL(2,3) and Q_8; dimensions
classical (period-4 cohomology; cited).
Evidence state: `shipped`.

Public statement:

The cover is first-order-scar-free: H^1(2T;C_2) = H^2(2T;C_2) = 0 —
retention is complete after one repair. Exactly one second-order scar
survives (dim H^3(2T;C_2) = 1): a coherence scar on a structure that
provably cannot carry a first-order retention bit. The registered diagonal
guess (the unique involution reads its own bracket scar, omega(z,z,z) = 1)
MISSED — the class is silent there, gauge-invariantly — and is scored as a
miss; the real witness is a 4-word composite bar-cycle on Q_8, where
restriction is injective at odd index. Reading the record of the record is
intrinsically relational.

Evidence:

- shipped: `verification/scripts/ladder_of_scars.py` §C.

Checks / controls:

- full rank computations (rank delta_2 = 506/529, rank delta_3 =
  11660/12167); Q_8 census with nullspace representatives; 20-gauge
  stability of the silence; duality-method witness verified as a cycle
  pairing 1.

Residuals:

- the registered miss stays on the record (the program scores its
  predictions); the dimensions are classical — the witness protocol and
  the miss are the shipped content.

## FCT-81 - Boundary Pinning and the Carrier Grade

Status: `THEOREM / MODEL-SCOPE`
Scope: exact rational arithmetic in the 2-dim irreps of 2T and real
Z_3-orbit carriers; the pinned configuration is the classical trine frame
(cited); consequences for the Chapter-10 protocol are program-specific.
Evidence state: `shipped`.

Public statement:

In every 2-dimensional twisted sector of 2T (all its twisted irreps are
2-dimensional), odd-class (Bargmann phase pi) orbit triples are pinned at
overlap r = 1/2 exactly: every order-6 lift satisfies g + g^{-1} = I, the
Bargmann invariant B = -c^3 is invariant under unimodular character
twists, and det G = (1-2r)(1+r)^2 = Delta_3(r) = 0 forces r = 1/2. Real
(H^1-grade) carriers realize the odd class floppily on r in (0, 1/2] with
the same hard cap. Consequences: the r = 1/2 saturation of current
photonic experiments is structural; Chapter 10's registered r = 3/5 bet
sits exactly one contact beyond every Hilbert carrier; overlap rigidity
under preparation detuning is a count-level carrier-grade witness; at the
pinned point the 3-window Gram is grade-blind and the grade is read by the
fourth contact (orbit closure -1 twisted vs +1 real). This row supersedes
Chapter 10's "natural home is anyonic/topological matter" remark (a
commissioned adversarial review found anyonic fusion holonomy is
transport-layer path data, not an indefinite-Gram source; the carrier
requirement is a preparation-side superselected grade), and upgrades the
FCT-61 protocol with the sharpened joint signature (pure P111 collapse to
7/1125, D_2 invariant 41/75, ~2328 triples for 5 sigma) and the pinning
sub-signature.

Evidence:

- shipped: `verification/scripts/ladder_of_scars.py` §D.

Checks / controls:

- exact operator identities on all order-6 elements; character-twist
  invariance with two exact unimodular twists; four exact rational floppy
  anchors; the -1/2 floor on 300 random exact vectors; grade-blindness
  verified at the pinned point.

Residuals:

- whether any physical system realizes either grade is open by name (the
  bridge premises of Chapter 10 are unchanged); the pinning is elementary
  given classical Bargmann/Gram facts (blind-sweep verdict KNOWN-VARIANT)
  and is claimed as packaging plus consequences.

## FCT-82 - The Exchange Rate of Irreversibility

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: finite sets and functions; exhaustive/exact; recovery components
cited (Lecerf 1963; Bennett 1973; Maslov–Dueck 2003/04).
Evidence state: `shipped`.

Public statement:

A non-injective act on a finite state set admits no lift to a permutation
of S x A with first component f on the whole product, for any fixed finite
ancilla (counting); slice-correct lifts exist but fail under composition.
One fresh one-use register per step dilates every act monoid reversibly
and compositionally. The minimal fresh-register alphabet for an exact
injective lift is exactly maxfiber(f) — the alphabet form of the
Maslov–Dueck minimal-garbage formula, a recovery this chapter registers
explicitly (its own open question, answered by the blind sweep with a
citation). The n-fork collapse act therefore prices at exactly log2 n
fresh bits. The price is submultiplicative under composition; permutation
acts keep price 1 under respected quotients, while an absorbing-state
mortality notion is quotient-discontinuous — the act-price sees through
it.

Evidence:

- shipped: `verification/scripts/debt_ledger.py` §1.

Checks / controls:

- counting certificates over all non-injective acts at |S| <= 4 plus brute
  search at |S| = 2; 30 random mortal monoids x 5-step words with global
  injectivity; exhaustive separator search at |S| <= 5; 400 composition
  cases with strictness; 200 cycle-quotients; the absorbing-state example
  verified with an invariant price vector.

Residuals:

- every component is classical and cited; the row exists because the
  program's selector-debt family reuses this exact number (see FCT-84).

## FCT-83 - The Ledger Inequality: Waste Possible, Theft Impossible

Status: `THEOREM / MODEL-SCOPE / RECOVERY-VARIANT`
Scope: finite sets and functions; the entropy/support forms are the
classical grouping bound (cited); the composed ledger form is packaging.
Evidence state: `shipped`.

Public statement:

For any history w = f_k o ... o f_1 on a finite state set,
log2|S| <= log2|im w(S)| + sum_k log2 maxfiber(f_k), exactly — verified
exhaustively on small cases and on random longer histories, with equality
achieved by aligned uniform merges and strict waste exhibited (a merge
re-paying for distinction already destroyed). Distinction is never
destroyed unpaid: waste is possible, theft is impossible. The support form
is exact and exhaustive; the Shannon form H(f(X)) >= H(X) - log2 maxfiber
is the classical grouping bound (float-checked, labeled).

Evidence:

- shipped: `verification/scripts/debt_ledger.py` §2.

Checks / controls:

- all 729 two-act histories at |S| = 3 (72 tight); 300 random histories at
  |S| <= 7; explicit tightness and waste witnesses; all 256 acts x 15
  supports at |S| = 4 for the support form; 500 random distributions for
  the labeled Shannon form.

Residuals:

- nearest prior refs recorded (Cicalese–Gargano–Vaccaro 2018 for the
  entropy face; Bowen 1971 for the dynamical analogue); claimed as
  packaging only.

## FCT-84 - The Refused Debt: Selection Priced by Counting Alone

Status: `THEOREM / MODEL-SCOPE`
Scope: finite fork monoids, exhaustive; pre-probabilistic (no probability
measure, no thermodynamic postulate); nearest thermodynamic prior art
cited and distinguished.
Evidence state: `shipped`.

Public statement:

For the n-fork, three independently computed quantities are one number:
the dilation register alphabet of the collapse act, the record alphabet
needed for registration-promotion certainty (Chapter 14's promotion,
priced), and the optimal binary-question identification depth — n in
alphabet form, ceil(log2 n) in binary form (n = 2..16 verified; the ceil
marks exactly the binary-resource convention). Exhaustively, every act
resolving an n-fork to one actual pays maxfiber = n, and partial
resolution to k images pays at least ceil(n/k). The no-selector law is a
refused debt: lawlike (injective) evolution is exactly the debt-free
sector, and any completion that selects an outcome books exactly log2 n.
The recurring ceil(log2 n) across the program — selector debt,
actualization debt, promotion records, garbage registers — is one
conserved ledger entry. Nearest prior art: Cabello, Gu, Gühne, Larsson,
Wiesner, Phys. Rev. A 94, 052127 (2016) reach the qualitative dichotomy
thermodynamically (probabilities + Landauer + finite memory); this
derivation needs none of the three — the composition claim of the chapter
per the release's blind sweep.

Evidence:

- shipped: `verification/scripts/debt_ledger.py` §2.

Checks / controls:

- triality rows n = 2..16 computed by three independent routes;
  exhaustive resolution-price census for n = 2, 3, 4.

Residuals:

- whether nature's measured dissipation realizes this price is open by
  name; the chapter prices selection, it does not perform it.

## FCT-85 - The Price Field on the Atlas

Status: `THEOREM / MODEL-SCOPE`
Scope: the atlas cells of Chapter 14, exact small models; the 2T/A_4
relocation model of Chapter 14 (24 elements).
Evidence state: `shipped`.

Public statement:

Ledger flow types the atlas cells: lattice settlement is a PAID quotient
(confluence is bought; max basin 25/64 in the shipped model) and
coagulation pays its whole state space; reversible floors are FLAT
(permutations neither pay nor mint); creation floors run in CREDIT
(injective acts mint distinction free); and the marks cell is MIXED — on
the relocation model, contact acts are permutations (history written
free) while the law quotient has maxfiber exactly 2, uniformly: the
price of lawfulness is exactly one bit, the retained central bit, and
Chapter 14's chance-to-certainty registration promotion is a 1-bit
ledger transaction (minimal record alphabet = 2 = maxfiber). The marks
cell is the only floor whose ledger balances internally.

Evidence:

- shipped: `verification/scripts/mint_and_bill.py` §A.

Checks / controls:

- exhaustive small models per cell; exact rational arithmetic on the
  24-element relocation model.

Residuals:

- the flow labels are model-scope; nature is not claimed to realize any
  cell (Chapter 14's fence carries over).

## FCT-86 - The Funding Identity: Forced Fork, Mint = Bill

Status: `THEOREM / MODEL-SCOPE`
Scope: one-use floors (static symmetric tolerance, pair-consuming
contacts), exhaustive smalls; lemma and identity components cited.
Evidence state: `shipped`.

Public statement:

All minted distinction is consumption-choice distinction (the mint is
faithful; every contact step injective). THE FORCED FORK: deterministic
multi-step one-use floors do not exist — any floor admitting a two-step
history has at least two admissible choices at step one (exhaustive,
4752 floors); the lemma is an instance of the persistence/diamond
property of consume-only transition systems (Winskel;
Nielsen-Plotkin-Winskel; conflict-free nets — cited), and its use here
demotes the fork-staging half of the program's central law from axiom
to theorem at model scope, leaving the no-selector half as the named
irreducible postulate. MINT = BILL: the admissible orderings of each
reachable content class equal exactly the fiber of the order-forgetting
quotient (Mazurkiewicz/Cartier-Foata linearization counting; the query
side is Kahn-Kim sorting under partial information — cited); the
accounting reading (credit minted forward = debt paid backward, class
by class) is commentary on the known identity.

Evidence:

- shipped: `verification/scripts/mint_and_bill.py` §B.

Checks / controls:

- exhaustive floor censuses; exact ordering/fiber counts per class.

Residuals:

- the forced-fork scope is static tolerances; state-dependent
  admissibility is out of scope here.

## FCT-87 - The Cut Flow-Typed; the Debt Calculus Unified

Status: `THEOREM / MODEL-SCOPE`
Scope: exact small floors per atlas cell; Chapter 3's calculus in its
own vocabulary; the flow-typing claim adjudicated PLAUSIBLY-NEW by the
release's blind sweep (two-part-code frame cited).
Evidence state: `shipped`.

Public statement:

The law/present identification asymmetry (Chapter 3's Cut) is derived
and delimited: the present is expensive because it is MINTED (exact
reachable counts grow geometrically; identification cost = the log of
the mint), and the asymmetry is FLOW-TYPED — paid cells erase their own
present at their terminals (settlement collapses 64 to 16 candidates;
coagulation's terminal costs zero), reversible cells never grow one
(orbits bounded by the group order). The Cut is the signature of the
mint. On the rival floors, Chapter 3's theorems instantiate in their
own vocabulary: content descriptions satisfy the waist everywhere and
order bits are FUTURE-INERT for contact protocols — re-deriving
Chapter 13's readability-of-order axiom as necessary; the T-19 receipt
bound equals the ledger price of the floor's own choice-to-outcome map,
cell by cell (coagulation full debt, reversible zero, marks-content the
order bit); future-completeness holds exactly when the mint is closed.

Evidence:

- shipped: `verification/scripts/mint_and_bill.py` §C-D.

Checks / controls:

- exact reachable counts; exhaustive future-tree comparisons; the
  law-side O(1) at model scope is stipulative (declared rule family) —
  Chapter 3's U-statistic theorem is the published law-side result.

Residuals:

- the trichotomy's necessity/sufficiency is established at the shipped
  model scope; broader dynamical classes are open by name.

## FCT-88 - The Three-Column Cut: Constants as Registrations

Status: `THEOREM / MODEL-SCOPE`
Scope: rule families exchanged by a relabeling symmetry, exact smalls;
the operational definition adjudicated PLAUSIBLY-NEW by the blind
sweep; informal ancestors cited (Gell-Mann frozen accidents; Wheeler;
Smolin; Weinberg; Duff; Mueller 2018 checked as nearest).
Evidence state: `shipped`.

Public statement:

A CONSTANT is a fork-registration that the law action inherits: (a)
underived — no equivariant selector exists at the level of rules; (b)
O(1)-identifiable from every observation window at every depth (it
parameterizes the transition structure every event obeys); (c) paid
once (one bit at the genesis branch) and replicated in all windows for
free (R = 1, versus decaying replication for present bits). The Cut
gains a third column: law = derivable, O(1) | constant = underived,
O(1) | present = underived, Theta(N). Omnipresence of constants is
thereby the definition of column two, not a puzzle. Column-two objects
already in this program: Chapter 13's orientation bit, Chapter 9's
world-phase, the research corpus's retrodicted constants (cited).

Evidence:

- shipped: `verification/scripts/mint_and_bill.py` §E.

Checks / controls:

- edge-disjoint isomorphic rule pair; exhaustive window census;
  monoid-deformation check. Honesty note: present-bit replication decay
  is established as a separation (R = 1 vs R < 1), not exhibited as a
  gradient, in the shipped model.

Residuals:

- whether nature's constants are column-two registrations is open by
  name (a structural definition is offered, not an empirical
  identification).

## FCT-89 - The Two Bills

Status: `MEASURED / MODEL-SCOPE / RECOVERY-VARIANT`
Scope: master-equation models, numeric discovery grade (labeled); an
organizing restatement of the housekeeping/excess decomposition
(Hatano-Sasa; Speck-Seifert) with Maroney and Kolchinsky-Wolpert as the
logical/thermodynamic-reversibility prior art (all cited); the claimed
sliver is the identification of Chapter 16's combinatorial merge price
as the excess floor.
Evidence state: `shipped` (labeled grade).

Public statement:

A driven floor's steady-state dissipation is housekeeping — cycle
driving with zero contraction (EP = 0.0327 in the shipped model, system
entropy rate zero; the structure-blind point has detailed balance and
EP = 0) — and carries no logical merging; the ledger price floors the
EXCESS (erasure) bill only (every 2-state erasure protocol dissipates
at least the entropy contraction, whose clean limit is ln 2 = the
2-merge price in nats). The corners tune independently. The floor pays
two independent bills — exploration (per unit time) and selection (per
merge) — and the ledger prices selection only.

Evidence:

- shipped: `verification/scripts/mint_and_bill.py` §F (numeric
  discovery grade, labeled in-script).

Checks / controls:

- Euler master-equation integration; Landauer bound checked across four
  protocols; detailed-balance null.

Residuals:

- exact-arithmetic certification of Section F is not claimed; the
  section is an organizing demonstration over cited theory.

## FCT-90 - The Width Hierarchy and the Forced Qutrit

Status: `THEOREM / MODEL-SCOPE`
Scope: the cyclic commutant of C[S_n], n = 3..8, exact combinatorics;
engine mathematics KNOWN and cited (Kraskiewicz-Weyman; Klyachko;
Springer; Stembridge; Reiner-Stanton-White; Swanson); the physical
composition adjudicated PLAUSIBLY-NEW (Dittel et al. and the
noiseless-subsystem line cited as nearest art).
Evidence state: `shipped`.

Public statement:

Tableau counting (SYT by major-index residue) reproduces Chapter 12's
commutant dimensions exactly (4, 10, 28, 136, 726, 5100 for n = 3..8)
and computes the emergent-fiber profile: max width 1, 1, 2, 3, 5, 12;
the FIRST EMERGENT QUTRIT is forced at n = 6 by pigeonhole on the 16
standard tableaux of (3,2,1), appearing as four M_3 blocks at residues
1, 2, 4, 5; the count-invisible budget is 0, 0, 2, 56, 480, 4290.

Evidence:

- shipped: `verification/scripts/emergent_hierarchy.py` U1-U3.

Checks / controls:

- RSK sanity per n; cross-engine against the closed form; exhaustive
  shape/residue profiles.

Residuals:

- growth asymptotics are Swanson's theorems (cited), not re-proven.

## FCT-91 - The Exposure Law

Status: `MEASURED / MODEL-SCOPE`
Scope: all 22 multiplicit fibers at n = 6; discovery-grade numeric
engines from the research corpus (cited; the same grade as the corpus
engine behind Chapter 12's n = 5 fiber constants); exactness
adjudicated PLAUSIBLY-NEW with the phenomenon's prior sightings cited
(Stueckelberg / Hardy-Wootters; Dhand et al.; Menssen et al.;
Hickey-Gour; Wu et al.; Oszmaniec-Brod-Galvao).
Evidence state: `cited`.

Public statement:

On every multiplicity-m fiber at n = 6, the compressed single-shot
count effects span EXACTLY the m(m+1)/2-dimensional symmetric part
(Hermitian on the fiber; real-symmetric in a suitable basis), and one
multiplication layer opens the full m^2. Rebit-blindness is the
universal readout law of counting: single-shot counting sees the shape
of every emergent fiber, never its orientation, and the orientation is
always and only one multiplication away.

Evidence:

- cited: research-corpus fiber engines (Young orthogonal
  representation; tolerance-based ranks).

Checks / controls:

- carried in the corpus; the exact tableau layer that locates the
  fibers is shipped (FCT-90).

Residuals:

- exact certification queued if the law becomes load-bearing beyond
  this chapter; n >= 7 extension open.

## FCT-92 - Width, Not Depth

Status: `THEOREM / MODEL-SCOPE`
Scope: exact rational span computations (m = 2, 3, 4); the algebraic
lemma is folklore and cited (with Rudolph-Grover and
McKague-Mosca-Gisin as the nearest physics); the dichotomy is claimed
as the organizing law.
Evidence state: `shipped`.

Public statement:

Real-symmetric generators plus one product/commutator layer span all
of M_m exactly; given the exposure law, one multiplication always
suffices where the symmetric part is exposed. The hidden resource of
the counting grammar grows in WIDTH (the number and size of invisible
fibers) and never in DEPTH (one multiplication opens any exposed
fiber).

Evidence:

- shipped: `verification/scripts/emergent_hierarchy.py` U4; the n = 6
  fiber instance cited (FCT-91).

Checks / controls:

- exact rank over Q; random rational generators.

Residuals:

- depth saturation is conditional on exposure; exposure is verified at
  n = 5 (Chapter 12, exact) and n = 6 (cited grade).

## FCT-93 - The Price of Self-Description

Status: `THEOREM / MODEL-SCOPE`
Scope: finite one-use floors with decoupled registers, exhaustive tiny
models; the impossibility core is KNOWN and cited (Breuer 1995; Wolpert
2008; Dalla Chiara 1977; Svozil); the PRICE structure is the claim
(adjudicated the release sweep's strongest priority candidate).
Evidence state: `shipped`.

Public statement:

A causally decoupled register carries zero world-information without a
scheme key (the interpretation map); the keyless deficit is an exact,
computable FLOOR INVARIANT that no scheme improves; adding inert
register capacity strictly INCREASES it (the anti-regress: visible
capacity is always below the placement entropy it adds); and the key
suffices exactly while being underived — the law's automorphisms
permute schemes without fixed points, so the key is a column-2 constant
(Chapter 17) of the hosted world.

Evidence:

- shipped: `verification/scripts/seventh_attempt.py` Movement A.

Checks / controls:

- exhaustive scheme construction and fiber censuses on two model sizes;
  the anti-regress verified with exact counts (36 -> 540).

Residuals:

- scope is the stated finite model class; nothing is claimed about
  physical observers.

## FCT-94 - The Books of a Self-Describing World

Status: `THEOREM / MODEL-SCOPE`
Scope: same model class; the zero-slack identity is presented as
Shannon-chain-rule bookkeeping per the sweep's demand (not a result);
the unwritability route is a new exact proof of a known conclusion
(Löfgren; Putnam's permutation argument; Harnad; Kripke — all cited).
Evidence state: `shipped`.

Public statement:

Minted = banked + deficit exactly and uniformly (bookkeeping, in
chain-rule form); the scheme space outweighs the history space
factorially (33.6M vs 216; 90! vs 162,000), so the complete
interpretation map is unwritable by the entire floor; and no
equivariant convention exists at any level of a meta-register tower
(single-orbit token registers force constant conventions, and a
constant still requires WHICH constant). Every interpretation tower
terminates in a paid, underived registration: every self-describing
world runs on injected capital, and the capital is a constant.

Evidence:

- shipped: `verification/scripts/seventh_attempt.py` Movement B.

Checks / controls:

- exact integer factorizations; exhaustive orbit computations at two
  tower levels.

Residuals:

- the injected-capital thesis has its lineage cited (Löfgren; Rosen);
  the computed constant-hood of the capital inherits FCT-93's novelty.

## FCT-95 - The Two Semantics and the Measurement Dial

Status: `THEOREM / MODEL-SCOPE`
Scope: coupled floors (cross tolerance), exhaustive; null-result
information labeled an exact RECOVERY of Renninger (1960) and
Elitzur-Vaidman (1993) with its inference-by-elimination character
stated; Wheeler/RQM/QBism cited as the prefiguring frame; two
registered misses upgraded into the results and scored in-text.
Evidence state: `shipped`.

Public statement:

With coupling, the joint floor has strictly more histories than the
decoupled product — measurement mints new, co-minted branch structure —
and key-free information exists exactly on co-minted events: an
observer can read without a key precisely what it helped mint.
Null-result information is exact: on an exhaustion floor a trace
showing no interaction still constrains the world. Consumptive and
conventional information partition the total exactly, each priced in
its own currency (participation vs seed); across the coupling dial, a
hosted observer's key burden is exactly its non-participation.

Evidence:

- shipped: `verification/scripts/seventh_attempt.py` Movement C.

Checks / controls:

- zero-coupling control (decoupled limit recovered); exact fiber
  factorizations; the full dial table (mint 216 -> 528; key-free
  traces 0 -> 60).

Residuals:

- the participation semantics is claimed as the two exact theorems
  only; the phenomenon-level readings are cited to their ancestors.

## FCT-96 - First-Person Theorems

Status: `THEOREM / MODEL-SCOPE`
Scope: no-partition floors (observer = any mark subset), exhaustive;
the biography theorem is cited to Everett's memory sequences and
Lamport's happened-before partition; the healing monotonicity has
quantum Darwinism's shape (Zurek; Blume-Kohout–Zurek, cited); THE
JUXTAPOSITION — inert capacity worsens while participation heals — is
the claim (found nowhere by the sweep); the privacy-unit is flagged
plausibly-new-but-near-trivial.
Evidence state: `shipped`.

Public statement:

The keyless residual is monotone decreasing in observer size
(6.49 -> 0 bits): capacity heals exactly when it participates, against
the anti-regress of FCT-93 which governs non-participants — one model,
both regimes. Privacy's unit is the unobserved CONTACT (a one-mark
exterior has no private life). Every observer's complete causal
biography, in its internal order, is key-free and exact; the fibers
factorize so that the unknown is always and only the exterior's
private life and the observer's placement among it — proper time is
free; the unknown is when-relative-to-others.

Evidence:

- shipped: `verification/scripts/seventh_attempt.py` Movement D
  (self-consumption).

Checks / controls:

- the full observer-size sweep; fiber factorization for every trace.

Residuals:

- phenomenological readings are not claimed; the theorems are
  combinatorial.

## FCT-97 - Intersubjectivity Theorems

Status: `THEOREM / MODEL-SCOPE / RECOVERY`
Scope: disjoint observer pairs on one floor, exhaustive; the partition,
forced order-agreement, and reconstruction-from-projections are
textbook trace-theory facts (Mazurkiewicz; Zielonka — cited wholesale);
presented against the quantum backdrop where intersubjectivity is
contested (Frauchiger-Renner-style arguments; Ozawa's QM-side
agreement theorem; quantum Darwinism consensus).
Evidence state: `shipped`.

Public statement:

Events partition into four sectors (shared / private-1 / private-2 /
exterior-private); co-witnessed events appear in both observers'
experiences in the same relative order in every history (forced
agreement — no Rashomon, a property of the floor's semantics); each
observer determines the other exactly where they touched (mutual
opacity, symmetric first-person privilege); pooled experience
determines everything except what neither touched. An exact classical
recovery, valuable for the contrast: the floor forces the agreement
that quantum theory's own intersubjectivity debates contest.

Evidence:

- shipped: `verification/scripts/seventh_attempt.py` Movement D
  (witnesses).

Checks / controls:

- exhaustive over all 90 histories; joint-fiber factorization.

Residuals:

- claimed as recovery + contrast framing only.

## FCT-98 — The Observer Hierarchy (Chapter 20)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Public statement: sub-experience = restriction of super-experience;
  the one-way mirror (exact knowledge asymmetry across containment);
  additively telescoping inter-level ignorance, zero slack.
- Evidence: shipped — `verification/scripts/observers_price.py` §1.
- Checks / controls: exhaustive on the 6-mark clique; fiber counts.
- Residuals: labeled recovery (Parikh–Ramanujam, FHMV, Mazurkiewicz);
  telescoping = entropy chain rule on trace fibers (bookkeeping).

## FCT-99 — Teaching Is Measurement (Chapter 20)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: boundary transfer and measurement are one
  co-minting operation; capacity = boundary fork width; a learner's
  budget = the boundary mint.
- Evidence: shipped — `observers_price.py` §2.
- Checks / controls: exact boundary-fiber computations.
- Residuals: components classical (von Neumann cut, Zurek transfer,
  Goldman–Kearns teaching dimension — cited); the identity is the
  claim.

## FCT-100 — The Shared Eye and Redirection (Chapter 20)

- Status: `THEOREM / MODEL-SCOPE` (redirection = priority candidate)
- Public statement: shared senses are automatic consensus channels;
  overlap redirects agreement from pair-facts to world-facts (common
  sector does not grow); pooled coverage pays.
- Evidence: shipped — `observers_price.py` §3.
- Checks / controls: exact overlap sweep 0/1/2 at fixed sizes.
- Residuals: consensus mechanism = quantum-Darwinism recovery
  (Zurek); the conservation statement located nowhere by the sweep.

## FCT-101 — Membership Opacity and the Price List (Chapter 20)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: observer membership is unwitnessed; mutual
  knowledge requires a received membership key; the six ways of
  knowing close into a two-currency list (participation in consumed
  marks / convention in received keys).
- Evidence: shipped — `observers_price.py` §4.
- Checks / controls: relabeling-ensemble identity; six-row coherence.
- Residuals: impossibility core classical (Lewis, Halpern–Moses,
  Clark–Marshall — cited); Krakauer et al. 2020 distinguished; the
  exhaustiveness is the claim with its falsifier named.

## FCT-102 — The Capability Anthropic Filter (Chapter 21)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: three exact observer-capability predicates select
  the mixed cell as the only asker-compatible floor; no ensemble, no
  measure.
- Evidence: shipped — `verification/scripts/genesis_of_space.py` §1.
- Checks / controls: per-cell exact mini-models (coagulation,
  reversible, credit, mixed).
- Residuals: Carter's WAP already measure-free (sweep correction,
  absorbed); the three-predicate structural test is the contribution.

## FCT-103 — Locality Is Affordability (Chapter 21)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: bounded observers require sparse worlds — the
  fork frontier is world-size-independent exactly on bounded degree.
- Evidence: shipped — `genesis_of_space.py` §2.
- Checks / controls: exact frontier counts, two world sizes; a
  registered opposite hunch killed by its own test and scored.
- Residuals: quantum graphity named as the narrative incumbent.

## FCT-104 — The Inheritance Budget and the Sparsification Arrow (Chapter 21)

- Status: `THEOREM / MODEL-SCOPE` (priority candidate)
- Public statement: intersection inheritance always paid; union
  unpaid exactly off the local-clique case; degrees never increase;
  total tolerance monotone non-increasing.
- Evidence: shipped — `genesis_of_space.py` §3.
- Checks / controls: exhaustive over all 1024 five-mark worlds,
  every contact; 20,480 vertex-checks.
- Residuals: per-contact monotonicity is what quantum graphity's
  thermodynamic story does not prove (relation stated).

## FCT-105 — Crystals, Germs, Fission (Chapter 21)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: reproduction iff adjacent twins; universal
  reproduction iff disjoint cliques; gradient flow with crystal
  plateaus; healing by fission only; triangle-free is dust; the
  triangle is the minimal witnessed durable structure.
- Evidence: shipped — `genesis_of_space.py` §4.
- Checks / controls: exhaustive n=5 twin theorem; BFS reachability.
- Residuals: twin algebra classical (Gallai; cluster graphs —
  cited); dynamical theorems are the claims; one miss upgraded.

## FCT-106 — The Radius-1 Light Cone (Chapter 21)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Public statement: influence moves at most one adjacency per
  contact (pass-through exact); the cone saturates at distance k−1;
  wounds erasable; no cone on the clique.
- Evidence: shipped — `genesis_of_space.py` §5.
- Checks / controls: exhaustive 46,080 triples; all schedules to
  depth 4 on the triangle ring.
- Residuals: Lieb-Robinson-shaped recovery (cited); no Hamiltonian,
  norm, or clock needed is the point.

## FCT-107 — Two Arrows and the Frozen Wound (Chapter 22)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: time factors into clock and ledger arrows; lazy
  contacts hand divergence on pointwise unchanged; information
  dynamics lives on the spend sector.
- Evidence: shipped — `verification/scripts/price_of_chance.py`
  §§1–3.
- Checks / controls: exhaustive n=4 all pairs; n=5 all single
  wounds; one registered bet killed as frozen.
- Residuals: none beyond stated scope.

## FCT-108 — The Participation Calendar (Chapter 22)

- Status: `THEOREM / MODEL-SCOPE` (priority candidate)
- Public statement: symmetry forces counting; the seat un-forces it;
  the invariant simplex's coordinates are the participation calendar
  (closed form m²−1 / m²+2m−1); exchangeability collapses the key to
  interaction-type propensities.
- Evidence: shipped — `price_of_chance.py` §§2, 4–5.
- Checks / controls: exhaustive K4–K8; K7/K8 closed-form values
  frozen before computation and hit.
- Residuals: ergodic-decomposition structure and seat-symmetry
  arguments classical (Phelps; Sebens–Carroll; Elga — cited).

## FCT-109 — The Measure Dilemma (Chapter 22)

- Status: `THEOREM / MODEL-SCOPE` (priority candidate)
- Public statement: a weighting can be shared or affordable, never
  both — cross-seat agreement forces exactly counting, and counting
  is provably nonlocal.
- Evidence: shipped — `price_of_chance.py` §§6–8.
- Checks / controls: exact path-world nonlocality (identical
  radius-5 neighborhoods, different propensities); hand-derived
  agreement equations frozen and hit; six seat geometries.
- Residuals: ingredients classical and cited (Valiant; Balliu et
  al.; Aumann; Aaronson; Li–Vitányi); the dichotomy theorem is the
  claim.

## FCT-110 — The Field Carrier and Forced Limits (Chapter 22)

- Status: `THEOREM / MODEL-SCOPE` (mathematics = recovery, cited)
- Public statement: a 3-dimensional local recurrence carries the
  shared measure (one field serves all seats — the dilemma dissolves
  at the price constant → field); limits are forced algebraic
  numbers (1/ρ² boundary, C/ρ² bulk); chance has a boundary layer;
  a boundaryless world is pure bulk with Perrin branch counts and
  mandatory symmetry breaking on primes.
- Evidence: shipped — `price_of_chance.py` §§9–14.
- Checks / controls: recurrence vs brute force; bidirectional
  propagation; frozen Perrin values; free-action exhaustive C5–C13.
- Residuals: Padovan/Perrin/plastic-number/transfer machinery known
  (Bonte–Devillez–Hertz–Mélot 2024 the nearest work; Heilmann–Lieb;
  Pearl; Lucas; Füredi; Georgii — all cited); framings are the
  claims.

## FCT-111 — The Earned Key and the Ladder (Chapter 22)

- Status: `THEOREM / MODEL-SCOPE` (priority candidate: the
  mortal/eternal split)
- Public statement: world size is written in an eternal observer's
  waiting times; one mortal life identifies structure (a lifetime is
  a census) but provably never the measure (irreducible TV floor);
  mortal and eternal worlds assign different chances to identical
  events; measure-holding is a three-rung ladder priced in nothing,
  boundary data, or time.
- Evidence: shipped — `price_of_chance.py` §§15–17.
- Checks / controls: exact binomial/Fraction arithmetic throughout;
  one frozen bet killed and upgraded twice, scored.
- Residuals: classical components cited (Le Cam; Chernoff–Stein;
  inspection paradox; Hájek; anthropic shadow); the amplitude
  suggestion is a target with Müller 2020 named.

## FCT-112 — The Rational Decay Spectrum (Chapter 23)

- Status: `THEOREM / MODEL-SCOPE` (triangular-chain mechanism =
  recovery, Kemeny-Snell 1960 / Miclo 2021 cited)
- Public statement: no temporal cycles; triangular transfer in the
  tolerance grading; spectrum = laziness diagonal with closed form
  [C(a,2)+C(b,2)+C(u,2)]/[C(n,2)-ab].
- Evidence: shipped — `verification/scripts/decay_of_worlds.py`.
- Residuals: closed form and quantization-by-grading reading are the
  claims; mechanism classical.

## FCT-113 — The Urn Identification (Chapter 23)

- Status: `THEOREM / MODEL-SCOPE` (urn laws classical —
  Eggenberger-Polya 1923, Johnson-Kotz 1977; identification of a
  rewrite system's DECAY sector as an exact urn located nowhere by
  the sweep; growth-side precedent Berger-Borgs-Chayes-Saberi 2014
  cited as the inverted analogue)
- Public statement: the wounded clique's reachable classes = the
  W(a,b,u) family; spends recruit at a/(a+b) — the Polya urn from
  (1,1); uniform profiles, closed-form branching, size-biased
  healthy fates, certain separation, exact lifetimes.
- Evidence: shipped — `decay_of_worlds.py` (urn sections).
- Residuals: presented as a bridge to 1923, not a divergence.

## FCT-114 — The Self-Location Split (Chapter 23)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (Sebens-Carroll 2018,
  Elga 2004, Kent 2010 cited — the dialectic recovered as an exact
  finite theorem)
- Public statement: self-location is mass-weighted by conservation
  (trivial, stated so) and has zero residual freedom given any
  branch weighting; the weighting stays free. No self-location key
  exists.
- Evidence: shipped — `decay_of_worlds.py` (self-location sections).
- Residuals: the exact split is the contribution.

## FCT-115 — Fate Laws and Breeding Wounds (Chapter 23)

- Status: `THEOREM / MODEL-SCOPE` (occupation ergodics + single-run
  non-identifiability = recoveries, cited; one registered arity
  bound killed by the engine, scored)
- Public statement: clock rates identify fate; wounded uniform vs
  healthy size-biased (conservation-linked); 3-fission from two
  disjoint wounds at 76/495; the octahedron: (3,3) 4/5, (2,2,2)
  1/5, no isolation.
- Evidence: shipped — `decay_of_worlds.py` (clock + multi-wound).
- Residuals: exact rationals model-specific.

## FCT-116 — The Opposition Calculus (Chapter 24)

- Status: `THEOREM / MODEL-SCOPE` (join primitive = CRDT/gossip,
  Shapiro et al. 2011 + Baker-Shostak 1972 cited; conservation
  corollaries = lattice monotonicity, zero priority, stated so;
  factorization labeled trivial, NOT Haag-Wightman — sweep-mandated
  downgrade absorbed in print)
- Public statement: genesis on the complement is the join protocol;
  lines are partial sign vectors over wound coordinates; opposition
  is coordinate conflict (exact, 647 labeled states).
- Evidence: shipped — `verification/scripts/particles_of_floor.py`.
- Residuals: the identity and the contamination mechanism are the
  claims.

## FCT-117 — Bound States and Profile Universality (Chapter 24)

- Status: `THEOREM / MODEL-SCOPE` (beta-binomial laws classical,
  cited; the topology-to-urn dictionary + fission-arity rule =
  plausibly-new per sweep)
- Public statement: adjacent wounds are one composite (urn from
  (1,2), mass 4/13); profile universality; composites never
  three-way fission — interaction requires separation.
- Evidence: shipped — `particles_of_floor.py` (bound states).
- Residuals: fenced against QFT vocabulary; model scope.

## FCT-118 — The Charge Law (Chapter 24)

- Status: `THEOREM / MODEL-SCOPE` (the sweep's strongest priority
  candidate; Pemantle 2007 cited as mechanism, not prior)
- Public statement: P(a mark ends totally isolated) = k/(n-1)
  exactly, k = its opposition count — three topologies + urn
  recursion. Charge is a fate rate.
- Evidence: shipped — `particles_of_floor.py` (charge law).
- Residuals: charge-universality conjecture registered in the
  research record, frozen, untested.

## FCT-119 — Selection Rules, the Healthy Floor, r-Cliques (Chapter 24)

- Status: `THEOREM / MODEL-SCOPE` (Mermin's selection-rule template
  + Blackwell-MacQueen Dirichlet law cited as known)
- Public statement: only founders isolate (the healthy floor); the
  octahedron's forbidden channel = an exhaustion bound (max 4 of 5
  needed oppositions); r-cliques = r-color urns, exactly r pieces,
  uniform compositions — the triangle sector predicted in full
  before measurement and hit. Minting-capacity formula named open.
- Evidence: shipped — `particles_of_floor.py` (rules + triangle +
  criterion).
- Residuals: capacity formula open; topology- not count-dependent.

## FCT-120 — The Conservation Sector and Immortality (Chapter 25)

- Status: `THEOREM / MODEL-SCOPE` (number-conserving frame +
  von Neumann/Langton lineage cited; all-n open by name)
- Public statement: surplus = periphery; maximal retention conserves
  exactly; a reproducing assignment always exists at tested scope.
- Evidence: shipped — `verification/scripts/the_up.py`.

## FCT-121 — The Dial and the First Equilibrium (Chapter 25)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: E[loss] = (1-theta) x periphery; two axes
  (retention/steering); exact rational stationary ensemble, cycle
  weight 1/61.
- Evidence: shipped — `the_up.py`.

## FCT-122 — The Edge Theorem and the Thin Edge (Chapter 25)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (Langton,
  Mitchell-Hraber-Crutchfield, Darroch-Seneta, von Neumann 1956
  cited)
- Public statement: diversity maximized strictly below full
  retention; the edge is razor-thin (full tour needs 1-1e-6).
- Evidence: shipped — `the_up.py`.

## FCT-123 — The Maintenance Inversion and the Matching Principle (Chapter 25)

- Status: `THEOREM / MODEL-SCOPE` (cost form Landauer/Bennett,
  cited; the INVERSION = priority candidate)
- Public statement: cycles pay 1 bit/contact, the wounded clique is
  free — sparse is the expensive kind; growth needs received
  contact AND retained periphery.
- Evidence: shipped — `the_up.py`.

## FCT-124 — The Steering Radius and the Gossip Theorem (Chapter 26)

- Status: `THEOREM / MODEL-SCOPE` (shotgun assembly / LOCAL model
  instances, cited; a registered lean died, scored)
- Public statement: rows insufficient (2/32 ambiguous), the ball
  perfect (138 types, 0 violations); upkeep = one round of gossip;
  privacy failure exhibited.
- Evidence: shipped — `verification/scripts/habitability.py`.

## FCT-125 — The Privacy Census and the Zero-Traffic Bootstrap (Chapter 26)

- Status: `THEOREM / MODEL-SCOPE` (LCL census frame + silent
  self-stabilization, cited; frozen bet held)
- Public statement: one gossip-requiring shape; the habitable set
  fully private; fresh-by-construction + stationarity close the
  bootstrap with no channel; the gossip sector cannot run on memory
  (96/125 mis-steer).
- Evidence: shipped — `habitability.py`.

## FCT-126 — No Doom States: the Price List (Chapter 26)

- Status: `THEOREM / MODEL-SCOPE` (recovery property =
  self-stabilization's guarantee, cited; the finite cost-annotated
  price list = the priority artifact)
- Public statement: every conserved-sector class steerable (max 3
  bits); slips are 1-bit detours.
- Evidence: shipped — `habitability.py`.

## FCT-127 — The Habitability Theorem (Chapter 26)

- Status: `THEOREM / MODEL-SCOPE` (plausibly-new equivalence;
  Conant-Ashby / requisite variety / autopoiesis cited; THE CAUTION
  IN PRINT: both sides downstream of sparseness — a convergence
  theorem about the shared root)
- Public statement: habitable = maintainable at tested scope.
- Evidence: shipped — `habitability.py`.


## FCT-128 — The Kernel Symmetry and the Microcanonical State Count (Chapter 27, 29)

- Status: `THEOREM / MODEL-SCOPE` (symmetric-kernel stationarity
  textbook, cited; the exact 3660 = 61 x 60 state count and the
  sector-certification are the artifacts; all-sector statement =
  named conjecture)
- Public statement: the labeled contact kernel is exactly symmetric
  on the C4/C5/C6 closures; uniform-on-labeled-worlds, detailed
  balance, and class weights = orbit counts follow at this scope.
- Evidence: shipped — `floor_thermodynamics.py`,
  `two_kinds_of_the_given.py`.

## FCT-129 — The Boltzmann Identity and the Forced Rate Form (Chapter 27)

- Status: `THEOREM / MODEL-SCOPE` (surprisal-as-energy definitional
  — Jaynes/Landauer cited; Cauchy-equation exponential route
  classical — Arrhenius/Kramers cited)
- Public statement: 2^(-cost) = class-preserving fraction; cost
  exactly additive over disjoint components (all 16 cross-pairs);
  price-only independent rate laws are exponential; temperature
  received.
- Evidence: shipped — `floor_thermodynamics.py`.

## FCT-130 — The Free-Move Law (Chapter 27)

- Status: `THEOREM / MODEL-SCOPE` (min-cost-over-mean selection =
  Freidlin-Wentzell / blowtorch phenomenon, cited; the exact split
  is the artifact; a frozen mean-cost bet died and is scored)
- Public statement: price-weighted stationary redistribution is
  sorted exactly by possession of a certainty contact (7/6 split),
  not by mean cost (29/74 violations); self-retention lemma exact.
- Evidence: shipped — `floor_thermodynamics.py`.

## FCT-131 — Unimodal Dissipation and the Cost-Pump Cycle (Chapter 27)

- Status: `THEOREM + MEASURED / MODEL-SCOPE` (Schnakenberg EP and
  cycle theory cited; a frozen monotone bet died and is scored)
- Public statement: entropy production zero exactly at beta=0,
  measured unimodal (peak beta=2) on the sampled grid; current
  divergence-free; downhill functional vanishes identically; mean
  cost falls under coupling.
- Evidence: shipped — `floor_thermodynamics.py`.

## FCT-132 — The Preservation-Price Law (Chapter 28)

- Status: `THEOREM / MODEL-SCOPE` (context preservation standard,
  cited; the price law with unique witness and boundary rigidity
  are the artifacts)
- Public statement: preserving E[S] costs one bit per exposed
  member with a unique stay-with-parent witness; preserving the
  boundary too admits only the identity. 1700 cases, exhaustive.
- Evidence: shipped — `maintained_structures.py`.

## FCT-133 — Control Invariance (Chapter 28)

- Status: `THEOREM / MODEL-SCOPE` (safety-game/controlled-invariance
  frame, cited)
- Public statement: the stay-with-parent strategy keeps E[S]
  invariant under every schedule and every environment assignment
  (729-state exhaustive).
- Evidence: shipped — `maintained_structures.py`.

## FCT-134 — The Copy-Construction Dichotomy (Chapter 28)

- Status: `THEOREM / MODEL-SCOPE` (attractor/safe-set machinery
  classical, cited; the exact dichotomy = an instance of the
  constructor-theoretic asymmetry, von Neumann / Deutsch-Marletto
  cited; the exhaustively-solved instance is the artifact)
- Public statement: adversarial environments block complement-copy
  forever from every not-yet-copied state; cooperative environments
  reach it in one contact. Persistence autonomous; copying needs
  cooperation.
- Evidence: shipped — `maintained_structures.py`.

## FCT-135 — Defect Concentration (Chapter 28)

- Status: `THEOREM / MODEL-SCOPE` (plausibly-new structural
  corollary per the sweep)
- Public statement: in K6-e all singles lie at the defect; a
  triangle avoiding it has preservation cost exactly zero; C6
  hosts no free size-3 substructure.
- Evidence: shipped — `maintained_structures.py`.

## FCT-136 — The Symmetry Certification and the Two Retirements (Chapter 29)

- Status: `THEOREM / MODEL-SCOPE` (discrete Liouville-type
  property; classical frame cited; the retirements are the
  explanatory artifact)
- Public statement: kernel symmetry certified on three closures;
  the reference measure is thereby derived (retired as a received
  input); the consent boundary reduces to membership.
- Evidence: shipped — `two_kinds_of_the_given.py`.

## FCT-137 — Kernel Equivariance (Chapter 29)

- Status: `LEMMA / MODEL-SCOPE` (consistency check; label-
  exchangeability standard, cited)
- Public statement: P(gE -> gE') == P(E -> E') exactly; no hidden
  label dependence.
- Evidence: shipped — `two_kinds_of_the_given.py`.

## FCT-138 — The Two Kinds of the Given (Chapter 29)

- Status: `FRAMEWORK + CONJECTURE` (torsors — Baez; scale typology
  — Stevens/KLST; absolute objects — Anderson-Friedman/Pitts; all
  cited; completeness = named conjecture with standing falsifier;
  the reversibility-selects-the-origin criterion = candidate-novel)
- Public statement: eleven torsor-type + two dial-type received
  inputs; graded 5/4/2; two retired this release.
- Evidence: shipped — `two_kinds_of_the_given.py`.

## FCT-139 — The Certified Phase Windows (Chapter 30)

- Status: `THEOREM (Sturm-certified) / MODEL-SCOPE` (EP mechanism
  classical — Kato and the stochastic-EP literature cited; the
  certified specimen is the artifact)
- Public statement: complex pairs exist at certified couplings and
  not at others, both sides of equilibrium; the reversible point is
  provably real; positive-side window reported (not certified,
  irrational weights).
- Evidence: shipped — `where_i_lives.py`.

## FCT-140 — The Sign Cover: Mass Without Phase (Chapter 30)

- Status: `THEOREM (certified) + CITED-RECOVERY / MODEL-SCOPE`
  (2-lift spectra = Bilu-Linial; nonsplit = Zaslavsky balance;
  gap = frustration-index phenomenon, Belardo/Martin cited; gap
  sizes reported; certified realness at sampled couplings = the
  artifact)
- Public statement: the deviation-parity cover is nonsplit; the
  sign sector is Sturm-certified all-real at beta = 0, -1, -4, -2
  — including inside the base's certified complex windows.
- Evidence: shipped — `where_i_lives.py`.

## FCT-141 — The Quarter-Turn Sectors Ring at Equilibrium (Chapter 30)

- Status: `THEOREM (exact Gaussian-rational) / MODEL-SCOPE`
  (gain graphs — Reff; covering characters — Mizuno-Sato;
  Frobenius-Schur dichotomy; Galois conjugacy; all cited; measured
  args kernel-inherited, stated)
- Public statement: charge-1/3 characteristic polynomials are
  exactly non-real at the reversible point on both sectors —
  complex eigenvalues exist at equilibrium in the quarter-turn
  sectors while charge-0/2 are real.
- Evidence: shipped — `where_i_lives.py`.

## FCT-142 — Swap-Pairing and the Identity Move (Chapter 31)

- Status: `LEMMA / MODEL-SCOPE` (sign-reversing involutions
  standard — Lindstrom / Gessel-Viennot cited)
- Public statement: flipping all singles conjugates the successor
  by (a b); stay-with-parent is the exact labeled identity. 587
  cases, three sectors.
- Evidence: shipped — `the_reading_law.py`.

## FCT-143 — Rest Is Precluded on the Cycles (Chapter 31)

- Status: `THEOREM / MODEL-SCOPE` (checkerboard i-per-deviation
  mechanism — Feynman-Hibbs, Jacobson-Schulman; Sorkin preclusion;
  both cited; the exact cycle vanishing is the artifact)
- Public statement: C4/C5/C6 quarter-turn survival amplitude is
  exactly zero; even-sector cancellation exact at k = 2 mod 4.
- Evidence: shipped — `the_reading_law.py`.

## FCT-144 — The Additivity Dichotomy (Chapter 31)

- Status: `THEOREM / MODEL-SCOPE` (the consistency condition of
  decoherent histories — Griffiths/Omnes/Gell-Mann-Hartle — and
  the Dowker-Kent genericity, cited; explicitly NOT Born's
  squared-modulus rule; a frozen majority bet died and is scored)
- Public statement: |amp| <= P entrywise; every channel
  additivity-exact xor strictly destructive; purity falls with
  size (0.462/0.312/0.286).
- Evidence: shipped — `the_reading_law.py`.

## FCT-145 — The Readout Theorem and the Zeno Tier (Chapter 31)

- Status: `THEOREM / MODEL-SCOPE` (records-imply-consistency —
  Gell-Mann-Hartle strong decoherence, Zurek environment-as-witness
  — and Misra-Sudarshan Zeno, cited; the exact structural
  criterion is the artifact)
- Public statement: full anchoring admits only the identity;
  shape-locked touching outcome channels are single-path (2734
  channels) — consistency holds exactly on everything read.
- Evidence: shipped — `the_reading_law.py`.

## FCT-146 — The Reading Law (Chapter 31)

- Status: `THEOREM / MODEL-SCOPE` (visibility/spin-bath product
  form cited — Zurek; Englert lineage; constants kernel-inherited,
  stated; the exact closed formula is the artifact)
- Public statement: amplitude = P x ((1+i)/2)^m x i^d on every
  channel, exactly — consistency at m=0, 1/sqrt2 damping and pi/4
  drift per unread single, recorded phase i^d.
- Evidence: shipped — `the_reading_law.py`.


## FCT-147 — The Double-Slit Census and the Exact Eraser (Chapter 32)

- Status: `THEOREM / MODEL-SCOPE` (Scully-Druhl / Kim delayed-choice
  phenomenology cited; here rational-arithmetic theorems; frozen
  bets all hit)
- Public statement: 2504 constructive / 1284 destructive fringe
  channels; ceiling |amp| <= P intact; branch partition exact;
  2448 full-recovery eraser channels — determination, not timing,
  is the invariant.
- Evidence: shipped — `the_coat_and_the_count.py`.

## FCT-148 — The Forced Path Integral (Chapter 32)

- Status: `THEOREM / MODEL-SCOPE` (Dowker-Kent genericity cited; a
  universal one-law bet died and is scored; eraser-lesson
  instrument note in print)
- Public statement: the determined-bits law holds exactly on free
  channels and fails exactly on correlated hidden-bit support;
  trajectory amplitudes are Markov over invisible worlds — Feynman
  summation forced at path-entanglement.
- Evidence: shipped — `the_coat_and_the_count.py`.

## FCT-149 — The Coupling Trichotomy and the Actuality Measure (Chapter 32)

- Status: `THEOREM + NAMED DOOR / MODEL-SCOPE` (Fine; Feynman
  negative-probability; Sorkin quantum measure; Kirkwood-Dirac
  lineage — all engaged; the amplitude-LINEAR dictionary is the
  floor-native correction)
- Public statement: coat silent on fully-read channels; no
  nonnegative path measure yields coat statistics (darkening +
  preclusion witnesses); record-level actuality required — the
  founding no-selector postulate; actuality measure = received
  input with an exact separation table.
- Evidence: shipped — `the_coat_and_the_count.py`.

## FCT-150 — The Mirror-Pair (Chapter 32)

- Status: `LEMMA / MODEL-SCOPE` (algebraic status stated: |Sum|^2
  expanded; content = uniqueness of the positive pairing)
- Public statement: on record-sharing run pairs the conjugate
  pairing is the unique real/nonnegative pairing and equals
  |amp|^2 channelwise.
- Evidence: shipped — `the_coat_and_the_count.py`.

## FCT-151 — Affine Supports and the Code Reading Law (Chapter 33)

- Status: `THEOREM / MODEL-SCOPE` (Poisson/MacWilliams classical,
  cited; stabilizer-amplitude echo engaged)
- Public statement: every censused hidden-bit support is an affine
  code coset with uniform marginals (the pre-Hilbert anomaly
  form); amplitude = dual-codeword sum exactly; the free case is
  the reading law.
- Evidence: shipped — `the_codes_of_the_coat.py`.

## FCT-152 — The Brightness Criterion and the E8 Threshold (Chapter 33)

- Status: `THEOREM / MODEL-SCOPE` (Gleason on self-dual codes;
  Pless classification — classical, cited; the floor-native
  instantiation and the dark/bright dichotomy framing are the
  contribution)
- Public statement: coherence 1 iff coset weight constant mod 4;
  all self-dual codes below length 8 perfectly dark; e8 = [8,4,4]
  the first bright self-dual class (1 bright coset of 16).
- Evidence: shipped — `the_codes_of_the_coat.py`.

## FCT-153 — The Cap Theorem, Dual Autobiography, and the Tilted Light (Chapter 33)

- Status: `THEOREM-AT-SCOPE` (proof shape in print; the general
  pin lemma a NAMED GAP; two hunting bets died and are scored;
  the two-null parking and same-day promotion in the record)
- Public statement: aligned solo channels never exceed correlation
  distance 2; the dual code is a direct sum of per-line parts (no
  cross-line conservation law); every bright channel is a sum of
  odd-offset pairs — unlimited quantity, coarsest quality, every
  bright bond twisted.
- Evidence: shipped — `the_codes_of_the_coat.py`.

## FCT-154 — Entanglement Needs Privacy (Chapter 33)

- Status: `THEOREM / MODEL-SCOPE` (monogamy / environment-
  monitoring rhymes engaged, not claimed)
- Public statement: two shape-locked witnesses on C6 determine
  every deviation bit of every channel — the hidden sector is
  annihilated; coherence is private where copying is social.
- Evidence: shipped — `the_codes_of_the_coat.py`.

## FCT-155 — The Pin Lemma Closed, with Aligned Wandering (Chapter 34)

- Status: `THEOREM / CENSUS-SCOPE` (frozen Lemma B killed and kept
  in print; proof chain leaner than designed)
- Public statement: position autonomy + interval duals close the
  distance-2 cap and dual autobiography at solo-aligned censused
  scope; two-spot confinement fails on 1148/1524 channels
  (aligned wandering) and the proof never needed it.
- Evidence: shipped — `pin_lemma.py`.

## FCT-156 — The Stratification Theorem + Operational Blindness (Chapter 34)

- Status: `THEOREM / CENSUS-SCOPE` (decoherent-histories strata
  cited; evasion hunt scored as a returned-empty bet)
- Public statement: every misaligned channel is an exact weighted
  sum of aligned code strata (1896 channels; no third kind); all
  12 bright misaligned channels die to a single reveal; no
  misaligned channel is exactly precluded; the modulus reading
  composes multiplicatively (512 interleaved sequences).
- Evidence: shipped — `stratified_sector.py`, `coat_composition.py`.

## FCT-157 — The Sign-Separation Discriminator (Chapter 35)

- Status: `THEOREM + ORGANIZING COROLLARY / MODEL-SCOPE`
  (counting and squared rows recoveries: Sorkin, Kofler-Brukner,
  Clemente-Kofler, Sinha; linear row a one-line corollary stated
  at that altitude; Goldstein-Page disambiguated; Halliwell
  engaged)
- Public statement: NSIT violations separate the three actuality
  candidates by sign — zero / never-positive / both-signed — so
  one positive violation kills counting and linear at once; the
  triple test separates linear (I3 nonzero) from squared (I3 = 0
  exactly, grade 2).
- Evidence: shipped — `actuality_protocol.py`.

## FCT-158 — The Registered Actuality Package (Chapter 35)

- Status: `REGISTERED CONDITIONAL TARGET SUITE` (discipline
  claimed as practice, not contribution; bridge premises OPEN)
- Public statement: three exact tables (refusal 1/4x4 vs 0,0,0,1;
  NSIT signs; triples) with kill conditions per candidate; no
  candidate substrate is named, and this is said plainly.
- Evidence: shipped — `actuality_protocol.py`.

## FCT-159 — The Alien Transport: Generic Core vs Genesis-Contingent Laws (Chapter 36)

- Status: `THEOREM / TWO-FLOOR CENSUS SCOPE`
- Public statement: on the mortal coagulation floor the coat,
  ceiling, dichotomy, and mod-4 purity law travel intact while
  rest, indifference, and exact preclusion die (0 dark channels
  through depth 3); the cap is floor-contingent — six bright
  distance-4 channels exist where the genesis cap forbids them.
- Evidence: shipped — `alien_coat.py`, `alien_reading.py`.

## FCT-160 — The Two Grammars and the Reveal Law (Chapter 36)

- Status: `RECOVERY / CENSUS-SCOPE` (Dicke states, Delsarte
  Johnson-scheme duality, stabilizer-vs-symmetric dichotomy,
  Briegel-Raussendorf persistency, Dur, Stockton et al. — all
  cited; the floor-side derivation is the claim)
- Public statement: parity (linear-code) vs cardinality
  (constant-weight) correlation grammars, unmixed per channel in
  census; alien bright channels reveal-robust (459/459) while
  genesis parity channels are reveal-fragile — the GHZ-versus-W
  persistency contrast recovered from record grammar; the NSIT
  sign table transports unchanged (coat-level).
- Evidence: shipped — `alien_reading.py`.

## FCT-161 — The Permission Map (Chapter 37)

- Status: `THEOREM SUITE + METHOD / TWO-FLOOR MATCHED-CENSUS
  SCOPE` (taxonomy question cited to the reconstruction
  literature; method claim narrow)
- Public statement: a twelve-row map, every cell engine-backed,
  with three verdict types — generic (staging, ceiling,
  dichotomy, purity, sign table), contingent (conservation,
  survival, indifference, darkness, the distance cap: 2 vs 4
  live and matched), instrument-indexed (grammar, persistence);
  conservation is the measure-side switch (necessity converse
  open).
- Evidence: shipped — `permission_map.py`.

## FCT-162 — The Third Axis + Factorization Persistence + the Repetition Anomaly (Chapter 37)

- Status: `THEOREM / MATCHED-CENSUS SCOPE` (observable-induced
  structure cited: Zanardi, Zanardi-Lidar-Lloyd,
  Barnum-Knill-Ortiz-Viola; factorization a no-signaling
  recovery; anomaly model-internal with the temporal-correlations
  genre cited)
- Public statement: the genericity verdict itself is
  instrument-relative (grammar and blindness flip with the
  observable keying, by exact matched computation); genesis
  persistence is factorization-only (6/6 disjoint-contact tensor
  factors); the same contact fired twice mints the census's only
  beyond-grammar support.
- Evidence: shipped — `permission_map.py`.

## FCT-163 — The Single-Event Completeness Theorem (Chapter 38)

- Status: `THEOREM / CENSUS-EXHAUSTIVE` (the v0.24 sweeps' located
  novelty pocket, closed; structural all-sizes proof open by name;
  the authors' own break-it bet died in print)
- Public statement: every single-event faithful support on the
  extended heterogeneous arena is exactly affine-cap-weight-union
  (24/24; orbit lemma verified) — one interaction can only
  correlate by parity and cardinality.
- Evidence: shipped — `grammar_closure.py`.

## FCT-164 — The Exit Law + the Absolute Factorization Theorem (Chapter 38)

- Status: `THEOREM / CENSUS-SCOPE` (temporal-correlations genre,
  Anders-Browne adaptivity threshold, and tensor-locality recovery
  cited; carrier uniqueness is the claim)
- Public statement: grammar exits are sequential dependence with
  per-floor carriers (conserving: repetition only — non-repeat
  never exits; mortal: shared neighbors only — repetition
  impossible); disjoint-neighborhood pairs on the conserving floor
  factorize exactly in support and amplitude (12/12).
- Evidence: shipped — `sequence_grammar.py`.

## FCT-165 — Mortal Holism (Chapter 38)

- Status: `THEOREM-GRADE CONTRAST / MODEL SCOPE`
  (constraint-induced correlations, ensemble nonequivalence,
  superselection, and collider-conditioning cited as neighbors per
  the release's blind sweep; the claim is the exact dichotomy with
  the anti-deflation contrast in print; measure-vs-record control
  and record-choice quantifier named open; slogan flagged)
- Public statement: on the mortal floor support factorization
  fails at every tested separation (0/52) — a shrinking world's
  class record is global, so even the record couples spacelike
  events; conservation buys separability itself (five loads on
  one switch).
- Evidence: shipped — `sequence_grammar.py`.

## FCT-166 — The Free Shape-Lock + the Evasion Found (Chapter 39)

- Status: `THEOREM / CENSUS-SCOPE` (context preservation cited;
  the authors' blindness bet died in print)
- Public statement: external merges never touch a mortal body's
  interior (75/75; the observer pays in lifespan, not
  admissibility); 4 of 32 revealable bright body-reading channels
  persist a reveal — the evasion proven absent on the conserving
  floor exists on the mortal one; blindness is floor x
  instrument.
- Evidence: shipped — `mortal_observer.py`.

## FCT-167 — The Finite Biography (Chapter 39)

- Status: `THEOREM / CENSUS-SCOPE` (grammar-under-instrument
  inconclusive, named open)
- Public statement: every mortal observer reads a terminating
  text (depth bound = population); zero dark channels in reading
  keying; the mortal refusal analog shipped.
- Evidence: shipped — `mortal_observer.py`.

## FCT-168 — Priced Rest (Chapter 40)

- Status: `THEOREM / CENSUS-SCOPE` (thermodynamics-of-computation
  lineage held at arm's length by name; not Landauer)
- Public statement: on the breathing floor class-preserving
  returns exist and every return pays at least the fission
  surcharge (131 returns; min price exactly 1) — the survival
  row's third value: free / impossible / priced.
- Evidence: shipped — `breathing_floor.py`.

## FCT-169 — The Switch Decomposition + the Locality Gradient (Chapter 40)

- Status: `THEOREM-GRADE CONTRAST / THREE-FLOOR CENSUS SCOPE`
  (path reconvergence cited as elementary, Lam-Leung vanishing
  sums classical; holism bet corrected in print; shared-budget
  confound named open with the coalescent adjacency cited)
- Public statement: exact preclusion follows RETURN (dark:
  112/0/196 across conserving/mortal/breathing); separability
  follows CONSERVATION graded by net drift (12/12, 201/215,
  0/52); indifference follows conservation; the assignment table
  closes at three-floor scope.
- Evidence: shipped — `breathing_floor.py`, `two_switches.py`.

## FCT-170 — The Three-Floor Sign-Table Invariant (Chapter 40)

- Status: `THEOREM / THREE-FLOOR CENSUS SCOPE`
- Public statement: the NSIT sign table (zero / never-positive /
  both-signs) is identical on all three floors — the Chapter-35
  registered package's bridge target widens to any substrate
  realizing any of the three dynamics.
- Evidence: shipped — `two_switches.py`.

## FCT-171 — The Phase-Flat Fork (Chapter 41)

- Status: `THEOREM / CENSUS-SCOPE, SCOPED TO DERIVED-PHASE
  CONSTRUCTIONS` (the parentless bet died in print; postulated-
  phase QM explicitly out of scope)
- Public statement: every tie-jump branch sits at equal minimal
  Hamming distance by the no-selector rule itself, so the
  deviation-derived coat is constructible but inert —
  interference in this construction requires unequal deviation
  (a parent); the no-selector law is native on all four floors,
  the coat only on parented ones.
- Evidence: shipped — `fourth_column.py`.

## FCT-172 — Idle Rest + the Local Monotone Floor (Chapter 41)

- Status: `THEOREM / CENSUS-SCOPE` (Birkhoff/Apt idempotence
  cited; lumpability + observable-induced locality +
  partial-balance triangulation cited; one arena discarded in
  print as an instrument lesson)
- Public statement: the subtractive floor rests free but
  inconsequentially (survival row: free/impossible/priced/idle)
  and is LOCAL at tested scope though monotone — record locality
  follows the record's decomposability.
- Evidence: shipped — `fourth_column.py`.

## FCT-173 — The Controls, Run (Chapter 41; closes the named-open
## confounds of FCT-165 and FCT-169)

- Status: `THEOREM / CONTROL RUNS COMPLETE`
- Public statement: measure control — population-independent
  weights make mortal amplitude factorization equal support
  factorization exactly; record control — local-record keying
  restores locality with the collider exhibited (one global
  class fuses 2/4 local pairs); mortal holism final form — the
  mortal global class is irreducible to local classes (0/12),
  the conserving one decomposes (12/12).
- Evidence: shipped — `controls_engine.py`.

## FCT-174 — The Moving Parent (Chapter 42)

- Status: `THEOREM / PROTOCOL-CENSUS SCOPE, DERIVED-PHASE
  SCOPED` (impossible first witness corrected in print;
  relational reference-frame adjacencies cited)
- Public statement: 335/795 multi-path subtractive channels mix
  accumulated deviation; the reference for every jump is the
  current point — derived phase needs a persistent REFERENCE,
  not per-fork inequality.
- Evidence: shipped — `phase_hunt.py`.

## FCT-175 — The Return Law, Four Floors (Chapter 42)

- Status: `ENUMERATION / FOUR-DYNAMICS SCOPE`
- Public statement: darkness tracks return with no exceptions —
  112 / 0 / 196 / 0 against returns free / no / priced / no.
- Evidence: shipped — `phase_hunt.py`.

## FCT-176 — The Four-Floor Sign Invariant (Chapter 42)

- Status: `THEOREM / FOUR-DYNAMICS CENSUS SCOPE`
- Public statement: the NSIT sign table (zero / never-positive /
  both-signs) holds on the subtractive floor exactly (linear
  0+/746-, squared 992+/56-); the registered actuality package's
  bridge target is any of four dynamics; the subtractive floor
  is fully reveal-fragile (460/460).
- Evidence: shipped — `fourth_table.py`.

## FCT-177 — The Critical Corner (Chapter 43)

- Status: `THEOREM / CENSUS-SCOPE, PREDICTIONS FROZEN IN
  ADVANCE` (two instrument lessons recorded)
- Public statement: (conserving, return-free) is realizable (the
  root floor); zero dark on a live mixed-phase uniform-weight
  census; amplitude factorization == support factorization (the
  conserving signature) — both called before the run.
- Evidence: shipped — `critical_corner.py`.

## FCT-178 — The Minimal Pair + the Sterility Lemma (Chapter 43)

- Status: `THEOREM + METHOD / CENSUS-SCOPE` (sterility lemma at
  full proof strength, attributed to gauge-triviality; the
  method = foil-theory/axiom-knockout lineage named, the
  census-resolved dynamics variant the only claimed sliver)
- Public statement: parentless-with-returns is sterile (uniform
  pricing forces phase purity); one zero-cost stay branch
  switches exact preclusion on at the same arena (1 dark, 25
  partial; the checkerboard mechanism).
- Evidence: shipped — `minimal_pair.py`.

## FCT-179 — The Two-Factor Theorem of Preclusion (Chapter 43)

- Status: `ENUMERATION + LEMMA / SEVEN-FLOOR SCOPE` (Masanes-
  Muller reversibility and Meyer's coinless no-go the nearest
  frames, the Meyer difference stated preemptively; the
  conjunction and its construction-level isolation the claim)
- Public statement: exact darkness requires RETURNS and A
  PARENT; dark 112/0/196/0/0/0/1 tracks the conjunction with no
  exceptions across seven rival dynamics.
- Evidence: shipped — `critical_corner.py`, `minimal_pair.py`.

## FCT-180 — The Kept-World Theorem (Chapter 44)

- Status: `THEOREM / SEVEN-FLOOR CENSUS SCOPE` (decisive cells
  live-checked; anthropic literature engaged at register level)
- Public statement: every observer-hostable floor has returns
  AND locality; the mortal, subtractive, and root floors host no
  unbounded observer — keeping is anthropic.
- Evidence: shipped — `observer_corner.py`.

## FCT-181 — The Sterile Observer (Chapter 44)

- Status: `THEOREM / CENSUS SCOPE, REGISTERED MISS SCORED`
  (the classical-worlds deflation pre-stated; structural not
  physical separation)
- Public statement: a provably interference-free recurrent
  dynamics passes every observer predicate — interference is not
  anthropically forced; phase is received, and the quantum
  dividend (what phase buys an observer) is named open.
- Evidence: shipped — `observer_corner.py`.

## FCT-182 — The Quantum Dividend (Chapter 45)

- Status: `THEOREM + METHOD / MINIMAL-PAIR CENSUS SCOPE`
  (both priced faculties are recoveries — Sorkin preclusion /
  Elitzur-Vaidman and the Sorkin/NSIT hierarchy — the
  ablation-on-dynamics method with observers embodied on both
  sides the contribution; Spekkens ancestor and Zurek inverted
  cousin cited)
- Public statement: interference is worth two faculties to an
  observer — certainty of absence and the destructive sector —
  each localized to one structural bit; everything else free on
  both floors. Phase is received (Ch44) and priced.
- Evidence: shipped — `quantum_dividend.py`.

## FCT-183 — The Mirror Law + Coat-Generic Discriminator (Chapter 46)

- Status: `THEOREM / CENSUS-SCOPE` (phase-group genericity a
  robustness fact; the mirror law the Lam-Leung obstruction
  instantiated, cited; the even/odd-d dichotomy the
  framing-inverted neighbor)
- Public statement: the interference algebra and the NSIT
  discriminator are phase-group generic (coat-generic
  invariance); exact preclusion requires even phase-group order
  (an order-2 mirror), a Z3 coat minting zero dark where Z4
  mints dark.
- Evidence: shipped — `third_root.py`.

## FCT-184 — The Resonance Condition + Coat Spectroscopy (Chapter 47)

- Status: `THEOREM / CENSUS-SCOPE, PRIORITY CLAIM HEDGED`
  (resonance hedged against commensurability — Harper-Hofstadter
  — and the specialness-of-i literature; spectroscopy a
  possibilistic self-test; the spectrometer reading a synthesis
  of cited instruments)
- Public statement: even order is necessary but not sufficient —
  the mirror must sit at the return cost; a world's phase-group
  order and its return structure are one fact, i the unique coat
  resonant with pair-return dynamics; the phase-group order is
  readable from the support of forbidden events alone.
- Evidence: shipped — `sixfold_coat.py`.

## FCT-185 — The Arithmetic of Refusal (Chapter 48)

- Status: `ELEMENTARY + CONJECTURE / GRID SCOPE` (the opposition
  criterion the elementary linear-congruence result; the
  half-turn forward direction 1+zeta^c=0; the converse a
  grid-verified conjecture in Lam-Leung territory, NOT a proven
  iff; the organizing reading the contribution, on the
  explanatory axis)
- Public statement: opposition exists iff gcd(c,n) | n/2; exact
  refusal first appears at depth c where 1 + zeta_n^c = 0, i.e.
  the return is a half-turn of the coat; refusal-only-in-half-
  turn-worlds is verified on the grid to depth 12 and conjectured
  in general; return-as-negation organizes the refusal story and
  recovers the one-use founding intuition.
- Evidence: shipped — `harmonic_law.py`, `octave_law.py`.

## FCT-186 — The r_F Boundary Theorem (Chapter 49)

- Status: `THEOREM / EXACT, UNCONDITIONAL, FULL 3x3 MATRIX SCOPE`
  (independently reproduced by three routes; two adversarial
  statement breaks scored in print as corrections)
- Public statement: Q_r = (1 + r³)N + (1/2)r²(1 − r)P −
  (1/2)r²(1 + r)D ≥ 0 for every complex 3×3 matrix iff
  −1 ≤ r ≤ r_F, the positive root of 4r³ + 3r² − 2
  (0.6070073…); boundary equality = N = 0 or the full toric
  Fourier orbit. The certified odd window (1/2, r_F] becomes
  theorem, superseding the per-campaign positivity patchwork.
- Evidence: shipped — `rf_boundary.py`.

## FCT-187 — The Sharp Constant and the Unitary Companion (Chapter 49)

- Status: `THEOREM / EXACT, SHARP, EQUALITY ORBITS CHARACTERIZED`
  (33N + 2P ≥ 8 adjudicated by direct reading against the
  bunching-witness genre: new sharp matrix inequality within a
  cited genre, not a reparametrization)
- Public statement: D ≤ (9/2)N with equality exactly on the
  Fourier orbit (replacing Cauchy–Schwarz D ≤ 6N); for every
  3×3 unitary, 33N + 2P ≥ 8, saturated at the Fourier point.
- Evidence: shipped — `rf_boundary.py`.

## FCT-188 — The BRACKET-1 Registration (Chapter 50)

- Status: `FROZEN PREREGISTRATION / EXPERIMENT-OPEN` (mechanism adapted
  from the published fusion-order technique, cited; thresholds are
  named pre-acquisition placeholders; killed premise scored in print)
- Public statement: a fully discrete preregistered test of whether a
  natural odd sector composes strictly (Ω ≡ +1) or twisted
  (Ω = (−1)^(q₁q₂q₃), −1 on (1,1,1) alone — the unique discriminator),
  with frozen decision rules, charge certificate, and exact-binomial
  statistics template.
- Evidence: the frozen tables and templates in the chapter (hand-checkable).

## FCT-189 — The Contact-Faithful Causal Ceiling (Chapter 51)

- Status: `REGISTERED PREDICTION / EXPERIMENT-OPEN` (recovery labels on
  the family and the floor location; eight named conditions)
- Public statement: under eight named conditions (acyclic background,
  definite typed source base, contact-faithful acting once, local order,
  fine-grained closed laboratories, free settings, lift preservation, no
  postselection) the d-ary order game obeys p_d ≤ (d+1)/(2d); kill
  conditions exact; the decisive experiment is a certified one-message
  realization that violates.
- Evidence: shipped — `causal_ceiling_family.py`.

## FCT-190 — The d = 3 Violation, Certified (Chapter 51)

- Status: `TWO-ENGINE-CERTIFIED LOWER BOUND / OPTIMALITY OPEN`
  (phenomenon recovery-class; the number adjudicated plausibly new)
- Public statement: the process formalism exceeds the d = 3 ceiling 2/3:
  see-saw value 0.738466; certified lower bound 0.738443 via an
  independently coded second engine on a rigorously positive repaired
  certificate; the violation SHRINKS from d = 2 (margin 0.104) to d = 3
  (margin 0.072) — existence, never growth.
- Evidence: shipped — `causal_ceiling_family.py` (the d = 2 exact gate).

## FCT-191 — The Amalgamation Boundary (Chapter 52)

- Status: `SYNTHESIS / EXPLANATORY` (every boundary component cited to
  its incumbent; claims confined to the per-rung extension, provenance
  typing, and the elliptope-margin presentation)
- Public statement: one amalgamation law per rung buys that rung's
  coherent value; the tritter observable W = P111 + D2 − 2/3 =
  (2/9) det G is an elliptope-margin (realizability) functional — the
  triangle is chordal; genuine completion obstruction starts at the
  4-cycle and is exhibited exactly.
- Evidence: shipped — `amalgamation_boundary.py`.

## FCT-192 — The Admission Forks (Chapter 53)

- Status: `THEOREM / FORCING-UNDERDETERMINATION PROVED; DISPOSITIONAL BETH
  FOR CELLS ONLY` (explicit finite model pair shipped as an exhaustive check)
- Public statement: the floor stages the admission interval for cells and for
  processes and selects no value; implicit undefinability is earned only for
  dispositional cell admissibility; standard quantum theory takes the maximal
  value at both forks.
- Evidence: shipped — `admission_forks.py`.

## FCT-193 — Eligibility and the Erasure Exclusion (Chapter 53)

- Status: `THEOREM AT STATE SCOPE / CONDITIONAL AT PROCESS SCOPE (H_C)`
  (recovery label on the erasure line: reversible-computing conservation)
- Public statement: at finite receipt-individuated scope, a state event is
  ancillary-stable with total receipt compatibility iff it books zero unpaid
  distinction debt; unreceipted erasure is excluded at merge-audit scope;
  the map-level equivalence is conditional on named audit completeness.
- Evidence: shipped — `admission_forks.py`.

## FCT-194 — The Obstruction Spectrum and the One-Hot Atlas (Chapter 54)

- Status: `THEOREMS AT STATED SCOPES / SIXTH OBSTRUCTION = EXISTENCE WITNESS`
  (receipt-inclusive extensionality derived canonical)
- Public statement: five pairwise-independent obstruction axes; the one-hot
  atlas has five sectors at receipt-inclusive scope and four in the visible
  shadow; realization amalgamation is a genuine further obstruction whose
  computable criterion is open.
- Evidence: shipped — `obstruction_spectrum.py`.

## FCT-195 — The Irreducible-Face Purity Theorem (Chapter 55)

- Status: `THEOREM (FINITE SCOPE, H_A) / REDUCIBLE VERDICT UNDER RIA`
  (anti-classical statement labeled RECOVERY of purification/steering)
- Public statement: minimal universal payments steer faithfully; over an
  irreducible face every minimal payment is pure unconditionally; over
  reducible faces the broadcast payment exists and is excluded exactly by
  the receipt-individuation ledger; classically, universal recovery and
  purity are jointly unsatisfiable where quantum theory realizes both.
- Evidence: shipped — `carrier_anatomy.py`.

## FCT-196 — Two-Step Complex Selection and the Balanced Sector (Chapter 55)

- Status: `THEOREM AT REGISTERED SCOPE / PREMISE PLAUSIBLY NEW`
  (orientation-universality derivation of the balancing)
- Public statement: the skeleton is real-quaternionic by the registered
  ternary-scar package; complex structure requires the received arrow and a
  received polarization with no natural selector; one world arrow fixes the
  composite balancing sector.
- Evidence: shipped — `carrier_anatomy.py`.

## FCT-197 — The Fixed Point and the Five Receipts (Chapter 56)

- Status: `ASSEMBLY / CLOSING CLAIM DELIBERATELY BOUNDED`
- Public statement: thirteen floor-derived theorems, five received world
  inputs, one provenance axiom, one closure condition, two admission
  policies; the maximal one-phase collapsed branch recovers the finite
  complex quantum architecture; nearest departures: carried twisted
  genealogy and routing-open admission.
- Evidence: shipped — `fixed_point_registry.py`.

## FCT-198 — The Ledger Theorems (Chapter 57)

- Status: `THEOREMS AT GLOBAL-LINEAGE / RECEIPT-COMPLETE SCOPE; STRONG
  CONSERVATION READING KILLED IN PRINT` (antecedents named: no-deleting
  lineage, causal-set growth arrow, Landauer–Bennett)
- Public statement: receipt-lineage persistence is the conservation
  theorem; the H-theorem and arrow are pathwise and probability-free;
  floor-level fluctuation symmetry is impossible by support; the physical
  Landauer bound is conditional on a named thermal receiver.
- Evidence: shipped — `ledger_books.py`.

## FCT-199 — The Representation Identity and the Receiver Anatomy (Chapter 57)

- Status: `THEOREM ON THE RECEIPT-COMPLETE CLASS / NONDEFINABILITY PROVED`
  (identity's antecedent: the maximum-entropy decomposition)
- Public statement: thermodynamic entropy over k_B equals receipt capacity
  minus structural slack minus bias slack, exactly, on the receipt-complete
  class, and is incomparable outside it; a universal Landauer receiver must
  be completely passive and Gibbs on exchange-complete sectors; the floor
  cannot define a nontrivial calibrated receiver.
- Evidence: shipped — `ledger_books.py`.

## FCT-200 — The Moduli and the No-Native-Selector Theorem (Chapter 58)

- Status: `THEOREMS PER TIER; EVERY ADDED AXIOM SHOWN EXTERNAL`
  (Erdős characterization credited; Padoa method named)
- Public statement: valuations are free at floor scope, a prime-weight cone
  under additivity, logarithmic only under monotonicity; no canonical
  selector is constructible from the frame; natural chance is uniform
  within orbits and free across them; the quadratic weighting is exactly
  as forced as Pythagorean carrier composition.
- Evidence: shipped — `valuation_prices.py`.

## FCT-201 — The Fork Trichotomy and the Received Vocabulary (Chapter 59)

- Status: `THEOREMS AT RELATIVE-STAGING STRENGTH; TWO SELF-CORRECTIONS
  PRINTED` (torsor/classification distinction's ancestry cited)
- Public statement: staged forks difference by their own symmetry, by an
  external module without a zero, or by one with a zero; must-receive data
  have no equivariant default, may-receive data have an unforced one; a
  floor-native unpointed splitting torsor exists and its calibration bit is
  an aperture-conditional candidate receipt.
- Evidence: shipped — `spine_forks.py`.

## FCT-202 — The Worlds Above the Pentagon (Chapter 60)

- Status: `KNOWN-MATHEMATICS RECOVERY WITH REGISTERED-PREDICTION SCORECARD;
  COEFFICIENT RELATIVITY STATED` (semion, no-programming, and
  Dijkgraaf–Witten antecedents named)
- Public statement: the third-degree twisted completion is the semion
  category; the fourth-degree world lives on the composite group over
  divisible coefficients with three-loop statistics; over the floor's
  native finite coefficients a single binary grading carries a
  fourth-degree class that admitting the imaginary unit erases.
- Evidence: shipped — `twisted_worlds.py`.

## FCT-203 — The Price of the Square and the One Import (Chapter 61)

- Status: `NON-SELECTION THEOREMS EXACT; BRIDGE AUDIT CLEAN; THESIS =
  ORGANIZATION WITH REGISTERED PROVENANCE` (Jordan–von Neumann and the
  ℓ_p deformation literature credited)
- Public statement: interference worlds exist for every exponent and the
  phase layer cannot select one; the reconstruction axioms are imports by
  explicit countermodel pairs; retention earns the metric given linearity,
  and linearity itself is received; capacity supplies no natural ruler;
  four independent audits localize the standard completion's imports in
  one place — the continuum.
- Evidence: shipped — `born_price.py`.

## FCT-204 — Tolerance Is Equality Minus Free Transitivity (Chapter 62)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: the posited floor relation (reflexive, symmetric,
  non-transitive) is derived in one line from the root R — identity/use is an
  event, never free. Reflexivity is paid re-identification; symmetry is a single
  undirected mark; non-transitivity is the whole content of R, since the third
  identification is never free.
- Evidence: proof (§1.2), not a computation.

## FCT-205 — One-Use Realizability Is Bipartiteness (Chapter 62)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: one-use exactly-once realizability equals proper
  2-colorability equals bipartiteness; the joint-outcome count is `N = 2^c`
  (c = components) when bipartite and 0 otherwise. Two independent engines agree
  on every topology: single edge 2, odd cycles 0, even cycles 2.
- Evidence: shipped — `verify_62_equality.py` part (a).

## FCT-206 — The Three-Object Taxonomy, With Its Correction (Chapter 62)

- Status: `THEOREM / MODEL-SCOPE` (carries an attached WITHDRAWN on the earlier
  (B)=(C) identification, caught by three external reviewers)
- Public statement: the taxonomy splits into (A) pairwise-complete with a
  positive global section (classical), (B) an odd 2-coloring cycle,
  pairwise-constrained and frustrated, and (C) Peres–Mermin, pairwise-complete
  with no global section (contextual); (B) ≠ (C). An early count conflated (B)
  and (C); the conflation is withdrawn and re-derived in print.
- Evidence: shipped — `verify_62_equality.py` part (b).

## FCT-207 — Odd-Cycle Contextuality From Two Conservation Laws Is Recovery (Chapter 62)

- Status: `RECOVERY` (Kochen–Specker / CSW class; adjudicable against CSW —
  they posit exclusivity, the floor derives 2-coloring from two conservation
  laws; no new empirical door)
- Public statement: odd-cycle contextuality derived from one-use L1+L2 as a
  counting fact is a recovery, not a divergence; the sign that would upgrade (B)
  frustration to (C) contextuality is not present in unordered exclusion
  counting — flagged OPEN.
- Evidence: shipped — `verify_62_equality.py` part (b).

## FCT-208 — The Exact Twins (Chapter 62)

- Status: `THEOREM / MODEL-SCOPE` (depth 2)
- Public statement: the depth-2 genesis reading-law table is rank-deficient for
  C5 (4/5) and C6 (5/6) with `det G = 0` exactly, the null being one class minus
  another with coefficients ±1; C4 is full rank. Recomputed from a self-contained
  successor engine in exact Gaussian-rational arithmetic, not hardcoded.
- Evidence: shipped — `verify_62_equality.py` part (c).

## FCT-209 — Coat-Universality of the Twins (Chapter 62)

- Status: `THEOREM / MODEL-SCOPE` (depth 2)
- Public statement: no deviation-dependent reading law can split or
  oppositely-sign the depth-2 twins — they carry identical channel data, making
  the central "stages a fork, forces no selector" law exact at this scope.
  Scope-fenced: depth-3 splitting is OPEN; this is twin degeneracy, not
  many-class cancellation, and no divergence is claimed.
- Evidence: proof (§4.2) + `verify_62_equality.py` part (c).

## FCT-210 — The Identity Root R+F as the Proposed Bottom (Chapter 62)

- Status: `EXTENSION / SYNTHESIS` (above model; not asserted forced)
- Public statement: the identity root R+F ("identity/use is an event";
  finitude), adjudicated against the classical free/transitive/reusable-identity
  assumption, explains more with fewer posits — it derives tolerance, marks,
  one-use, receipts, and presheaf-not-sheaf. The case for it is the set of
  retired posits, not a forcing claim.
- Evidence: argument by derivation (§1).

## FCT-211 — The Falsifiability-Line Process Rule (Chapter 62)

- Status: `METHOD` (standing rule)
- Public statement: no instrument ships without a written line stating what
  result would falsify it; an A·A† construction cannot test positivity (PSD by
  construction), only rank. The rule is born from the withdrawn floor-run, a
  public-record credibility asset.
- Evidence: born from the WITHDRAWN floor-run (§5).

## FCT-212 — The Closure Charge Is a Z₂ Holonomy Class (Chapter 63)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: on unit-diagonal 3×3 overlap Grams, χ = sign(g₁₂g₂₃g₃₁) is
  invariant under all eight local rephasings ψ_i ↦ −ψ_i, so it is the Z₂
  odd-cycle holonomy class in H¹(C₃; Z₂), not a sign convention. A charge of −1
  means the loop's frustration is not trivializable by any local phase choice.
- Evidence: shipped — `verify_63_charge.py` (check A1).

## FCT-213 — The Exact Visibility Wall (Chapter 63)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: on the canonical equal-magnitude odd triad,
  det G_odd(v) = −(2v−1)(v+1)²; since (v+1)² > 0, the Gram fails
  positive-semidefiniteness (no common carrier) precisely when v > 1/2. The
  charge is carried at every v; it becomes Gram-visible only above the exact
  wall v = 1/2.
- Evidence: shipped — `verify_63_charge.py` (check A2).

## FCT-214 — The Closure-Charge Fusion Algebra (Chapter 63)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: under Hadamard fusion χ is multiplicative
  (χ(A#B) = χ(A)χ(B)); odd # odd cancels exactly to a positive-semidefinite
  triad with det = (1−v)²(1+2v); the charge screens below v = 1/2 and survives
  fusion with a strong even triad; under Kronecker fusion the visibility sign
  obeys det(A⊗B) = det(A)³det(B)³. Six exact model-theorems; the composition law
  is the un-forgeable signature but does not force the charge into nature.
- Evidence: shipped — `verify_63_charge.py` (checks A3–A6).

## FCT-215 — The Tritter Amalgamability Boundary (Chapter 63)

- Status: `RECOVERY / MODEL-SCOPE`
- Public statement: a three-photon internal-state triad amalgamates (one common
  carrier) iff cos θ ≥ (3v²−1)/(2v³), i.e. θ ≤ θ_max(v) = arccos[(3v²−1)/(2v³)],
  with θ_max = 180° at v = 1/2 and 90° at v = 1/√3, exact — a one-line corollary
  of Gram positivity giving the measured triad phase (Menssen et al. 2017) its
  amalgamability meaning. Presentational; every real photonic triad sits inside
  the boundary.
- Evidence: shipped — `verify_63_charge.py` (checks B1–B2).

## FCT-216 — Photons Always Glue; the Charge in Nature Is a Registered Wager (Chapter 63)

- Status: `EXTENSION / REGISTERED / CONDITIONAL` (zero observations)
- Public statement: real photonic states have positive-semidefinite Grams, so no
  photonic experiment can exhibit the charge above the wall; the wager needs a
  non-photonic realization producing a non-amalgamable triad, and the floor does
  not force χ = −1. Below the wall the fusion algebra is QM-legal and
  photonically testable now. Kill condition: abandon if every accessible triad
  amalgamates, every triadic anomaly reduces to context/postselection/classical
  correlation, and no compositional conservation law exists.
- Evidence: cited — private spine record; zero observations.

## FCT-217 — The Spoof-Closed Witness Designs (Chapter 63)

- Status: `REGISTERED / PROTOCOL`
- Public statement: a global route-phase is invisible to complete process
  tomography yet flips an interferometer (Oi 2003), so the naive assembly-sign
  experiment is forgeable and is KILLED; two rebuilds close the spoof — (i) the
  commutator witness, where a scalar route-phase cancels identically in
  U V U⁻¹ V⁻¹ while genuine noncommuting transport yields −I; (ii) the
  sector-conditional correlator K = ⟨sign · χ_A χ_B⟩, which any fixed
  tomographically-invisible spoof drives to K ≈ 0 while genuine charge-transport
  gives K = 1 (exact-sector null sign ≡ +1, K = 0). The floor does not force
  K = 1; protocols registered, not run.
- Evidence: shipped — `verify_63_charge.py` (checks C1–C2); protocol cited.

## FCT-218 — The Two-Sector Split of the n=4 World (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: the n=4 (tetrahedral) world splits into a complementary
  pair — the exact sector `Π_glue = δχ`, fixed by the four triadic face charges
  and reachable by pair-identity gluing (no assignment over all 4096 gives
  `Π_glue = −1`), and the non-exact sector, the essential (faceless) generator
  of `H⁴(Z2; Z2)`, trivial on every proper face and reachable from no rung-3
  data. No pair-identity assignment crosses between the sectors. HKR detection is
  OPEN; "faceless" = standard essential cohomology (Adem–Karagueuzian; Green).
- Evidence: shipped — `verify_64_tower.py` (enumeration + generator).

## FCT-219 — The Half-Eraser (Top-Cell Lemma) (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE`
- Public statement: a normalized (n−1)-cochain on Z2 has one free value λ, and
  its coboundary on the all-ones top cell is `λ(1 + (−1)ⁿ)`; adjoining i (mod-4
  coefficients) trivializes exactly the even rungs and can never touch the odd
  rungs, while over the sign group no rung is ever a coboundary. Recovers
  `H^n(Z2; U(1)) = Z2` (odd) / 0 (even) as a constructive which-half-and-why
  statement, verified rungs 2–5 mod 2 and mod 4.
- Evidence: shipped — `verify_64_tower.py`.

## FCT-220 — The Law Tower Is F2[x] on One Generator (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Public statement: every charge and law of the program is a power of the single
  generator x (the elementary distinction) — x² the mirror, x³ the triadic
  charge, x⁴ the essential pentagonator — and the tower is the classical ring
  F2[x], with exponent parity the appearance/law grading and multiplication by x
  the parity shift. The ring is classical (RECOVERY); its identification as this
  floor's exact charge tower is the model content. Cup identities all enumerated
  and confirmed.
- Evidence: shipped — `verify_64_tower.py` (cup identities).

## FCT-221 — The Stage/Receipt Weyl Calculus and the Four-Way Law Sector (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE` (+ `RECOVERY` for the operator centre)
- Public statement: with U = multiply-by-x (stage) and D = formal derivative
  (receipt) on F2[x], `DU + UD = 1` (the characteristic-two Weyl relation) and
  the law sector F2[x²] has four coinciding descriptions —
  `ker D = im D = im Frobenius = Z(⟨U,D⟩)`, the centre of M₂(F2[x²]). The
  coincidence ker D = im D is exact at p = 2 only (fails at p = 3), a structural
  reason distinction is binary; the complex is contractible, witnessed by
  `x⁴ = D(x⁵)`. The operator-centre / Frobenius-descent tower is classical
  (Revoy 1973; Bezrukavnikov–Mirković–Rumynin).
- Evidence: shipped — `verify_64_tower.py`.

## FCT-222 — The Bockstein Carry Theorem: Laws Are Doubly Sourced (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE` (assembly = expository unification)
- Public statement: the carry of the signed differential over the mod-2
  differential, divided by two, is the classical Bockstein β, with
  β(xⁿ) = x^{n+1} for odd n and 0 for even n; combined with vertical exactness
  (`x^{2k} = D(x^{2k+1})`), every law is generated twice — as the carry of the
  appearance below it (β) and the receipt shadow of the appearance above it (D) —
  the double-generation pincer, with the compact identity β = x²·D on the tower.
  The mirror is additionally the extension class of Z/4 and the first Witt carry
  polynomial (W₂(F2) = Z/4). Constituents classical; the packaging is an
  expository unification; no divergence claimed.
- Evidence: shipped — `verify_64_tower.py`.

## FCT-223 — The Carry-Depth Filtration (Chapter 64)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY`
- Public statement: `ν₂(Catalan(n−1)) = s₂(n) − 1` — the program's binomial and
  Catalan parity mechanisms are the same carry counter (Kummer 1852;
  Alter–Kubota 1973; Deutsch–Sagan 2006). The dyadic essential classes are the
  zero-carry stratum; arity is a simplex of scales, carry depth its dimension,
  dimension d first visible at sign-resolution 2^{d+1}. At mod 4 the two-lift
  fork acts on the filtration and the identity-installation projector K fails by
  an exact 2-torsion term. Number theory classical (RECOVERY); the
  resolution-ladder physical reading is CONJ/registered and lives in Chapter 67.
- Evidence: shipped — `verify_64_tower.py` (n = 2..40).

## FCT-224 — The Pentagon Evaluates the Coboundary; the Essential Class Is Non-Local (Chapter 65)

- Status: `THEOREM / MODEL-SCOPE` (the pentagon↔coboundary correspondence is
  RECOVERY: Mac Lane 1963; EGNO 2015; essential-class term Adem–Karagueuzian /
  Green)
- Public statement: the five-edge loop-sign of the K4 pentagon equals the
  group-cohomology coboundary δf of the grading-local reassociation rule for all
  256 rules × 16 sectors; 0/256 local μ2 rules reproduce the essential degree-4
  class ω4 = abcd (it must be prescribed, not generated by any local move); the
  μ4 i-dressing has δg = 2·ω4 (mod 4), flattening the pentagon holonomy to gauge.
  Facelessness = there is no cycle in assembly space below four leaves. The HKR
  reading of ω4 is OPEN; the physical "why i" is a fenced EXTENSION.
- Evidence: shipped — `verify_65_e8.py` (checks a1–a3 + Tamari census).

## FCT-225 — Q8 and the 24-Cell Forced From the Anticommuting Lift (Chapter 65)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (2T = Hurwitz units = 24-cell is
  classical: Coxeter; Conway–Smith 2003; the derivation and exact minimality are
  the model claim)
- Public statement: the anticommuting lift of the essential sign generates Q8;
  adjoining the triadic 3-cycle ω closes to exactly the 24 Hurwitz units =
  2T = Q8 ⋊ C3 = the 24-cell, minimal (index 3 is prime and each of the 16 units
  outside Q8 regenerates all 24). The rotor τ = (1+i+j+k)/2 is a cube root of the
  pentagon sign inside the derived receiver (τ² = ω, τ³ = −1, τ⁶ = 1).
- Evidence: shipped — `verify_65_e8.py` (checks b1–b9, exact quaternion
  arithmetic over Q).

## FCT-226 — The Chirality Gate: F4 Is the Gate, the 24-Cell Is the Received Half (Chapter 65)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (the 2O/48 = F4 split is classical:
  Conway–Smith 2003; Coxeter)
- Public statement: admitting the reflective triad symmetry forces the binary
  octahedral group 2O (48 units) = the F4 root system (24 long + 24 short) = the
  24-cell plus its dual; the program's received chirality selects 24 over 48. F4
  is the pre-reception gate; the 24-cell is the received (orientation-preserving)
  half. The chirality datum is RECEIVED — enumerated, not forced.
- Evidence: shipped — `verify_65_e8.py` (check b10).

## FCT-227 — E8 Forced by Evenness + Self-Duality From Two 24-Cells (Chapter 65)

- Status: `THEOREM / MODEL-SCOPE / RECOVERY` (D4 glue theory, discriminant forms,
  and even-unimodular-in-dim-8 uniqueness classical: Conway–Sloane SPLAG;
  Nikulin 1979)
- Public statement: over the discriminant group (D4*/D4)² = (Z/2)⁴ all 35
  order-4 glues enumerate; exactly 6 are even (the mirror excludes the 29
  one-sided glues); all 6 are graphs of automorphisms = S3 = triality; each
  yields 240 roots = E8, the unique even unimodular lattice in dimension 8
  (240 = 24 + 24 + 64 + 128). The residual glue choice is a received triality
  element. NOT the McKay correspondence (firewall §5): McKay sends 2T → E6 and
  E8 → binary icosahedral; this is D4+D4 glue-forcing, and distinct from the
  Kitaev-type E8 state.
- Evidence: shipped — `verify_65_e8.py` (checks c1–c4, d1–d4).

## FCT-228 — The Derived Receiver Is the Strict Height-2 Stabilizer's Maximal Finite Subgroup (Chapter 65)

- Status: `CITED / OBSERVATION` (Hewett 1995; Bujard 2012;
  Beaudry–Bobkova–Goerss–Henn and collaborators; NOT a program result)
- Public statement: 2T = Q8 ⋊ C3, the group the essential class forces as the
  receiver, is the maximal finite subgroup of the strict height-2 Morava
  stabilizer group at p = 2; the extended (Galois) stabilizer's maximal finite
  subgroup has order 48, and the strict-vs-Galois split aligns with the
  chirality gate's 24-vs-48 — labeled an observation. The chromatic frame is
  carried at CITED/CONJ in Chapter 67.
- Evidence: literature (cited); the group itself shipped via
  `verify_65_e8.py` (b2).

## FCT-229 — The K-Projector and the Non-Cohomological Transverse (Chapter 66)

- Status: `THEOREM / MODEL-SCOPE` (homotopy transfer credited to Kadeishvili
  1980 / Merkulov 1999 / Markl / Loday–Vallette 2012; the 50/50 split framing is
  the program's)
- Public statement: on the inhomogeneous bar cochains of the two-element group
  over F2, the operator K = δι + ιδ built from the coboundary and a one-slot
  contraction is an idempotent chain map of rank 2^{n−1} that acts as zero on
  cohomology, splitting the cochain space exactly in half into a flat sector
  containing the certified tower and a transverse sector invisible to every
  invariant. The alternating (total) contraction squares to zero, so
  Q = δ + ι_alt is a genuine total operator with Q² the Cartan law flow, and the
  tower is the stationary sector of that flow.
- Evidence: shipped — `verify_66_carry.py` (a), (b).

## FCT-230 — The Dyadic Selection Theorem (Chapter 66)

- Status: `THEOREM / MODEL-SCOPE` (Catalan-parity a clean corollary of known
  machinery — Kummer / Alter–Kubota 1972 / Deutsch–Sagan 2006; the
  three-mechanism convergence is the program's)
- Public statement: the homotopy-transferred n-ary product on the tower diagonal
  equals Catalan(n−1) mod 2 times the mirror class ω₂, hence is nonzero exactly
  when n is a power of two — the same dyadic skeleton produced independently by
  binomial parity, Catalan-tree parity, and the formal-group doubling series (the
  triangulation is the artifact control). The physical dyadic ladder remains
  CONJ/registered (Chapter 67).
- Evidence: shipped — `verify_66_carry.py` (c), (e).

## FCT-231 — The Contraction Moduli: Arity-3 Rigidity and Delayed Law Memory (Chapter 66)

- Status: `THEOREM / MODEL-SCOPE; GAUGE CAVEAT PRINTED` (BV propagator
  identification credited to Mnev 2017 / Cattaneo–Mnev / Costello–Gwilliam 2016;
  the contraction space as a moduli object appears underexplored)
- Public statement: the witnesses h with δh + hδ = K form a moduli of dimension
  117 in the degree 1–4 window (170 unknowns, rank 53); the transferred triple
  product on the tower diagonal is witness-independent (arity-3 rigidity), while
  admissible witnesses agreeing at m₂ and the m₃ diagonal first differ at m₄
  (delayed law memory). Standard theory makes the transfers A∞-isomorphic, so the
  difference is A∞-gauge; the physical claim that nature declines to quotient by
  it is registered separately (FCT-232).
- Evidence: shipped — `verify_66_carry.py` (d).

## FCT-232 — Law Memory as Non-Quotiented Homotopy Gauge (Chapter 66)

- Status: `EXTENSION / REGISTERED / CONDITIONAL` (incumbent named: BV
  propagator-independence; descriptor named: the process tensor, Pollock et al
  2018 / Milz–Modi 2021; kill condition printed)
- Public statement: the registered nature-facing wager is that a floor-native
  process could carry, in its ledger, the distinction between A∞-isomorphic
  transfers — law memory = physical non-quotienting of homotopy gauge. The
  internal structure is undetectable by the process tensor at every finite depth,
  distinct from non-Markovian memory precisely by being invisible to the
  descriptor that captures non-Markovianity. Kill condition: process-tensor
  completeness for the class of processes in question. No divergence is claimed
  as observed.
- Evidence: cited — private research corpus; in-model precondition shipped via
  `verify_66_carry.py` (d).

## FCT-233 — The Abelian Wall and the Incidence Complex (Chapter 66)

- Status: `THEOREM / MODEL-SCOPE; THREE KILLS PRINTED`
- Public statement: the linear-contraction law-flow algebra is abelian at
  n = 3, 4 (a strong closed null — noncommutativity lives only in the lifts,
  never the flows); the law tower is the chain complex of a directed path with
  laws as vertices, appearances as edges, D as past boundary and the Bockstein β
  as future boundary, and all laws homologous. Two further bets are retired in
  print: a stationary-cohomology count killed by window-dependence before it was
  claimed, and a pre-registered twin-holonomy prediction falsified publicly.
- Evidence: shipped — `verify_66_carry.py` (a); cited — private research corpus
  for the twin and incidence computations.

## FCT-234 — The Mirror Is the Quadratic Term of the Multiplicative Formal Group (Chapter 67)

- Status: `THEOREM / RECOVERY`
- Public statement: the program's mirror cocycle is exactly the nonlinear part
  of the multiplicative formal group law F(a,b) = a + b + a·b, namely a·b, and
  the dyadic tower of essential-class degrees is exactly that group's doubling
  series — `[2^k] (x) = x^{2^k}` over F2, with the lowest term of [n] (x) at
  x^{2^{v2(n)}}. An identity of arithmetic; the physical reading is separate
  (FCT-238).
- Evidence: shipped — `verify_67_chromatic.py` (checks A1, A3, A4, B1).

## FCT-235 — Three-Mechanism Carry-Depth Stratification (Chapter 67)

- Status: `THEOREM / RECOVERY`
- Public statement: the stratification "arity n first visible at resolution
  2^{s2(n)}" is produced by three independent mechanisms — binomial parity
  (Lucas/Kummer), Catalan tree parity (v2(Catalan(n−1)) = s2(n) − 1;
  Alter–Kubota, Deutsch–Sagan), and the multiplicative formal-group [n]-series —
  which agree exactly; an independent unsigned Z/16 recursive tree-transfer
  reproduces the ladder. The three-way agreement is the artifact control; the
  physical modulus-visibility prediction that rides on it is CONJ/registered
  (FCT-239).
- Evidence: shipped — `verify_67_chromatic.py` (checks D1, D2, D2b).

## FCT-236 — The Transport Group Is Nonabelian, Hence Not Height One (Chapter 67)

- Status: `THEOREM / RECOVERY`
- Public statement: the program's transport group is exactly 2T (order 24):
  τ = (1+i+j+k)/2 has τ² = (−1+i+j+k)/2 and τ³ = −1, the closure of {i, j, τ} has
  24 elements, and it is nonabelian (ij = −ji); the height-one automorphism
  structure is abelian, so the transport architecture cannot be height one.
  Referee caution PRINTED: bare nonabelianness proves nothing; the load-bearing
  evidence is the specific group match (FCT-237), which is CITED.
- Evidence: shipped — `verify_67_chromatic.py` (checks C1, C2, C3).

## FCT-237 — The 24-Cell Group as Strict Height-2 Stabilizer Maximal Subgroup (Chapter 67)

- Status: `RECOVERY / CITED`
- Public statement: per the published classification, 2T = Q8 ⋊ C3 (order 24) is
  the maximal finite subgroup of the strict height-two Morava stabilizer at
  p = 2; extending by the Galois action gives order 48 (2O) — exactly the
  24-vs-48 chirality gate the floor produced independently, the strict-vs-Galois
  split aligning with the gate as an OBSERVATION. A citation is not a check; the
  stabilizer classification is not reproduced, and the value rests on the cited
  literature (Hewett 1995; Bujard 2012; Beaudry–Goerss–Henn).
- Evidence: cited; our side (the group is exactly 2T) shipped via
  `verify_67_chromatic.py`.

## FCT-238 — The Chromatic Frame (Complex QM as the Height-One Chart) (Chapter 67)

- Status: `CONJ / EXTENSION`
- Public statement: complex quantum mechanics is conjectured to be the
  height-one chart of a height-indexed physics — complete for its stratum, blind
  above it — with the carry-depth filtration as the height stratification and the
  half-eraser as height-blindness. It goes beyond the Stolz–Teichner ladder (the
  named incumbent), which does not assert it. If the decisive formal-group-law
  extraction (OPEN) yields only the multiplicative law, the frame retreats to a
  conjecture about symmetry decoration.
- Evidence: held — the decisive extraction has not run.

## FCT-239 — ℑ₂: The Registered Central Wager (Zero Is Detector-Relative) (Chapter 67)

- Status: `CONJ / REGISTERED`
- Public statement: a height-pure physical unit ℑ₂ with ℑ₂ ≠ 0 and every
  height-one observable of it zero — "a root of invisibility" — subsuming law
  memory, essential classes, exact twins, and sector-conditional witnesses into
  one principle: zero is detector-relative. It is NOT a hidden variable (no
  present-tense information), NOT non-Markovian memory (invisible to the process
  tensor at every finite depth; kill condition = process-tensor completeness),
  and NOT a modification of height-one Born probabilities. Registered
  experimental program: commutator/rotor witness (spoof-closed via Oi 2003),
  sector-conditional correlator, two-witness delayed-law-memory protocol, the
  modulus ladder, subthreshold photonic fusion-algebra validation (Menssen 2017).
  Zero confirmed empirical divergences; entirely registered/conditional.
- Evidence: held — no measurement.

## Future Claim Template

```text
ID:
Short name:
Status:
Scope:
Evidence state:
Public statement:
Evidence:
Checks / controls:
Residuals:
Scope sensitivity:
```
