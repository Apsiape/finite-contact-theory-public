# Correction Ledger

Status: public correction ledger, first curated pass.

The correction ledger is part of the method. It records claims that were killed,
demoted, narrowed, or moved to a hold. A failed claim may leave a useful
residue, but the old headline must not survive under softer wording.

## Rule

Every correction should preserve:

1. the original or tempting statement;
2. what survived;
3. the current public status;
4. the condition under which the issue may be reopened.

## Stable Corrections And Demotions

| ID | Original or tempting statement | Correction | Current public status | Reopen only if |
|---|---|---|---|---|
| C-01 | Binary arity is uniquely forced because matter and memory coincide. | The matter-memory coincidence premise failed; memory depends on record channel, selectivity, structural excess, and survival under gates. | Withdrawn. Residue folded into FCT-04. | A binary-specific survival theorem holds after matching channel, density, substrate, composition, and coarse projections. |
| C-02 | Matter is fixed at dimension 2 across arities. | Higher-arity closed coherent structure can climb in dimension in tested engines. | Withdrawn as general premise. | Grown engines show persistent physical atoms remain dimension-2 under matched arity changes. |
| C-03 | Raw `kdim` is memory. | Raw capacity can be density or counting lens. Memory is structural excess over matched controls. | Demoted; FCT-04 replaces it. | Raw capacity predicts gate survival under strengthened controls. |
| C-04 | The present is real because it is maximally expensive to identify. | Cost asymmetry supports law-side invariants versus identity expense; present-realness is separate ontology/admission data. | Withdrawn as proof of present ontology. | A theorem links access cost to ontology without firing on arbitrary set identity. |
| C-05 | The floor predicts naive real quantum mechanics and is falsified by complex-network data. | The floor stages a composition fork. No-signaling and monoidal grammar are recovered; product law is admission data at current scope. | Corrected; see FCT-07/FCT-08. | The floor independently forces the naive real tensor product under the same network assumptions. |
| C-06 | The arrow of time, local complex structure, and complex composition are independent mysteries fully solved by one statement. | Conditional unification survives only when arrow-`J` is admitted; the arrow-`J` itself remains admission data. | Narrowed. | A floor-native mechanism forces arrow-`J`. |
| C-07 | Native one-use growth is literally a steady-state entropy-production engine. | Native one-use growth has support orientation. Thermodynamic EP belongs to recurrent/driven comparison sectors. | Corrected; see FCT-05 and FCT-14. | A native one-use floor is made recurrent without importing reverse support. |
| C-08 | Closure is the unique forced potential. | Closure-seeking is a measured dynamics class; exponent and weights are admission data and physically loaded. | Demoted; see FCT-06. | Record, composition, and coarse gates robustly select one exponent across held-out substrates. |
| C-09 | Closure dynamics selects Tsirelson. | Native closure/frame objectives do not currently climb to Tsirelson without target-guided or interface-specific structure. later private work supplied a different, scoped route: native carrier grammar at binary-Bell scope, not closure-dynamics selection. | Withdrawn as closure claim; scoped binary-Bell carrier result split into FCT-17/FCT-19. | A floor-native dynamics reaches the boundary without CHSH-guided search and survives controls. |
| C-10 | D4 time-reversal pattern asymmetry is a usable external bet. | The binary pattern instrument is underpowered/blind to strong synthetic arrows after binarization. | Withdrawn from prediction shelf. | A new instrument passes synthetic power, source-specificity, and detector-confound controls before external data. |
| C-11 | The three forks are an orthogonal basis of physics-like structure. | They are useful architecture, not an independent basis. Memory couples to transitivity and trades against the quantum gate. | Demoted to architecture. | A full factorial audit shows independence under stricter openness coordinates. |
| C-12 | Graph-floor gravity is a generic divergence from general relativity. | Graph-floor horizon-area projection survived internally, but held-out substrate probes failed portability. | Demoted to graph-floor frontier. | A pre-registered projection or theorem carries area-lock plus absent matter sourcing across distinct floors. |
| C-13 | The private research corpus gives a new nature-facing thermodynamic divergence. | Later private audits demoted this to stochastic-thermodynamics recovery plus conservation. | Withdrawn as divergence; see FCT-14. | A typed generator fingerprint survives matched CRN/rate-shuffle controls or a new arrow statistic passes bridge gates. |
| C-14 | Completed continuum is derived as a native object. | Finite skeletons and algebraic/computable horizons are recovered; completed continuum structures are admitted overlays unless reconstructed for prediction. | Corrected; see FCT-12. | A completed-real structure is required by a physical prediction with no finite reconstruction. |
| C-15 | Cardinality fragmentation `m/c` is a confirmed second R-style prediction. | Strict graph controls demoted it to a lens/recovery readout unless a physical arena survives stronger controls. | Demoted. | A domain target survives degree, triangle, component, nontransitivity, and clustering controls. |
| C-16 | Level-2 sourcing or Einstein gravity is already derived. | Energy/temperature and area-law export are scoped results; sourcing remains open and instrument-sensitive. | Narrowed; see FCT-13. | A powered within-geometry or equivalent flux/sourcing instrument passes its positive controls and real test. |

## Active Holds, Not Corrections

These are not failed claims. They are held because public wording still depends
on scope review, packaging, or further proof beyond the binary-Bell native lift:

- general almost-quantum selector theorem;
- q >= 3, more outcomes, and non-projective extensions;
- cross-site interlocking and CHSH weight selection;
- terminalized one-receiver carrier/interface mechanisms beyond model scope;
- Sliwa-41 artifacts until copied or cited precisely.

See `hold-register.md`.

## Reopening Protocol

To reopen a killed or bounded claim:

1. State the old failure.
2. Name the new projection, admission data, or interface.
3. Pre-register matched controls.
4. Define the success statistic.
5. Define the kill condition.
6. Run the smallest decisive audit first.
7. Promote only the scoped residue, not the old headline.
