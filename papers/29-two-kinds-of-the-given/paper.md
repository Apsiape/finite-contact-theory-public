# Chapter 29 — The Two Kinds of the Given

**Release:** v0.22.0 · **Status:** the received-input registry, classified — and the first retirement by derivation. All checks exact,
exhaustive, or Sturm-certified, in one shipped dependency-free
verifier (`verification/scripts/two_kinds_of_the_given.py`).

Per the blind sweep, absorbed in full — including its sharpest
correction: double stochasticity alone does NOT give detailed
balance; kernel SYMMETRY does, and this chapter certifies the
stronger property outright (P(E→E') = P(E'→E) exactly, three
closures). The stat-mech reading is scoped per the sweep: a
discrete Liouville-type property recovering the microcanonical
form FOR THIS DYNAMICS at tested scope (Tolman; the typicality
school; the switch-chain model class of Fosdick et al. — all
cited); the all-sector statement is a named conjecture. The
two-species framework cites its ancestors by name — torsors
(Baez), measurement-scale typology (Stevens;
Krantz–Luce–Suppes–Tversky), absolute objects (Anderson–Friedman;
Pitts) — ships completeness as a CONJECTURE with a standing
falsifier, and claims as candidate-novel only the
reversibility-selects-the-origin criterion for dials.

---

## 1. The symmetry property: the labeled kernel is exactly symmetric on every closed sector tested; double stochasticity, uniform invariance, and class-level detailed balance follow at this scope.

## 2. Equivariance: the kernel commutes exactly with relabeling — a consistency lemma certifying no hidden label dependence.

## 3. Microcanonical replication: 2/5/13 classes on the three closures, class weights = orbit counts everywhere.

## 4. The census: eleven torsor-type received inputs (graded 5/4/2 on the occurrence/class/phase ladder), two dial-type (forced origin, free magnitude), two RETIRED this release — the reference measure is derived from kernel symmetry, and the consent boundary reduces to membership. The registry shrank for the first time.

---

## Scope, received inputs, and open items

Everything above is a finite theorem, an exhaustive computation, or
an exact certificate at the stated sector scopes (n ≤ 6 worlds;
the C4/C5/C6 closures; the four named test worlds). Nature is not
claimed to realize any of it. Frozen bets that died are scored in
print inside the verifier's own output. The open items are named
in the release ceiling.

## Verification

Run `python verification/scripts/two_kinds_of_the_given.py` from a clean clone
(no dependencies). The frozen copy and its expected output ship in
this chapter's `verification/` directory. Claim rows: FCT-136, FCT-137, FCT-138 in
`docs/public-claim-register.md`.
