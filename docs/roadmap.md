# Roadmap

Status: historical research roadmap (written at v0.10; retained as a record).
The program has since shipped through v0.35.0 (61 chapters); the live state is
the CHANGELOG, docs/public-claim-register.md, and the README ceiling.

As of v0.10 the program had shipped v0.1.0 through v0.10.0 (ten chapters on
three recovery lines plus a first extension line). The `release-roadmap.md` file is the
historical v0.1 DOI-readiness plan; this file is the shorter research roadmap
and the current open frontier.

## Current Frontier (as of v0.10)

The nearest open items, each held open by name in the live ceiling:

- **The mixed-state PSD-exclusion (the open crux of Chapter 10).** Whether the
  registered negative-Gram count vector `(Delta_3, W, Q_3)` can be reproduced
  by any mixed-state / mode-mismatch / multiphoton Hilbert model. This is the
  gate before any nature-facing outreach and awaits an external quantum-optics
  expert; the clean math core is an SDP / real-algebraic feasibility question.
- **The Chapter-10 bridge premises**, held open: that a physical particle class
  carries `Z_2` identity holonomy; that photons receive it (the natural home is
  anyonic / topological matter, photons the clean-grammar long shot); that
  passive linear optics is the complete measurement grammar.
- **A strict-margin (`> 0`, not just `>= 0`) universal accessible-positivity
  bound**, or a tighter characterization of the accessible set than the toric
  variety (the zero-margin universal theorem is proven; a positive floor is
  open).
- **Complex quantum mechanics** — the projective phase that would complexify
  the Chapter-8 cell; and the **actuality / update law** — whether a matched
  non-Born occurrence kernel survives, or one law is forced.
- **Which world-phase is selected** (the conserved, received input of Chapter
  9) and **whether nature realizes** `E_8`, the hexacode, or any code.
- **Spacetime, gravity, the continuum, observers, and constants** — the older
  reconstruction horizon, each held open by name.

## Historical: the v0.1 release goal

Published `v0.1.0` as a scoped, citable reconstruction package:

```text
finite contact -> counting -> gluing -> rational Born -> CHSH/Pell ->
native binary-Bell carrier -> scoped quantum-boundary theorem
```

## Initial Release Work

1. Freeze the v0.1 scope and rights posture.
2. Final-review the public claim register release rows.
3. Add proof sketches and dependencies to the theorem bank.
4. Finish the mathematical core as public vocabulary.
5. Convert the paper scaffold into a real v0.1 technical note.
6. Finalize the evidence posture: shipped scripts plus cited private evidence.
7. Run source-hygiene and overclaim audits.
8. Tag `v0.1.0` and archive through Zenodo after metadata review.

## Older candidate live hinges (binary-Bell era; see Current Frontier above)

- The general almost-quantum selector beyond the binary-Bell finite carrier
  scope.
- The original repair dynamics and whether it supplies the terminalized
  two-bottleneck carrier/interface motif.
- Cross-site interlocking and whether the CHSH-weight/magic bridge can become
  floor-native rather than terminalized model-scope.
- Level-2 sourcing at the aperture where energy and temperature first exist.
- Observer-lattice registration with a frozen real-rate null.

## Public Extraction Work We Can Do Now

- Final-review FCT-01 through FCT-20 release prose.
- Add held-material rows for native lift, complementarity angle, and scoped
  binary-Bell boundary.
- Add proof sketches for non-held theorem rows.
- Choose a small verification subset or citation-only evidence package.
- Add result-ledger templates for those scripts.
- Convert the paper scaffold into a short v0.1 overview with exact scope tags
  on quantum-boundary language.
- Keep general selector, q >= 3, cross-site interlocking, and nature-facing
  quantum claims in the reconciliation register until their own gates are paid.

## Public Release Rule

The public repository should remain curated. Private drafts, raw probes, and
untriaged probes stay outside this repository until extracted.

held quantum/carrier material is tracked in
`docs/hold-register.md`. The file now
acts as a reconciliation register.
